# Revue adversariale — commits 184b2b8 → e7586f6 (état au 2026-09-09 18:07 CEST)

> Fichier **régénéré automatiquement** par `build_progress.py` depuis les journaux du workflow (sauvegarde incrémentale demandée par Romain le 2026-09-09). Source de vérité : `state.json`. Pour reprendre sans repartir de zéro : `RESUME.md`. Mandat : revue **en lecture seule** (« ne code pas ») — rien n'a été modifié dans le code.

Commits revus : 184b2b8 (perf: keep-alive + 0.15s pacing + max-pages cap; matcher ™ strip), 54f1f88 (fix: keep-alive fail-open, no-redirect before host_locked), 9ddf185 (harden: Session max_redirects=10), 0394dfc (docs: HANDOFF.md), e7586f6 (docs: HANDOFF §7 commands) = HEAD

## 0. Avancement

| Étape | État |
|---|---|
| Chasseurs (5 lentilles) | parity ✓ (6), ops ✓ (10), security ✓ (5), tests ✓ (11), matcher ✓ (6), documentation ✓ (12), the ✓ (8), test ✓ (12) — parity/ops réutilisés du journal du 2026-09-08 |
| Dédoublonnage | dédoublonnage 77 → 42 |
| Vérification (3 lentilles/finding, majorité) | **38 gardés**, 7 réfutés, 0 indécis/en attente |
| Critique de complétude | fait — 3 nouveau(x) finding(s) |
| Agents (kind:status) | critic:done=1, critic:failed=2, dedup:done=1, dedup:failed=1, finder:done=10, finder:failed=3, verify:done=267, verify:failed=48 |

## 1. Findings confirmés (38) — classés par sévérité

### [major/CLOSED] Default --max-pages 30 makes every deep-feed sweep a fail-closed batch halt (exit 2, later targets never swept)
`scripts/10_data_entry_auto.py:296` — lentille(s) : ops · origine : prior-run-2026-09-08

**Constat.** With the new default cap, any merchant whose feed advertises >30 pages ends run_sweep with halted='coverage_incomplete_max_pages …' (src/data_entry_auto.py:288-289); scripts/10 treats any non-operator halt as a batch-stopping fail-closed halt (`break` at :406-408) and exits 2 (:423), and the admin /auto tab renders returncode!=0 as 'failed' (src/admin/submit_manager.py:991) with the pill 'ARRÊTÉ — …' (src/admin/static/auto.js:175). The commit/CHANGELOG only say the flag 'stays honest'; they never mention that the default now halts the batch on exactly the deep-feed merchants the cap targets.

**Scénario.** `python3 scripts/10_data_entry_auto.py --targets Kinguin:58,Eneba:19` (no --max-pages) against a 60-page Kinguin feed → Kinguin pages 30→1 are processed, recap.halted='Kinguin: coverage_incomplete_max_pages (feed has 60 pages)', Eneba is NEVER swept, exit code 2, console shows the run as failed. Same from the /auto tab with 'Max pages' left empty.

**Preuve.** Experiment (scratchpad/opsdocs/multi_target.py, main() with run_sweep stubbed to the recap real code produces for a capped 60-page feed — the cap→halted mapping is covered by tests/test_data_entry_auto.py:328-333): `exit code: 2 | run_sweep calls: [('Kinguin', 30, 1)] | targets in recap: ['Kinguin'] | batch halted: Kinguin: coverage_incomplete_max_pages (feed has 60 pages)`. Code: scripts/10 :406 `if sweep.get("halted") and sweep["halted"] != "operator_stop": recap["halted"] = …; break`, :423 `return 0 if recap["halted"] in (None, "operator_stop") else 2`.

**Piste (non appliquée).** Distinguish a benign coverage cap from a fail-closed halt: e.g. record it in a separate recap field (coverage='incomplete_max_pages') or exclude `coverage_incomplete_*` from the batch-break and exit-2 rule in scripts/10 (and the admin 'failed' mapping), while keeping the honest flag. Alternatively keep default=None (= full feed) and make 30 an explicit opt-in.

**Votes.**
- reproduce : confirmé (c=0.95, sév. major, dir. CLOSED) — Reproduced with the REAL scripts/10 main() and the REAL src/data_entry_auto.run_sweep (only _make_stages stubbed with the suite's own FakeStages: Kinguin = 60-page feed, Eneba = 5-page feed, every page clean, 0 candidates). `--targets Kinguin:58,Eneba:19 --run-id repro` with NO --max-pages → exit code 2; recap.halted = 'Kinguin: coverage_incomplete_max_pages (feed has 60 pages)'; Kinguin pages 30→1 processed (n=30); Eneba NEVER swept (targets list contains only Kinguin). Control with `--max-pages 60` → exit 0, halted None, Kinguin 60→1 and Eneba 5→1 both swept. Reversed order `Eneba:19,Kinguin:58` → Eneba sweeps, Kinguin caps, exit 2 (batch marked halted). Code path: scripts/10_data_entry_auto.py:296 `default=30` (was `default=200`, source.diff line 27→29, commit 184b2b8); src/data_entry_auto.py:196-197 `top = min(feed_last, start+max_pages-1); capped = feed_last > top`, :287-289 sets re
- reread : confirmé (c=0.92, sév. major, dir. CLOSED) — Re-read at HEAD, not from the finding's quotes. (1) scripts/10_data_entry_auto.py:296-297 — `--max-pages` default is now 30 (pre-commit `git show 184b2b8~1:scripts/10_data_entry_auto.py:295` had default=200). (2) src/data_entry_auto.py:196-197 `top = min(feed_last, start_page + max_pages - 1)`, `capped = feed_last > top`; :287-289 sets `recap["halted"] = "coverage_incomplete_max_pages (feed has N pages)"` whenever capped and nothing else halted — so with start_page=1 any feed advertising >30 pages ends with a non-None halted. (3) scripts/10:406-408 breaks the target loop on ANY halted != "operator_stop" (comment says it is meant for session/login failures) and :423 returns 2; this logic is unchanged since pre-commit (:398/:414 identical) — so the mechanism is pre-existing, but the default change makes it fire on the ordinary case the commit itself targets: CHANGELOG.md:48-53 says the cap
- materiality : confirmé (c=0.82, sév. major, dir. CLOSED) — Technically true and materially reachable on the documented production path. (1) The commit under review changed the default from 200 to 30 (scripts/10_data_entry_auto.py:296; `git show 184b2b8`), and src/data_entry_auto.py:287-289 turns any cap-over-feed into recap['halted']='coverage_incomplete_max_pages …'. (2) scripts/10:404-408 treats every non-operator halt as a batch-stopping fail-closed halt (`break`) — the code comment justifies this with 'a broken session / login bounce affects every subsequent merchant', which a benign coverage cap is not — and :423 returns exit 2. (3) The admin supervisor maps any non-zero returncode to 'failed' (src/admin/submit_manager.py:991) and auto.js:175 renders 'ARRÊTÉ — …'. (4) Reachability: docs/HANDOFF.md §7 (commit e7586f6) documents exactly `python3 scripts/10_data_entry_auto.py --targets "Kinguin:58,Eneba:70" --run-id <id>   # défaut --max-pages


### [major/OPEN] [security] Staff-UA host guard parses with urlsplit but the keep-alive backend connects with urllib3.parse_url — a backslash in the authority passes the guard and AKS/Staff is sent off-domain (verified locally)
`src/aks_env.py:400` — lentille(s) : security · origine : finder:security

**Constat.** The P2-15 guard `_allkeyshop_host(nxt)` (line 400, and the first-hop check at line 471 / scripts/11 `extract_slug` line 102) derives the host from `urlsplit(url).hostname`, whose netloc scanner stops only at `/?#` (`urllib.parse._splitnetloc`: `for c in '/?#'`), so for `http://evil\@www.allkeyshop.com/x` the hostname is `www.allkeyshop.com`. requests hands the SAME string to urllib3 `parse_url`, whose authority regex is `(?://([^\\/?#]*))?` — it stops at the backslash, so authority = `evil`, path = `\@www.allkeyshop.com/x`, and the request (with `User-agent: AKS/Staff`) is sent to `evil`. The urllib fallback fails closed on the same input (`_splithost` keeps `evil\@www.allkeyshop.com` as the host → DNS error). So the keep-alive backend is NOT 'provably fail-closed-equivalent' on the one invariant the host lock exists for.

**Scénario.** Staff-UA resolve (matcher/scripts-11/gate follow mode) hits an AKS page whose 301/302 `Location:` is `https://attacker.tld\@www.allkeyshop.com/blog/…` (hostile/compromised AKS response, CDN/MITM in the chain), or an operator pastes such a URL into scripts/11 `--urls` (its `extract_slug` uses the same `_allkeyshop_host`). Keep-alive: guard passes, `_SESSION.request(GET, nxt)` connects to attacker.tld and sends `AKS/Staff`; the attacker's 200 body is then parsed as an AKS product page (ok=True, status 200). urllib: `<urlopen error Name or service not known>`, ok=False.

**Preuve.** Local experiment (two http.servers, `_allkeyshop_host` patched to hostname=='127.0.0.1', evil server bound as `localhost:EVIL`): B1 keep-alive after `/bs-redirect` (301 → `http://localhost:EVIL\@127.0.0.1:ALLOWED/landed`): `ok:true status:200 body:'EVIL'`, evil server log `["EVIL", "/%5C@127.0.0.1:51413/landed", {"Host": "localhost:41305", "User-agent": "AKS/Staff", …}]`; urllib on the same: `ok:false status:null error:'<urlopen error [Errno -2] Name or service not known>'`, no request reached evil. B2 first-hop `http_get('http://localhost:EVIL\@127.0.0.1:ALLOWED/landed', user_agent=AKS_STAFF_UA)` → keep-alive `body:'EVIL'` with `User-agent: AKS/Staff` at evil; urllib fails closed. `urlsplit(...).hostname == '127.0.0.1'`; `urllib3.util.parse_url(...)` → `http://localhost:41305/%5C@127.0.0.1:51413/landed`. Code: src/aks_env.py:399-400 `nxt = urljoin(current, location)` / `if not _allkeyshop_host(nxt):`; :454 `host = (urlsplit(url).hostname or "").lower()`; :471 `if user_agent == AKS_STAFF_UA and not _allkeyshop_host(url):`; urllib3 util/url.py:17-19 `_URI_RE … r"(?://([^\\/?#]*))?"` (same regex in urllib3 1.26 on bookworm). Existing test `test_host_locked_refuses_off_domain_redirect` only covers a plain `https://evil.tld/x` Location.

**Piste (non appliquée).** Make the guard use the parser the backend connects with (or both): in `_allkeyshop_host` (or a keep-alive-specific check before every `_SESSION.request`) refuse when `urllib3.util.parse_url(url).host` differs from `urlsplit(url).hostname`, and/or refuse any URL containing `\\` (or whose netloc contains anything but `[A-Za-z0-9.\-:]`). Add a regression test with `https://evil.tld\\@www.allkeyshop.com/x` as Location and as first-hop URL asserting StaffUaRedirectRefused / ValueError and that the mocked session never receives an off-domain URL.

**Votes.**
- reproduce : confirmé (c=0.95, sév. major, dir. OPEN) — Reproduced at HEAD with the REAL, UNPATCHED guard (no need to fake the host check): src/aks_env.py:453-455 `_allkeyshop_host` uses `urlsplit(url).hostname`, which for `http://127.0.0.1:EVIL\@www.allkeyshop.com/landed` returns `www.allkeyshop.com` (guard → True), while urllib3 2.3.0 `parse_url` (regex `_URI_RE = r"(?://([^\\/?#]*))?"` — authority stops at the backslash) yields host `127.0.0.1`, port EVIL, path `/%5C@www.allkeyshop.com/landed`. Experiment (/tmp/.../verify/f21-reproduce/repro.py, two 127.0.0.1 http.servers): B2 first-hop `http_get(bad, user_agent=AKS_STAFF_UA)` via the keep-alive backend (aks_env.py:471 check passes, :411-412 `_SESSION.request(... allow_redirects=True)`) → `{"ok": true, "status": 200, "body": "EVIL"}` and the EVIL server logged `('/%5C@www.allkeyshop.com/landed', {'User-agent': 'AKS/Staff'})`. B1 redirect hop (AKS stand-in on 127.0.0.1 allowed only for the 
- reread : confirmé (c=0.95, sév. major, dir. OPEN) — Re-read at HEAD: src/aks_env.py:453-455 `_allkeyshop_host` uses `urlsplit(url).hostname`; the keep-alive host-locked loop at :392-405 computes `nxt = urljoin(current, location)`, checks `_allkeyshop_host(nxt)`, then hands the same string to `_SESSION.request(...)` (:394), which requests re-parses with urllib3 `parse_url`. The first-hop check at :471 and scripts/11_data_entry_by_urls.py:102 `extract_slug` use the same guard. Pre-commit (`git show 184b2b8~1:src/aks_env.py` :335-336, :312) had the identical urlsplit guard but only the urllib backend, which fails closed on this input — so the divergence is INTRODUCED by 184b2b8 (keep-alive backend), not pre-existing. Independent repro (scratchpad verify/f21-reread/repro.py, two loopback http.servers, guard patched to hostname=='127.0.0.1', Location `http://localhost:EVIL\@127.0.0.1:ALLOWED/landed`): `urlsplit(...).hostname == '127.0.0.1'` bu
- materiality : RÉFUTÉ (c=0.72, sév. minor, dir. OPEN) — Technically true and reproduced locally (keep-alive: ok=True/200 body 'EVIL', evil server received User-Agent AKS/Staff with Cookie=None; urllib: ok=False DNS error). Parser divergence confirmed: urlsplit hostname 'www.allkeyshop.com' vs urllib3 parse_url host 'evil.tld' for 'https://evil.tld\@www.allkeyshop.com/blog/x'. But materiality here is low: (1) the invariant gate and scripts/03 first-hop probe use follow_redirects=False on the constant AKS_DIRECT_URL (src/invariants.py:66-70, scripts/03_match.py:72) and the keep-alive backend handles no-redirect before host_locked (src/aks_env.py:380-389), so no hostile Location can reach the guard and no write stage can be unlocked; (2) matcher slug-guess URLs are built from constants (src/matcher.py:65,73), so the only attacker input is a Location header from an already-compromised AKS origin/CDN or TLS MITM — an attacker in that position alre


### [minor/CLOSED] Host-locked keep-alive loop follows only 9 redirects (urllib follows 10) — 10-hop same-domain chain diverges
`src/aks_env.py:392` — lentille(s) : parity+ops · origine : prior-run-2026-09-08

**Constat.** `for _ in range(10)` counts REQUESTS, not redirects: the 10th iteration fetches URL9 and if that is still a 3xx the loop falls into `else: raise URLError(...)` without ever fetching the 200. urllib's HTTPRedirectHandler (max_redirections=10) and the requests non-locked path (_SESSION.max_redirects=10) both follow 10 redirects and fetch the 11th URL. So the two backends of the same seam disagree on an exactly-10-hop same-domain chain, contradicting the line-341 comment 'keeps the backends provably fail-closed-equivalent' and docs/CHANGELOG.md:29 'parité fail-closed exacte'. The new test `test_session_matches_urllib_redirect_ceiling` only asserts the Session attribute, which this loop never consults. [merged, ops lens] The host-locked loop `for _ in range(10)` spends its first iteration on the initial URL, so a chain of exactly 10 same-domain redirects ending in 200 yields URLError (ok=False) on keep-alive but 200 (ok=True) on urllib. On >10-hop or self-looping 301/302 chains urllib raises HTTPError('redirect error') with code 302, which http_get maps to ok=True/status=302 (302 ∈ ACCEPTED_AKS_STATUSES, :129), whereas keep-alive gives ok=False/status=None. All production callers require status==200 and a body, so both fail closed at resolve_aks/resolve_pinned/search level; the only behavioural delta is scripts/11 resolve_pinned retrying 3× (status None is 'transient', :69) where urllib would not. test_session_matches_urllib_redirect_ceiling only asserts the Session attribute, not the manual host-locked loop.

**Scénario.** Staff-UA resolve probe (src/matcher.py:1416/1440, scripts/11 resolve_pinned) hits a same-domain 301/307 chain of exactly 10 hops ending in 200 → urllib: ok=True status=200 body=page (11 requests); keep-alive: URLError → ok=False status=None (10 requests) → matcher raises AksProbeUnreliable → offer skipped 'AKS probe unreliable (throttled?)' instead of resolving. Pathological on AKS; fail direction is CLOSED (unnecessary skip), never a wrong entry. [merged, ops lens] AKS staff-UA resolve probe meets a 10-hop same-domain canonicalisation chain → urllib backend resolves the page, keep-alive backend raises AksProbeUnreliable (over-abort, no wrong entry). Pathological; never observed on AKS.

**Preuve.** Local 127.0.0.1 matrix, mode 'GET follow STAFF(lock)': `chain 301 x10 => DIFF ok,status,n_req,body / keepalive: ok=False st=None n=10 raw=URLError err='<urlopen error <urlopen error too many redirects (host-locke' / urllib: ok=True st=200 n=11 raw=resp:200 body='CHAIN-END'`; same for `chain 307 x10`. Non-locked mode in the same run: `chain 301 x10 => same` (both 200, n=11) and `x11` refused on both — proving the Session cap matches urllib but the hand-rolled loop is one short. [merged, ops lens] Local experiment (scratchpad/opsdocs/parity.py, 127.0.0.1 server, _allkeyshop_host patched): `staff+follow /chain/302/10  keepalive=(False, None, 'TOOMANY') urllib=(True, 200, '', 'CHAIN-END')`; `staff+follow /chain/302/11 keepalive=(False, None, 'TOOMANY') urllib=(True, 302, 'TOOMANY')`; `staff+follow /loop keepalive=(False, None) urllib=(True, 302)`; `chrome+follow /chain/302/11 keepalive=(False, None, 'Exceeded 10 redirects') urllib=(True, 302)`. 5 ok-diverging cells out of 63; all diverge in the fail-closed direction.

**Piste (non appliquée).** Count redirects explicitly (allow 10 redirects then one final fetch, i.e. `range(11)` or a `redirects` counter compared against 10), and add a real-chain test (10 hops → 200, 11 hops → refused) instead of only asserting `_SESSION.max_redirects`. Alternatively soften the CHANGELOG/9ddf185 wording to 'equivalent for every production caller (status==200 required)'. Optionally add a table-driven parity test against a local http.server like the experiment.

**Votes.**
- reproduce : confirmé (c=0.95, sév. minor, dir. CLOSED) — Reproduced at HEAD (e7586f6) against a local 127.0.0.1 http.server with `_allkeyshop_host` patched to accept the loopback host, calling the real `src.aks_env.http_get` through each backend (`_http_open_keepalive` vs `_http_open_urllib`). Result for a same-domain chain of exactly 10 redirects ending in 200, staff UA (host_locked=True): keepalive=(ok=False, status=None, 10 requests, error='too many redirects (host-locked)') vs urllib=(ok=True, status=200, 11 requests, body='CHAIN-END'). Same for 307 x10. A 9-hop chain matches on both (200, 10 requests). Non-locked (Chrome UA) mode matches on the 10-hop chain (both 200, 11 requests), proving `_SESSION.max_redirects=10` does align with urllib's `HTTPRedirectHandler.max_redirections` (=10, verified) but the hand-rolled loop at src/aks_env.py:392 (`for _ in range(10)`) counts requests, not redirects: iteration 10 fetches URL9, and a 3xx there 
- reread : confirmé (c=0.95, sév. minor, dir. CLOSED) — Re-read at HEAD confirms the claim. src/aks_env.py:392-411: the host_locked branch does `for _ in range(10)` where EACH iteration issues one request (iteration 1 = initial URL). After the 10th request (URL9) still returning 3xx, the for/else raises URLError("too many redirects (host-locked)") — so only 9 redirects are followed. urllib (Python 3.13 HTTPRedirectHandler, inspected in-session): the check `len(visited) >= self.max_redirections` (10) only fires on the 11th redirect, i.e. 10 redirects are followed and the 11th URL is fetched; the urllib host-locked fallback `_StaffUaHostGuardRedirectHandler` calls `super().redirect_request` (aks_env.py:321) and therefore inherits that ceiling. The non-locked requests path uses `_SESSION.max_redirects = 10` (aks_env.py:341) which does follow 10 redirects — so the hand-rolled loop is the odd one out. This loop was INTRODUCED by 184b2b8 (pre-commi
- materiality : RÉFUTÉ (c=0.85, sév. nit, dir. CLOSED) — Technically accurate: src/aks_env.py:392 `for _ in range(10)` issues 10 requests, i.e. follows 9 redirects, while urllib's HTTPRedirectHandler (max_redirections=10) and `_SESSION.max_redirects = 10` (aks_env.py:342, non-locked path) follow 10. The two backends therefore disagree only on an exactly-10-hop same-domain 3xx chain ending in 200 (and on >10-hop/loop chains where urllib surfaces HTTPError(302) → ok=True/status=302 vs keep-alive URLError → ok=False/None). Materiality for this project is nil: (1) the invariant gate runs staff UA + follow_redirects=False, which takes the `if not follow_redirects` branch (aks_env.py:377-388) before host_locked — the divergent loop is never reached, so it cannot flip the Debian gate green. (2) Every production consumer of the host-locked path requires `probe.ok and probe.status == 200 and probe.body`: src/matcher.py:1372 (search — returns []), :1417


### [minor/NEUTRAL] Exhausted redirect chain / loop: keep-alive reports status=None, urllib reports status=3xx with ok=True for 301/302 — only scripts/11 distinguishes
`scripts/11_data_entry_by_urls.py:128` — lentille(s) : parity · origine : prior-run-2026-09-08

**Constat.** When the redirect ceiling is hit, urllib raises HTTPError(<last 3xx>) which http_get maps to status=3xx and ok=(code in ACCEPTED_AKS_STATUSES) → ok=True for a 301/302 chain; the keep-alive backend raises TooManyRedirects→URLError (non-locked) or its own URLError (locked) → status=None, ok=False. Loop detection also differs: urllib's max_repeats=4 stops an A↔B loop after 8 followed redirects (9 requests) while requests follows 10 (11 requests). Grepping every caller: matcher (status==200 required), difmark/IG (status==200), cdp_client (probe.ok only, then JSON parse fails on the 3xx body) all end fail-closed either way. The ONE caller that branches on None-vs-3xx is scripts/11 resolve_pinned: `transient = probe.status is None or probe.status in _TRANSIENT_RESOLVE_STATUSES` — keep-alive's None is treated as transient (3 attempts x 1.5s backoff), urllib's 3xx fails immediately. Both end in AksProbeUnreliable; no fail-open.

**Scénario.** scripts/11 pinned URL on a >=10-hop or looping same-domain redirect: keep-alive backend retries the whole chain 3 times (~3s extra + 3x the requests) before failing closed; urllib backend fails on the first attempt with 'HTTP Error 301: The HTTP server returned a redirect error that would lead to an infinite loop'. Different skip reason text and request volume, same final outcome.

**Preuve.** Matrix rows: `[GET follow plainUA] chain 301 x11 => DIFF ok,status,body / keepalive: ok=False st=None n=11 raw=URLError / urllib: ok=True st=301 n=11 raw=HTTPError(301)`; `loop a<->b => DIFF ok,status,n_req / keepalive: ok=False st=None n=11 / urllib: ok=True st=302 n=9`; identical pattern for `[HEAD follow]` and `[GET follow STAFF(lock)]` (keep-alive n=10 there). urllib source: `if (visited.get(newurl, 0) >= self.max_repeats or len(visited) >= self.max_redirections): raise HTTPError(req.full_url, code, self.inf_msg + msg, headers, fp)`.

**Piste (non appliquée).** Document the divergence in the _http_open docstring; optionally make http_get force ok=False when the 3xx is urllib's loop/ceiling HTTPError (message starts with inf_msg) so both backends report ok=False, and decide explicitly whether scripts/11 should treat a redirect-exhaustion as transient.

**Votes.**
- reproduce : confirmé (c=0.93, sév. minor, dir. NEUTRAL) — Reproduced end-to-end with a scripted 127.0.0.1 server (infinite same-path 301 chain + A<->B 302 loop) against the REAL http_get/_http_open at HEAD, toggling backends via aks_env._SESSION (keep-alive vs None=urllib) and exercising host_locked by monkeypatching _allkeyshop_host in memory only:

keepalive chain301 plain: ok=False status=None n_req=11 err="Exceeded 10 redirects."
keepalive chain301 staff: ok=False status=None n_req=10 err="too many redirects (host-locked)"
keepalive loop302 plain: ok=False status=None n_req=11 ; staff: n_req=10
urllib    chain301 plain/staff: ok=True status=301 n_req=11 err="HTTP Error 301: ...would lead to an infinite loop"
urllib    loop302 plain/staff: ok=True status=302 n_req=9

Mechanism confirmed in code: src/aks_env.py:369-426 (_http_open_keepalive raises URLError("too many redirects (host-locked)") at the for/else, and wraps requests.TooManyRedirect
- reread : confirmé (c=0.92, sév. minor, dir. NEUTRAL) — Re-read at HEAD confirms the claim. src/aks_env.py:369-426 (_http_open_keepalive): the non-locked path lets requests raise TooManyRedirects which the blanket `except Exception` at :414 converts to URLError; the host_locked path loops 10 times and raises URLError("too many redirects (host-locked)") at :409. http_get :488-489 maps URLError to status=None, ok=False. src/aks_env.py:428-436 (_http_open_urllib, identical to `git show 184b2b8~1:src/aks_env.py` lines 320-333): urllib's HTTPRedirectHandler raises HTTPError(<last 3xx>, inf_msg) at the ceiling, which http_get :474-486 maps to status=3xx and ok=(code in ACCEPTED_AKS_STATUSES=(200,301,302)) -> ok=True for 301/302 (StaffUaRedirectRefused is the only forced-False case). The divergence did not exist before 184b2b8 (single urllib backend), and 9ddf185's comment at :338-341 only claims equivalence for 303/307/308, silently conceding 301/3
- materiality : RÉFUTÉ (c=0.88, sév. nit, dir. NEUTRAL) — Technically true but immaterial. The divergence only occurs on a follow-redirects probe that exhausts urllib's 10-hop/4-repeat ceiling, and the keep-alive backend (the production path — requirements.txt installs requests) is the STRICTER side (ok=False, status=None). Every follow-mode caller except cdp_client requires status==200 (src/matcher.py:1372/1417/1441, src/merchants/difmark.py:171/181, src/merchants/instant_gaming.py:140, scripts/11_data_entry_by_urls.py:124/133), so a urllib ok=True/3xx is rejected the same way. The invariant gate (src/invariants.py:66) and scripts/03_match.py:72 use follow_redirects=False, so no chain is followed and the exhaustion path is unreachable there. In scripts/11 the only observable difference is that keep-alive's status=None enters the 3x1.5s transient retry (line 128) while urllib fails on attempt 1; both raise AksProbeUnreliable — no write unlocked


### [minor/NEUTRAL] /auto tab placeholder '(tout le feed)' now lies — empty field means 30 pages
`src/admin/static/auto.html:77` — lentille(s) : ops · origine : prior-run-2026-09-08

**Constat.** The console's Max-pages input advertises 'whole feed' when left empty, but the manager only passes --max-pages when set (src/admin/submit_manager.py:735-736), so an empty field now resolves to the script default 30 (and, per finding 1, a 'failed' run on any >30-page feed).

**Scénario.** Operator launches a default sweep from /auto on a 100-page merchant expecting the whole feed → 30 pages processed, run shown as ARRÊTÉ/failed.

**Preuve.** auto.html:77 `<input id="max-pages" … placeholder="(tout le feed)">`; auto.js:86 `if (mp > 0) body.max_pages = mp;`; submit_manager.py:735 `if max_pages is not None: argv += ["--max-pages", str(int(max_pages))]`; scripts/10:296 `default=30`.

**Piste (non appliquée).** Change the placeholder to '30 (défaut)' or have the manager pass an explicit full-feed value when the field is empty.

**Votes.**
- reproduce : confirmé (c=0.95, sév. minor, dir. NEUTRAL) — Reproduced end-to-end with the real code at HEAD (scratch script /tmp/claude-0/-root-aks-code-executor/fb12d4f4-2a87-4577-a2d8-a5d07afbcaa7/scratchpad/verify/f7-reproduce/repro.py). An /auto launch body with the Max-pages field empty (auto.js:86 only sets body.max_pages when mp > 0) gives `_parse_int(None) -> None` (src/admin/app.py:84-92); `SubmitManager.start_data_entry_auto(..., max_pages=None)` builds argv `[..., '--targets', 'Kinguin:58', '--run-id', ...]` with NO `--max-pages` (src/admin/submit_manager.py:735-736); feeding that argv through scripts/10_data_entry_auto.py's real argparse yields `args.max_pages == 30` (scripts/10:296, changed 200 -> 30 in 184b2b8); `run_sweep` on a 100-page FakeStages feed processed exactly 30 pages (30 -> 1), `recap.halted == 'coverage_incomplete_max_pages (feed has 100 pages)'`, exit code 2 (scripts/10:422), and the auto.js:175 pill renders `ARRÊTÉ 
- reread : confirmé (c=0.85, sév. minor, dir. NEUTRAL) — Re-read at HEAD confirms every link of the claim. src/admin/static/auto.html:77 still reads `placeholder="(tout le feed)"` (byte-identical to `git show 184b2b8~1:src/admin/static/auto.html`, i.e. untouched by the 5 commits). auto.js:86 only sets `body.max_pages` when `mp > 0`; app.py:575 forwards `_parse_int(body.get("max_pages"))` (None when absent); submit_manager.py:735-736 appends `--max-pages` only `if max_pages is not None`, so an empty field leaves the CLI default in force. That default moved from 200 (pre-commit, scripts/10 line 295) to 30 (HEAD scripts/10_data_entry_auto.py:296) in 184b2b8 — the only file in this diff-set touching the pipeline (`git diff --stat 184b2b8~1 HEAD` on static/, scripts/10, submit_manager: only scripts/10 changed). Consequence chain is real: src/data_entry_auto.py:196-197 computes `top = min(feed_last, start+max_pages-1)`, `capped = feed_last > top`, a
- materiality : confirmé (c=0.7, sév. minor, dir. CLOSED) — Technically confirmed and reachable in production via the documented /auto tab (docs/HANDOFF.md:22 lists `/auto` as an operator entry point). Chain: src/admin/static/auto.html:77 placeholder "(tout le feed)"; auto.js:86 only sends max_pages when >0; app.py:575 passes `_parse_int(body.get("max_pages"))` (None when empty); submit_manager.py:735-736 only appends `--max-pages` when not None; scripts/10_data_entry_auto.py:296 `default=30` (changed from 200 in 184b2b8, which did not touch auto.html — last change there was 91d9050). So an empty field now means 30 pages, and since the commit's own rationale says feeds are typically deeper than ~30 pages, the label is wrong for the common case (before the commit, default 200 made it practically true). Consequence when the cap is hit: src/data_entry_auto.py:287-289 sets halted="coverage_incomplete_max_pages (feed has N pages)"; scripts/10:406-408 


### [minor/NEUTRAL] CONTRIBUTING.md still says 'standard library only … no third-party dependencies … No virtualenv is required'
`docs/CONTRIBUTING.md:19` — lentille(s) : ops · origine : prior-run-2026-09-08

**Constat.** Stale after requirements.txt (optional requests). README/ARCHITECTURE/DATA_CONTRACTS/INSTALL_ADMIN stdlib statements are still accurate because they are scoped (admin app, CDP client, contracts) — CONTRIBUTING is the one project-wide statement left.

**Scénario.** A new contributor reads CONTRIBUTING, never installs requests, and benchmarks/operates on the urllib fallback.

**Preuve.** docs/CONTRIBUTING.md:19-21 `**Python 3.10+**, standard library only. There are **no third-party dependencies**, and none may be added without Romain's explicit approval (`AGENTS.md`). No virtualenv is required for the current code.`

**Piste (non appliquée).** One sentence pointing to requirements.txt as the optional accelerator.

**Votes.**
- reproduce : confirmé (c=0.95, sév. minor, dir. NEUTRAL) — Verified at HEAD e7586f6: docs/CONTRIBUTING.md:19-21 reads verbatim "**Python 3.10+**, standard library only. There are **no third-party dependencies**, and none may be added without Romain's explicit approval (`AGENTS.md`). No virtualenv is required for the current code." Meanwhile requirements.txt exists (introduced in 184b2b8, `requests>=2.28`, described as the OPTIONAL keep-alive accelerator) and src/aks_env.py:330-345 imports `requests` behind a try/except with a urllib fallback. CONTRIBUTING.md was last modified in 5384054 (before 184b2b8), so it was not updated when the dependency landed. The other project-level docs were updated: README.md:176-180 ("The sole **optional** dependency is `requests` (`requirements.txt`)") and docs/HANDOFF.md:37-38 ("`pip install -r requirements.txt` (optionnel ...)"), plus CHANGELOG.md:43-45. The remaining stdlib statements in ARCHITECTURE.md:139 (CD
- reread : confirmé (c=0.92, sév. minor, dir. NEUTRAL) — Confirmed at HEAD. docs/CONTRIBUTING.md:19-21 (Setup section) still reads "**Python 3.10+**, standard library only. There are **no third-party dependencies**, and none may be added without Romain's explicit approval (`AGENTS.md`). No virtualenv is required for the current code." Commit 184b2b8 (under review) added requirements.txt declaring `requests>=2.28` as an OPTIONAL accelerator for src/aks_env.py keep-alive (src/aks_env.py:330-344 try-imports requests, falls back to urllib). `git diff 184b2b8~1 HEAD --stat -- docs/CONTRIBUTING.md` is empty: CONTRIBUTING was not touched by any of the 5 commits (last edit 5384054, before the perf commit). The other docs WERE updated in the same series: README.md:178 ("dependency is `requests` (`requirements.txt`)..."), docs/HANDOFF.md:37-38 ("`pip install -r requirements.txt` (optionnel — `requests` pour le keep-alive ; fallback urllib sinon). Le cœu
- materiality : RÉFUTÉ (c=0.7, sév. nit, dir. NEUTRAL) — The finding is factually true: docs/CONTRIBUTING.md:19-21 still says "standard library only … no third-party dependencies … No virtualenv is required", while requirements.txt (184b2b8) introduces `requests>=2.28` as an OPTIONAL accelerator. But it is immaterial under the ops/materiality lens for this project: (1) Fail direction is NEUTRAL/CLOSED, never OPEN — src/aks_env.py:343-344 falls back to the urllib path when requests is absent ("identical behavior, just no keep-alive"), so a contributor who skips the install gets slower probes, not a different gate result, not an unlocked write, not a wrong offer. (2) Not production-reachable: production installs requests via requirements.txt, and BOTH operator entry points are already correct — docs/HANDOFF.md:37 ("`pip install -r requirements.txt` (optionnel — `requests` pour le keep-alive ; fallback urllib sinon)") and README.md:177-181 ("sole


### [minor/NEUTRAL] Docs contradict each other on whether the request RATE changed and on the ban history that justifies the change
`requirements.txt:9` — lentille(s) : ops · origine : prior-run-2026-09-08

**Constat.** requirements.txt:9-10 and src/aks_env.py:326-327 claim 'NO change to the request count or rate → ban-safe', but the same commit halves AKS_PROBE_DELAY_S so the sustained rate to allkeyshop.com rises from ≈138 to ≈255 measured / ≈330 theoretical req/min (src/matcher.py:79-85, CHANGELOG:52 '~255 req/min'). The safety premise 'AKS/OVH only ever banned under concurrent-browser load, not probe rate' (matcher.py:82-83, CHANGELOG:36) is contradicted by src/invariants.py:58-63, which attributes the 2026-08-28 IP ban to a bot-like plain-Chrome health probe pattern. Latency figures also disagree ('~134ms → ~32ms' in aks_env.py/requirements.txt vs '~30-84ms' in CHANGELOG:40). EXECUTOR_RULES/AGENTS/CLAUDE prescribe no probe rate (only the extractor's --pace 2-5 s page pacing, EXECUTOR_RULES:165, unchanged), so the CHANGELOG is the only record of this policy change.

**Scénario.** A future audit/operator reads requirements.txt or aks_env.py and concludes the request rate is unchanged, then adds concurrency 'since rate never changed' — the real rate already ~doubled.

**Preuve.** requirements.txt:9-10 `(~134ms → ~32ms per request), with NO change to the request count or rate`; src/matcher.py:83 `AKS/OVH only ever banned under concurrent-browser load, not probe rate`; src/invariants.py:60-61 `check from a datacenter IP looks bot-like and — run before EVERY stage — likely tripped AKS/OVH's WAF into an IP ban (2026-08-28)`. Rate arithmetic: 60/(0.15+0.032)≈330/min vs 60/(0.3+0.134)≈138/min.

**Piste (non appliquée).** State in requirements.txt/aks_env.py that keep-alive alone keeps the rate, and that the 0.15 s pacing is what raised it (to ~255-330 rpm, serial); reconcile the ban attribution in one place.

**Votes.**
- reproduce : confirmé (c=0.75, sév. minor, dir. NEUTRAL) — Doc finding; I opened every cited line at HEAD (e7586f6) plus the surrounding docs and the 2026-08-28 commit. The inconsistency is real.

(1) Rate claim. requirements.txt:9-10 ("with NO change to the request count or rate"), src/aks_env.py:326-327 ("no change to the request COUNT or rate -> ban-safe") and docs/HANDOFF.md:71-72 ("Concurrence de resolution : SEUL levier ... c'est le seul qui augmente le debit de requetes AKS") all say the AKS request rate is unchanged / only concurrency could raise it. The same commit 184b2b8 sets AKS_PROBE_DELAY_S 0.3 -> 0.15 (src/matcher.py:85) and its own comment says "Halving it ~doubles the resolve rate (measured target ~330 RPM ...)" (matcher.py:81); docs/CHANGELOG.md:53 records "~255 req/min (1,85x l'actuel)" measured live vs ~138 before (CHANGELOG:34). Even keep-alive alone, in a serial loop with fixed sleep, raises the rate (60/(0.3+0.032)=181 vs 
- reread : confirmé (c=0.85, sév. minor, dir. NEUTRAL) — Re-read at HEAD and diffed against 184b2b8~1; the contradiction is real and introduced by the commits under review (not pre-existing). (1) Rate: requirements.txt:9-10 and src/aks_env.py:326-328 (both new in 184b2b8) state keep-alive comes "with NO change to the request count or rate → ban-safe", while the same commit's new src/matcher.py:79-85 says "Halving it ~doubles the resolve rate (measured target ~330 RPM)" and docs/CHANGELOG.md:34/:53 record ~138 → ~255 req/min (1.85×). docs/HANDOFF.md:71-72 (0394dfc/e7586f6) compounds it: concurrency is "le seul qui augmente le débit de requêtes AKS" — false, since pacing 0.3→0.15 already raised the serial rate. Arithmetic 60/(sleep+latency): pre 138 rpm, keep-alive alone 181, pacing alone 211, both 330 (256 at 84 ms) — so even the narrow keep-alive-only reading of "no change to rate" is wrong for a serial loop with a fixed sleep; only README.md:
- materiality : RÉFUTÉ (c=0.72, sév. nit, dir. NEUTRAL) — Technically true as a wording inconsistency, but immaterial for this project's threat model. Facts verified: requirements.txt:9-10 and src/aks_env.py:325-327 say "NO change to the request count or rate" — scoped to the keep-alive lever, where it is accurate; src/matcher.py:79-85 halves AKS_PROBE_DELAY_S and itself states the rate rises (~138 → target ~330 RPM, "watched for 429"); docs/CHANGELOG.md:36 says the goal was gains "sans multiplier le débit" then at :52 records measured ~255 req/min (1.85x, 0 x 429/5xx); docs/HANDOFF.md:71-72 calls concurrency the "SEUL levier qui augmente le débit" — literally false since pacing already raised it. Ban attribution: matcher.py:82-83 / CHANGELOG:36 / HANDOFF:72-73 say "browser load", src/invariants.py:58-61 says a bot-like plain-UA health probe run before every stage — both say "not probe rate", so they are not contradictory on the rate premise, o


### [minor/OPEN] HANDOFF §7: safe-auto sweep (scripts/10) shown without any WRITE / 'sur GO' marker although it is a real auto-approved write; --dry-run exists and is not shown
`docs/HANDOFF.md:143` — lentille(s) : handoff-cmds · origine : lead-spotcheck

**Constat.** scripts/10_data_entry_auto.py performs real writes (module docstring lines 4-6: 'auto-approve EVERY matcher candidate → submit (05, --mode safe real write). NO human validation'; argparse description 'Safe-auto data-entry sweep (real writes)' at :289) and has a --dry-run flag (:312), but HANDOFF §7 lists the command as a plain example with no WRITE/'sur GO' marker, unlike the 05/06 lines (:137, :157) which are marked. A reader following the doc could launch a real multi-merchant submit believing it is a preview.

**Scénario.** Fresh-session operator or Claude copies the §7 safe-auto line to 'look at' a sweep → real submits on Kinguin without an explicit GO.

**Preuve.** scripts/10_data_entry_auto.py:4-6, :289 ('Safe-auto data-entry sweep (real writes).'), :312 (--dry-run); docs/HANDOFF.md:143-144 carry no WRITE/GO marker while :137 and :157 do.

**Piste (non appliquée).** Mark the line '(WRITE — sur GO)' and show the --dry-run form first.

**Votes.**
- reproduce : confirmé (c=0.9, sév. minor, dir. OPEN) — Confirmed at HEAD (e7586f6). docs/HANDOFF.md:141-146 ("Safe-auto sweep" block) lists `python3 scripts/10_data_entry_auto.py --targets "Kinguin:58,Eneba:70" --run-id <id>   # défaut --max-pages 30` and the `--triage` variant (`# + plan Move-to-List des skips`) with no WRITE / "sur GO" marker, whereas the sibling blocks explicitly mark writes: :137 `# DRY-RUN (défaut)`, :138 `# WRITE (safe: lot validé complet) — sur GO`, :139 `# canary de 1 (WRITE)`, :156 `# dry-run (plan only)`, :157 `# plan confirmé complet (WRITE) — sur GO`, :150 `# APERÇU dry-run`. scripts/10_data_entry_auto.py is a real write by default: module docstring :4-6 ("auto-approve EVERY matcher candidate → submit (05, --mode safe real write). NO human validation"), argparse description :289 "Safe-auto data-entry sweep (real writes).", and `--dry-run` exists at :312-315 ("Fully READ-ONLY preview ... NO submit and NO move"); `
- reread : confirmé (c=0.92, sév. minor, dir. OPEN) — Re-read at HEAD confirms every factual element. docs/HANDOFF.md:141-145 ("Safe-auto sweep") lists `python3 scripts/10_data_entry_auto.py --targets "Kinguin:58,Eneba:70" --run-id <id>   # défaut --max-pages 30` and the `--triage` variant with NO "WRITE" / "sur GO" / "dry-run" annotation, whereas the neighbouring 05 lines (:136-138) and 06 lines (:156-157) are explicitly labelled DRY-RUN vs WRITE — sur GO. scripts/10_data_entry_auto.py is a real write by default: docstring :4-6 ("auto-approve EVERY matcher candidate → submit (05, --mode safe real write). NO human validation"), argparse description :289 ("Safe-auto data-entry sweep (real writes)."), and submit() :147-148 invokes 05 with `--mode safe --submit`. There is no interactive confirmation or invariant gate inside script 10 (grep for input(/confirm/01_check → nothing). A `--dry-run` flag exists (:312-315, "Fully READ-ONLY preview … N
- materiality : confirmé (c=0.7, sév. minor, dir. OPEN) — Finding confirmed and material in the narrow "doc misleads an operator into an unintended WRITE" sense. docs/HANDOFF.md §7 (lines 112-166, added in e7586f6) uses an explicit convention: every write form is tagged "WRITE … — sur GO" (05 at :137, :138; 06 at :157) and the read-only/dry-run default is shown first (05 :136 "DRY-RUN (défaut)", 06 :156 "dry-run (plan only)", 11 :150 "APERÇU dry-run"). The safe-auto lines (:143-144) are the only pipeline commands with no marker at all, so a reader applying the section's own pattern infers "unmarked = preview". In reality scripts/10_data_entry_auto.py invokes 05_submit with "--mode safe --submit" unconditionally (scripts/10_data_entry_auto.py:146-148), auto-approves every matcher candidate with NO human validation (docstring :4-6, argparse description :289 "real writes"), and only the --dry-run flag (:312-315, gated at :73/:133-141 — 05 never in


### [minor/CLOSED] HANDOFF §7 example 'Eneba:70' uses a store id the safe-auto allowlist rejects (Eneba = 19); same stale example in the scripts/10 docstring
`docs/HANDOFF.md:143` — lentille(s) : handoff-cmds · origine : lead-spotcheck

**Constat.** src/admin/auto_merchants.py:23 maps Eneba to store id '19'; scripts/10 validates every target against that allowlist and exits 2 on a mismatch (scripts/10:354-357 rejection_reason). The documented example batch therefore fails the gate. scripts/10's own docstring (:17) carries the same stale 'Eneba:70' example.

**Scénario.** Operator runs the §7 command verbatim → {'aborted': true, 'reason': ...} exit 2, no sweep.

**Preuve.** src/admin/auto_merchants.py:23 ("Eneba", "19"); scripts/10_data_entry_auto.py:354-357; scripts/10:17; docs/HANDOFF.md:143.

**Piste (non appliquée).** Use Eneba:19 in both places.

**Votes.**
- reproduce : confirmé (c=0.97, sév. minor, dir. CLOSED) — Reproduced verbatim at HEAD. `python3 scripts/10_data_entry_auto.py --targets "Kinguin:58,Eneba:70" --run-id f15-verify-should-not-exist` printed `{"aborted": true, "reason": "Eneba attend le store 19, pas « 70 »"}` and exited 2; no `runs/<run-id>/` directory was created, so no sweep started. `rejection_reason('Eneba','70')` returns the same message while `('Eneba','19')` and `('Kinguin','58')` return None (src/admin/auto_merchants.py:23 `("Eneba", "19")`, gate at scripts/10_data_entry_auto.py:354-357). The stale example is at docs/HANDOFF.md:143 (added in e7586f6) and scripts/10_data_entry_auto.py:17 (docstring). docs/EXECUTOR_RULES.md:1163 and src/admin/static/app.js:26 both confirm Eneba = 19. Git history: the `Eneba:70` docstring example dates from 3a5110d (before the CLI allowlist gate was added in eafe4d0), and HANDOFF copied it. Severity stays minor: a documentation example that f
- reread : confirmé (c=0.97, sév. minor, dir. CLOSED) — Confirmed at HEAD by direct read and execution. src/admin/auto_merchants.py:23 has ("Eneba", "19"). scripts/10_data_entry_auto.py:354-357 calls rejection_reason(m, s) for every --targets entry and returns 2 with {"aborted": true, "reason": ...} on any miss. Running the gate locally: rejection_reason('Eneba','70') -> 'Eneba attend le store 19, pas « 70 »'; rejection_reason('Eneba','19') -> None. docs/HANDOFF.md:143 (added by HEAD commit e7586f6, combined.diff line 250) documents `--targets "Kinguin:58,Eneba:70"`, so the §7 command run verbatim aborts with exit 2 before any sweep. scripts/10:17 docstring carries the same "Eneba:70" (pre-existing since 3a5110d, predates the allowlist commit 86d087b; not touched by the 5 commits under review, but the HANDOFF line is newly introduced by e7586f6 and copies it). No guard elsewhere rescues the example: the abort happens in the CLI before _RUNNER
- materiality : confirmé (c=0.85, sév. minor, dir. CLOSED) — Confirmed, not refuted, but strictly CLOSED. src/admin/auto_merchants.py:23 maps ("Eneba", "19"); rejection_reason (auto_merchants.py:47-61) enforces the canonical store id and returns 'Eneba attend le store 19, pas « 70 »' for the documented example (verified by running it locally: rejection_reason('Eneba','70') -> that string; rejection_reason('Eneba','19') -> None). scripts/10_data_entry_auto.py:354-357 calls rejection_reason for every target before _RUNNER.install() and exits 2 with {"aborted": true, "reason": ...} on the first miss, refusing the whole batch — so a fresh Claude session or operator copying docs/HANDOFF.md:143 verbatim gets an immediate, deterministic, self-explanatory abort and no sweep, no write, no state change. The wrong id in HANDOFF.md:143 was introduced by e7586f6 (HEAD, under review); the identical stale example in scripts/10 docstring line 17 predates the revi


### [minor/CLOSED] HANDOFF §7 by-urls command form does not parse: scripts/11 requires --run-id and takes --urls / --urls-file, not positional URLs
`docs/HANDOFF.md:150` — lentille(s) : handoff-cmds · origine : lead-spotcheck

**Constat.** scripts/11_data_entry_by_urls.py main() argparse (:474-477) requires --run-id and accepts --urls or --urls-file; the documented positional form '<fichier_urls | urls...>' is rejected by argparse. The console form is --run-id <id> --urls-file <f> (dry-run is the script's only mode).

**Scénario.** Operator runs the §7 line → argparse error 'the following arguments are required: --run-id' / unrecognized arguments.

**Preuve.** scripts/11_data_entry_by_urls.py:474-477; docs/HANDOFF.md:150.

**Piste (non appliquée).** Rewrite the line with --run-id <id> --urls-file <f>.

**Votes.**
- reproduce : confirmé (c=0.97, sév. minor, dir. CLOSED) — Reproduced concretely. docs/HANDOFF.md:150 (introduced at HEAD e7586f6, per git blame) documents `python3 scripts/11_data_entry_by_urls.py <fichier_urls | urls...>`. scripts/11_data_entry_by_urls.py:474-477 declares `--run-id` (required=True), `--urls`, `--urls-file` — no positional argument. Running the documented forms from a scratch dir: (a) `11_data_entry_by_urls.py urls.txt` -> `error: the following arguments are required: --run-id`, exit 2; (b) `11_data_entry_by_urls.py https://...` -> same error, exit 2; (c) even with `--run-id x urls.txt` -> `error: unrecognized arguments: urls.txt`, exit 2. The console's real invocation (src/admin/submit_manager.py:769-770) is `--run-id <id> --urls-file <f> --dry-run`, matching the finding's suggested fix. The script's own `--dry-run` is `default=True` and described as "the only mode in stage 1", so dry-run is indeed the only mode. Impact is a d
- reread : confirmé (c=0.97, sév. minor, dir. CLOSED) — Confirmed at HEAD. docs/HANDOFF.md:150 (added by e7586f6, the HEAD doc commit under review) documents `python3 scripts/11_data_entry_by_urls.py <fichier_urls | urls...>`. scripts/11_data_entry_by_urls.py:474-477 defines argparse with `--run-id` (required=True), `--urls`, `--urls-file`; there is no positional argument, and _parse_urls (:296-307) only reads args.urls_file / args.urls. Experiment: `python3 scripts/11_data_entry_by_urls.py urls.txt` -> "error: the following arguments are required: --run-id"; `python3 scripts/11_data_entry_by_urls.py --run-id x urls.txt` -> "error: unrecognized arguments: urls.txt". The real invocation used by the console (src/admin/submit_manager.py:769-770) is `--run-id <id> --urls-file <f> --dry-run`, matching the finding's suggested fix. Not pre-existing: the line was introduced by e7586f6. Impact is a doc-only error that fails loudly at argparse (exit 2,
- materiality : confirmé (c=0.7, sév. nit, dir. CLOSED) — Technically confirmed and reachable: docs/HANDOFF.md:150 documents `python3 scripts/11_data_entry_by_urls.py <fichier_urls | urls...>`, but scripts/11_data_entry_by_urls.py:475-477 declares `--run-id` (required=True), `--urls`, `--urls-file` and no positional. Running the documented form with a real file path yields `error: the following arguments are required: --run-id`, exit=2 (experiment in scratchpad/verify/f16-materiality). The failure occurs inside parse_args, before run_dir creation (:487), before the invariants gate and before any SubmitSession/browser touch, so it is strictly fail-CLOSED: no write, no run artifact, no green-looking state. The argparse usage banner printed on failure shows the correct flags, so the operator self-corrects in seconds. It is not stylistic (it is a factually wrong command in the operator/fresh-session entry point, and the e7586f6 commit message claim


### [minor/CLOSED] HANDOFF §7 pipeline omits the validation step that produces approved.json (04_validate.py check / run_executor.sh check); 05 refuses an unverified approved.json
`docs/HANDOFF.md:132` — lentille(s) : handoff-cmds · origine : lead-spotcheck

**Constat.** The §7 per-merchant pipeline goes 02 → 03 → 05 with '(validation)' only in parentheses and no command; approved.json is produced by `scripts/04_validate.py check` / `manual_launch/run_executor.sh check` / the console, and scripts/05 refuses an approved.json not re-verified against candidates.json + validation.json (scripts/05:13-16). The §7 preamble (:113-114) also lists run_executor.sh as 'prepare/dry-run/submit', omitting its 'check' step (run_executor.sh:11-14, :19).

**Scénario.** Fresh operator follows §7, has no approved.json (or an unchecked one) → 05 refuses to load and the doc gives no command to produce it.

**Preuve.** scripts/05_submit.py:13-16; scripts/04_validate.py; manual_launch/run_executor.sh:11-14 and :19 ('check Verifies the validation file and writes RUN_DIR/approved.json'); docs/HANDOFF.md:113-114, :132-139.

**Piste (non appliquée).** Add the 04_validate.py check line between 03 and 05 and 'check' to the run_executor.sh list.

**Votes.**
- reproduce : confirmé (c=0.92, sév. minor, dir. CLOSED) — Finding confirmed against HEAD text and code. docs/HANDOFF.md:132 reads "extract → match → (validation) → submit" and the command block at :133-138 goes 02 → 03 → 05 with the 05 lines consuming runs/<id>/approved.json; `grep -n "04_validate\|run_executor.sh check\|approved.json" docs/HANDOFF.md` shows no 04_validate / check command anywhere in the file (only the abstract word "validation" at :17 and :132). HANDOFF.md:113-114 lists run_executor.sh as "(prepare/dry-run/submit)", while manual_launch/run_executor.sh:11-14 exposes a 4th subcommand `check RUN_DIR` and :19 documents it as "Verifies the validation file and writes RUN_DIR/approved.json". 03_match.py never writes approved.json (grep: only scripts/04_validate.py:60 writes it, via cmd_check). scripts/05_submit.py:329-352 reads approved.json then requires sibling candidates.json + validation.json and calls verify_approved_against_sou
- reread : confirmé (c=0.9, sév. minor, dir. CLOSED) — Re-read at HEAD. docs/HANDOFF.md:132-138 ("Pipeline par-marchand (manuel)") lists 02_extract_feed.py, 03_match.py, then jumps straight to 05_submit.py runs/<id>/approved.json; the only mention of validation is the parenthetical "(validation)" in the prose line and no command produces approved.json. grep of HANDOFF.md for "04_validate"/"approved.json" hits only the 05 lines (136-138) and the §1 overview (:17 "`04 report` → validation → `05 submit`"), so the doc never names 04_validate.py or the run_executor.sh `check` step anywhere. HANDOFF.md:113-114 describes run_executor.sh as "(prepare/dry-run/submit)", while manual_launch/run_executor.sh:11-14 and :19 document a 4-command flow whose step 3 `check` runs `scripts/04_validate.py check` and copies validation.template.json -> validation.json (run_executor.sh:151-163). scripts/04_validate.py:10-12 confirms `check` is what writes approved.j
- materiality : RÉFUTÉ (c=0.85, sév. nit, dir. CLOSED) — Technically true but immaterial. docs/HANDOFF.md:132 lists "extract → match → (validation) → submit" and the code block (:134-138) gives no 04_validate.py check command; :113-114 lists run_executor.sh as prepare/dry-run/submit without 'check'. But every consequence is fail-CLOSED and self-correcting: (1) scripts/05_submit.py:339-353 refuses a missing/unverified approved.json with exit 2 and a printed reason ("candidates.json and validation.json must sit next to approved.json" / "submit-time validation re-check failed"), in dry-run as well as --submit — nothing is written and no bad state looks green; (2) the HANDOFF preamble itself says "Les --help de chaque script font foi" (:113) and manual_launch/run_executor.sh:11-19 usage documents 'check ... writes RUN_DIR/approved.json'; (3) src/validation.py:64-66 embeds the exact recovery command in the validation template ("then run: 04_validat


### [minor/OPEN] [security] Session.trust_env left on: a ~/.netrc / $NETRC `default` (or host) entry injects `Authorization: Basic …` into every probe — staff-UA probes to AKS and plain probes to difmark/instant-gaming/CDP proxy — where urllib sends none (verified locally)
`src/aks_env.py:333` — lentille(s) : security · origine : finder:security

**Constat.** `_SESSION = _requests.Session()` keeps `trust_env=True`; `Session.prepare_request` does `if self.trust_env and not auth and not self.auth: auth = get_netrc_auth(request.url)` (sessions.py:480-481) and `rebuild_auth` repeats it per redirect hop (sessions.py:298). `get_netrc_auth` reads `$NETRC` or `~/.netrc`/`~/_netrc` and calls stdlib `netrc.authenticators(host)`, which returns the `default` entry for ANY host not listed. urllib's `urlopen`/`build_opener` never consult netrc. Result: credentials stored for some unrelated tool on the VPS (root's home) are silently sent as HTTP Basic auth to allkeyshop.com with the staff UA and to third-party merchants (difmark.com, instant-gaming.com) and the CDP proxy. This is new with the keep-alive backend only.

**Scénario.** Root on the VPS has `~/.netrc` with a `default login … password …` line (left by curl/git/pip usage). Keep-alive: matcher resolve probes, the invariant gate, 03_match's gate probe, scripts/11 resolves, and difmark/instant-gaming verification GETs all carry `Authorization: Basic <creds>`; if the creds happen to be a WordPress application password the AKS probes become authenticated requests (different page/ban profile), and the creds leak to third-party merchants. urllib: no Authorization header anywhere.

**Preuve.** Experiment C1-C3 with `NETRC` pointing at a 0600 scratch file containing `default login leakuser password leakpass`: keep-alive request logs show `"Authorization": "Basic bGVha3VzZXI6bGVha3Bhc3M="` on the STAFF follow path, the PLAIN (else-branch) path and the no-redirect gate-style probe; urllib logs show no Authorization on any of the three. stdlib `netrc.authenticators`: `if host in self.hosts: return self.hosts[host] elif 'default' in self.hosts: return self.hosts['default']`. requests utils.py:207-211 `netrc_file = os.environ.get("NETRC") … netrc_locations = (f"~/{f}" for f in NETRC_FILES)`. No netrc exists on this sandbox (`ls ~/.netrc` → No such file); VPS root home not checked. Note `trust_env` also controls proxy env vars — parity there is fine because requests' `get_environ_proxies` uses stdlib `getproxies()` (utils.py:829), the same source as urllib's ProxyHandler.

**Piste (non appliquée).** Set `_SESSION.trust_env = False` and re-apply only what urllib honours: `_SESSION.proxies.update(urllib.request.getproxies())` (keeps `https_proxy`/`no_proxy` parity, drops netrc and REQUESTS_CA_BUNDLE/CURL_CA_BUNDLE). Add a test that patches `NETRC` to a default-entry file and asserts the mocked session call carries no `auth`/Authorization.

**Votes.**
- reproduce : confirmé (c=0.93, sév. minor, dir. OPEN) — Reproduced with a scratch script (/tmp/claude-0/-root-aks-code-executor/fb12d4f4-2a87-4577-a2d8-a5d07afbcaa7/scratchpad/verify/f22-reproduce/repro.py) that starts a 127.0.0.1 HTTP server recording the Authorization header and calls the REAL src/aks_env.py functions at HEAD (http_get plain follow, http_get follow_redirects=False gate-style, http_head_status across a same-host 302, and the staff-UA host_locked branch via the _http_open seam). src/aks_env.py:333 builds `_SESSION = _requests.Session()` and never touches `trust_env` (grep of src/, scripts/, tests/, docs/ finds no `netrc`/`trust_env` at all); the process printed `trust_env=True`. Case A (`NETRC=<0600 file with 'default login leakuser password leakpass'>`): every keep-alive request — GET /plain, no-redirect GET /plain, HEAD /r AND the redirected HEAD /final, and the staff-UA GET /r3 — arrived with `Authorization: Basic bGVha3Vz
- reread : confirmé (c=0.9, sév. minor, dir. OPEN) — Re-read at HEAD: src/aks_env.py:333 `_SESSION = _requests.Session()` with only cookie policy (l.336) and max_redirects (l.342) adjusted — `trust_env` stays at its default True (confirmed at runtime: `trust_env True`, requests 2.32.3). All three branches of `_http_open_keepalive` (l.388 no-redirect, l.393 host_locked loop, l.411 plain follow) go through `_SESSION.request`, whose `prepare_request` calls `get_netrc_auth(request.url)` when `trust_env and not auth and not self.auth`. No guard anywhere in the repo: `grep -rn "trust_env|netrc|.auth" src scripts tests` returns nothing. Pre-commit code (`git show 184b2b8~1:src/aks_env.py`) used only `urlopen`/`build_opener`, and stdlib urllib.request has no netrc handling (`'netrc' in inspect.getsource(urllib.request)` → False), so this divergence is introduced by 184b2b8 and not present in the urllib fallback. Reproduced against a 127.0.0.1 serv
- materiality : RÉFUTÉ (c=0.72, sév. nit, dir. NEUTRAL) — Assumed true and reproduced locally (default-entry netrc -> 'Authorization: Basic dTpw' on a requests.Session to 127.0.0.1; trust_env=False -> none). But it is immaterial to this project's fail-open criteria: (1) Precondition is a `default` (or explicitly matching `machine`) entry in root's netrc on the VPS — a plain `machine github.com` entry injects nothing (verified: github-only-entry -> None); no netrc exists here, none referenced in src/scripts/docs/tests, and the finder admits the VPS home was not checked. (2) Even when present, the header cannot change a gate/resolve/match outcome: every AKS probe targets front-end blog pages, and WordPress only honours Basic/application-password auth for REST or XML-RPC requests (application_password_is_api_request in wp_validate_application_password), so the status/body seen by src/aks_env.py:_http_open_keepalive is unchanged — no spurious green


### [minor/CLOSED] [fail-closed] Pooled keep-alive socket closed by the server concurrently with the next probe → RemoteDisconnected → ok=False/status=None (adapter Retry(0, read=False) never retries) → matcher false-skips the offer; urllib's per-call connection is immune (verified locally)
`src/aks_env.py:393` — lentille(s) : security · origine : finder:security

**Constat.** Every `_SESSION.request(...)` reuses a pooled connection. urllib3 detects a socket the server already FIN'd (`connectionpool.py:288 if conn and is_connection_dropped(conn): conn.close()` — verified transparent after a server restart, G2), but when the server's idle-timeout close races the client's send (the classic keep-alive hazard: Apache KeepAliveTimeout 5s, nginx 65-75s, CDN idle timers), the request is written to a dying socket and `http.client.RemoteDisconnected` comes back. requests' default adapter is `Retry(total=0, connect=None, read=False, …)` (adapters.py:210 `self.max_retries = Retry(0, read=False)`), and urllib3 `Retry.increment` with `read is False` re-raises read errors (retry.py:471-474), so there is no transparent re-send: `_http_open_keepalive` maps it to `URLError` (line 419) → `http_get` returns ok=False status=None. urllib opens a fresh `Connection: close` socket per call, so the same server behaviour never affects it.

**Scénario.** Matcher sweep: a resolve probe follows a >idle-timeout pause (difmark/IG verification GETs + politeness sleeps, page boundary, on_progress logging) and the server closes the idle connection just as the probe is sent. `resolve_aks` gets `probe.status None` → `if probe.status not in (404, 410): raise AksProbeUnreliable` (matcher.py:1418-1419) → `match_offer` returns `SkippedOffer(offer, 'AKS probe unreliable (throttled?)…')` (matcher.py:1948-1949): a genuinely addable pending offer is skipped for this run, attributed to throttling. scripts/11 is shielded by its `RESOLVE_ATTEMPTS` retry (status None is 'transient'); the invariant gate runs in a fresh subprocess (first request, no pooled socket) so it is unaffected.

**Preuve.** Experiment G (server that closes the connection without a response on the 2nd request of the SAME connection): keep-alive `http_get('/two')` → `ok:false status:null error:"<urlopen error ('Connection aborted.', RemoteDisconnected('Remote end closed connection without response'))>"`; urllib → `ok:true status:200 body:'first'` (its `/two` was request n=1 on a new connection). Experiment G2 (server restarted between two calls, FIN'd pooled socket): keep-alive ok:true 200 on the second call (is_connection_dropped path works). `python3 -c 'requests.adapters.HTTPAdapter().max_retries'` → `Retry(total=0, connect=None, read=False, redirect=None, status=None)`.

**Piste (non appliquée).** Either (a) mount an adapter that retries exactly once on a connection-reset/RemoteDisconnected of an idempotent GET/HEAD on a REUSED connection (urllib3 `Retry(total=1, connect=0, read=1, status=0, allowed_methods={'GET','HEAD'})` is the blunt version — note it also retries a real read-timeout once, i.e. one extra request), or (b) keep behaviour and change the skip reason from 'throttled?' to 'transport error (keep-alive reset?)' so the operator re-runs rather than assuming AKS throttling. Document in EXECUTOR_RULES/README that keep-alive adds this false-skip mode.

**Votes.**
- reproduce : confirmé (c=0.92, sév. minor, dir. CLOSED) — Reproduced with the real code at HEAD (e7586f6), no tracked files modified, no external network. (1) Faithful idle-timeout race — local server closes an idle keep-alive connection after T=50ms; real `src.aks_env.http_get` (keep-alive backend, `_SESSION` not None) sleeps T±3ms between two probes: 58/600 second probes returned ok=False status=None (43× ConnectionResetError, 15× RemoteDisconnected), one FIRST probe also reset at iteration 153; server counters confirm every close was an idle close (idle_closes=440, mid_closes=0). (2) Deterministic close-on-2nd-request: keep-alive r2 → ok=False status=None error "<urlopen error ('Connection aborted.', RemoteDisconnected(...))>", r3 → 200 (pool recovers); the urllib backend on the same server → u2 ok=True 200, i.e. immune as claimed. No retry occurs: `requests.adapters.HTTPAdapter().max_retries` = Retry(total=0, connect=None, read=False,...) a
- reread : confirmé (c=0.85, sév. minor, dir. CLOSED) — Re-read at HEAD and reproduced locally. (1) src/aks_env.py:326-341 builds one module-level `_SESSION` with the default `requests` adapter; `python3 -c` on this box confirms `HTTPAdapter().max_retries == Retry(total=0, connect=None, read=False, ...)`, and urllib3 2.3.0 retry.py:471-474 re-raises any read error when `read is False`, so a request written to a socket the server is closing is never re-sent. (2) `_http_open_keepalive` (aks_env.py:393-395 / 421-425) maps that `ProtocolError('Connection aborted.', RemoteDisconnected(...))` to `URLError`, and `http_get` (aks_env.py:492-493) returns `ok=False status=None`. (3) matcher.py:1417-1419: `if probe.status not in (404, 410): raise AksProbeUnreliable(...)` and matcher.py:1948-1949 turn that into `SkippedOffer(..., 'AKS probe unreliable (throttled?)')`; grep shows no retry in src/matcher.py or scripts/03_match.py (only scripts/11 has `RESOL
- materiality : RÉFUTÉ (c=0.85, sév. nit, dir. CLOSED) — Accepting the mechanism as true (urllib3 Retry(0, read=False) does not re-send after a RemoteDisconnected on a reused socket), it is immaterial for this project. (1) Fail direction is strictly CLOSED: src/aks_env.py:414-419 maps the exception to URLError, http_get returns ok=False/status=None, src/matcher.py:1418-1419 raises AksProbeUnreliable immediately, and src/matcher.py:1948-1949 turns it into SkippedOffer. Nothing is submitted, no state looks green, no write is unlocked; the offer lands in skipped.json and, because a pending AKS offer stays in the pending feed until added, it is simply re-processed on the next matcher sweep. (2) It is not a new failure class: the same status=None -> AksProbeUnreliable -> skip path already existed for urllib timeouts/connection errors; keep-alive only adds one more transient trigger. The skip reason 'AKS probe unreliable (throttled?)' carries the ac


### [minor/NEUTRAL] test_trademark_symbol_does_not_glue_into_a_token: the Halo®Deluxe assertion is vacuous (passes on pre-fix code), build_slug_candidates is claimed covered but has no assertion, cleaned_title is pinned only by a weak assertNotIn, and 'precheck' coverage is not a thing
`tests/test_matcher.py:133` — lentille(s) : matcher · origine : finder:matcher

**Constat.** Of the 7 assertions, 6 fail on the pre-commit code and genuinely pin the fix; `tokenize("Halo®Deluxe") == ["HALO","DELUXE"]` passes on the OLD code too (® has no NFKC decomposition and `[A-Z0-9']+` already splits on it), so the comment '® / © … a glued Halo®Deluxe still splits' describes pre-existing behavior, not the change. The comment at :134 says 'cleaned_title / build_slug_candidates are covered too' but build_slug_candidates has zero assertions, and cleaned_title is only pinned by `assertNotIn("COMPANYTM", …)` (would accept 'Company', 'Company ', 'CompanyT M'). The code comment at src/matcher.py:477 lists 'precheck' among covered callers, but precheck_skip never calls normalize_apostrophes (it uses `re.sub(r"[^A-Z0-9]+", " ", offer.name.upper())`, which already turned ™ into a space) — nothing to cover, nothing tested. ℡ is named in the comment but not asserted.

**Scénario.** A future edit that reverts the ® / © entries from _NFKC_LETTER_SYMBOL_RE, or changes what cleaned_title/build_slug_candidates emit for a ™ title (e.g. 'company-tm' slug), would keep this test green; the coverage the commit message claims for slugs/precheck is not enforced.

**Preuve.** Experiment running each assertion against the OLD module: 'OLD passes? True NEW passes? True tokenize(Halo®Deluxe)'; all six others 'OLD passes? False NEW passes? True'. Old build_slug_candidates("Company™") == ['companytm'] vs new ['company'] — no assertion pins this. src/matcher.py:611 `padded = " " + re.sub(r"[^A-Z0-9]+", " ", offer.name.upper()) + " "`; precheck_skip old==new on all 58 corpus titles.

**Piste (non appliquée).** Replace the Halo®Deluxe line with an assertion that actually differs pre/post (e.g. `cleaned_title("NBA 2K24 ©") == "NBA 2K24"`, old gave 'NBA 2K24 ©'); add `build_slug_candidates("STAR WARS Zero Company™ Deluxe Edition Steam Key") == ['star-wars-zero-company-deluxe-edition', 'star-wars-zero-company']` and `cleaned_title("Company™ (PC)") == "Company"`; drop 'precheck' from the src/matcher.py:477 comment.

**Votes.**
- reproduce : confirmé (c=0.95, sév. minor, dir. NEUTRAL) — Reproduced every factual claim with the real code (scratch script loading 184b2b8~1 src/ as OLD vs HEAD as NEW, plus mutation runs of the actual test). (1) Vacuous assertion: of the 7 assertions in tests/test_matcher.py:115-136, six fail on OLD and pass on NEW, but `tokenize("Halo®Deluxe") == ["HALO","DELUXE"]` (line 133) passes on OLD too — ® NFKC-normalizes to itself and the `[A-Z0-9']+` findall already splits on it — so the comment at :131-132 describes pre-existing behavior. (2) No build_slug_candidates assertion: confirmed by reading the test (only `cleaned_title` is imported at :136 and only via `assertNotIn("COMPANYTM", …)`); OLD `build_slug_candidates("Company™")` == ['companytm'] vs NEW ['company'], OLD "STAR WARS Zero Company™ Deluxe Edition Steam Key" → ['star-wars-zero-companytm-deluxe-edition', …] vs NEW ['star-wars-zero-company-deluxe-edition', 'star-wars-zero-company'] — a
- reread : confirmé (c=0.93, sév. minor, dir. NEUTRAL) — Re-read tests/test_matcher.py:115-138 and src/matcher.py:471-510 (HEAD) vs `git show 184b2b8~1:src/matcher.py`, and ran every assertion of the test against both module trees (old tree extracted with `git archive 184b2b8~1 src`). Results: tokenize(Company™) OLD=False NEW=True; tokenize(STAR WARS…) OLD=False NEW=True; missing_aks_words OLD=False NEW=True; tokenize(Widget℠ Pro) OLD=False NEW=True; tokenize(Halo®Deluxe) OLD=True NEW=True; cleaned_title assertNotIn OLD=False NEW=True; tokenize(Game № 5) OLD=False NEW=True. So the Halo®Deluxe assertion is indeed vacuous w.r.t. the change (NFKC of ® and © is identity — confirmed `unicodedata.normalize("NFKC","®") == "®"` — and the old `[A-Z0-9']+` split already separated HALO/DELUXE); the test comment "a glued Halo®Deluxe still splits" documents pre-existing behaviour. The test body (lines 115-138) contains no call to build_slug_candidates desp
- materiality : RÉFUTÉ (c=0.85, sév. nit, dir. NEUTRAL) — Technically accurate but immaterial for this project. Confirmed: tests/test_matcher.py:133 `tokenize("Halo®Deluxe")` passes on the pre-fix NFKC+`[A-Z0-9']+` path (re-derived locally: old tokenizer yields ['HALO','DELUXE']); src/matcher.py:611 precheck_skip scrubs with `re.sub(r"[^A-Z0-9]+", " ", ...)` and never calls normalize_apostrophes, so the 'precheck' word in the comment at src/matcher.py:477 is inaccurate; build_slug_candidates has no assertion (new output ['star-wars-zero-company-deluxe-edition','star-wars-zero-company'], cleaned_title('Company™ (PC)') == 'Company'). But: (1) six of seven assertions in the same test DO fail on old code and pin the ™/℠/№ strip in normalize_apostrophes, which is the single shared code path cleaned_title and build_slug_candidates call (src/matcher.py:502, 511, 1107, 1122) — a regression of the strip is caught; only a hypothetical targeted revert of 


### [minor/OPEN] Real `_http_open` dispatcher and the stdlib fallback `_http_open_urllib` execute 0 times in the suite (with or without requests)
`src/aks_env.py:449` — lentille(s) : tests · origine : finder:tests

**Constat.** Every HTTP test patches `src.aks_env._http_open` (the seam) or calls `_http_open_keepalive` directly with `_SESSION` mocked, so neither the new backend-selection line nor the production stdlib path is ever run; a defect in either stays invisible while the suite is green.

**Scénario.** Mutation the suite cannot catch: swap two positionals in the dispatcher, `backend(request, timeout, host_locked, follow_redirects)` — the invariant gate / 03_match probe `http_get(AKS_DIRECT_URL, follow_redirects=False, user_agent=AKS_STAFF_UA)` (scripts/03_match.py:72, src/invariants.py:66) then runs with follow_redirects=True and host_locked=False: a same-domain 3xx on /blog/ is followed to 200 -> ok=True (the exact CRITICAL fail-open 54f1f88 fixed) and the staff UA loses the P2-15 host-lock; all 1322 tests still pass. Likewise any breakage inside `_http_open_urllib` (the path a VPS without `requests` on /usr/bin/python3 actually runs) is never exercised — the same was true of the old single-function body, but the refactor added a dispatcher plus a second positional contract.

**Preuve.** Runner wrapping the real functions with counters while running tests.test_aks_env + test_invariants + test_matcher (352 tests): requests installed -> `seam execution counts: {'_http_open(real)': 0, '_http_open_urllib': 0, '_http_open_keepalive': 9}`; requests blocked (sys.modules['requests']=None) -> `{'_http_open(real)': 0, '_http_open_urllib': 0, '_http_open_keepalive': 7}`. grep tests/ for `_http_open_urllib|urlopen|build_opener|_NoRedirectHandler`: no hits in test_aks_env.py (only a comment at :382); `_StaffUaHostGuardRedirectHandler` is unit-tested in isolation (tests:503-522), never through the opener. Code: `backend = _http_open_keepalive if _SESSION is not None else _http_open_urllib` (src/aks_env.py:449).

**Piste (non appliquée).** One test per backend that calls the REAL `_http_open` (not a mock) against a throwaway `http.server` on 127.0.0.1 (pattern of my parity_matrix.py: bind port 0, monkeypatch `_allkeyshop_host`), once with `_SESSION` patched to None and once with the real Session, asserting the three modes: no-follow -> HTTPError(3xx); host-locked off-domain -> StaffUaRedirectRefused; follow -> 200. Cheap (<50 ms) and stdlib-only.

**Votes.**
- reproduce : confirmé (c=0.95, sév. minor, dir. OPEN) — Reproduced every element of the scenario with the real code at HEAD, without editing tracked files. (1) Coverage counts: a runner wrapping src.aks_env._http_open/_http_open_urllib/_http_open_keepalive at module level and running tests.test_aks_env + test_invariants + test_matcher (352 tests) gives {'_http_open_keepalive': 9} with requests installed and {'_http_open_keepalive': 7} with sys.modules['requests']=None; real _http_open and _http_open_urllib execute 0 times in both. Extending to all five modules that import aks_env (adding test_cdp_client, test_data_entry_by_urls; 396 tests) does not change the zeros. All 9/7 keep-alive executions come from KeepAliveBackendTests._open (tests/test_aks_env.py:320-333) calling _http_open_keepalive directly with _SESSION mocked; every http_get/http_head_status test patches src.aks_env._http_open (tests/test_aks_env.py:208-297, 535). (2) Mutation: r
- reread : confirmé (c=0.9, sév. minor, dir. OPEN) — Confirmed by re-reading HEAD and by experiment. src/aks_env.py:439-450 is the dispatcher added in 184b2b8 (`backend = _http_open_keepalive if _SESSION is not None else _http_open_urllib; return backend(request, timeout, follow_redirects, host_locked)`); _http_open_urllib (428-436) is the stdlib fallback. All 13 HTTP tests in tests/test_aks_env.py patch `src.aks_env._http_open` (lines 208-297, 535) and KeepAliveBackendTests call `_http_open_keepalive` directly with `_SESSION` mocked (325-334); no test in tests/ references _http_open_urllib/urlopen/build_opener/HTTPServer. Instrumented run of tests.test_aks_env + tests.test_invariants (60 tests, OK): counts {'_http_open_keepalive': 9}, real _http_open 0, _http_open_urllib 0. Mutation on a scratch copy (swap positionals to `backend(request, timeout, host_locked, follow_redirects)`) passes tests.test_aks_env, test_invariants, test_matcher, t
- materiality : confirmé (c=0.8, sév. minor, dir. OPEN) — Confirmed experimentally, not just by argument. In a scratch copy (/tmp/.../verify/f32-materiality/repo, tracked tree untouched) I swapped the dispatcher positionals at src/aks_env.py:450 to `backend(request, timeout, host_locked, follow_redirects)` and ran `python3 -m unittest tests.test_aks_env tests.test_invariants`: "Ran 60 tests ... OK". Under that mutation the invariant gate's `http_get(AKS_DIRECT_URL, follow_redirects=False, user_agent=AKS_STAFF_UA)` (src/invariants.py:66) would call the keep-alive backend with follow_redirects=True/host_locked=False, i.e. exactly the same-domain-3xx->200 spurious green that 54f1f88 fixed, plus loss of the P2-15 staff-UA host lock — and the gate is the only thing that unlocks write stages on the VPS. Every test in tests/test_aks_env.py patches `src.aks_env._http_open` (lines 208-297, 535) or calls `_http_open_keepalive` with `_SESSION` mocked (lin


### [minor/OPEN] CONTRIBUTING.md tells contributors to patch `src.aks_env.urlopen` — that patch is now bypassed and the test hits the real network when requests is installed
`docs/CONTRIBUTING.md:65` — lentille(s) : tests · origine : finder:tests

**Constat.** The documented IO-boundary pattern no longer intercepts anything on a box with `requests`; a test written from the doc against an allkeyshop.com URL with `AKS_STAFF_UA` sends the staff UA off the dev box / CI runner.

**Scénario.** Contributor follows docs/CONTRIBUTING.md:65 (`unittest.mock.patch("src.aks_env.urlopen", ...)` "to test http_get / http_head_status branches") and writes `http_get('https://www.allkeyshop.com/blog/x', user_agent=AKS_STAFF_UA)` expecting the mock to answer -> the keep-alive backend performs a real GET to allkeyshop.com with `User-agent: AKS/Staff` from whatever machine runs the suite.

**Preuve.** Local experiment: `with mock.patch('src.aks_env.urlopen', fake), mock.patch('src.aks_env.build_opener', fake): p = env.http_get('http://127.0.0.1:PORT/x')` -> output `probe.body = REAL-SERVER | urlopen/build_opener mock called: False | server hits: 1`. No existing test uses the pattern: the only urlopen patch in tests/ is tests/test_cdp_session.py:193 `mock.patch("src.cdp_session.urlopen", ...)`, and src/cdp_session.py:182/200 call urlopen directly (not via the seam), so nothing in the current suite reaches the network — this is a doc/process hazard, not a present leak.

**Piste (non appliquée).** Change the CONTRIBUTING example to `mock.patch("src.aks_env._http_open", ...)` (what the suite actually does) and state that `urlopen`/`build_opener` are NOT interception points any more.

**Votes.**
- reproduce : confirmé (c=0.88, sév. minor, dir. OPEN) — Confirmed by local experiment against a 127.0.0.1 HTTPServer (no external network). At HEAD, with `requests` 2.32.3 installed (`env._SESSION is not None` -> True), `with mock.patch('src.aks_env.urlopen', fake), mock.patch('src.aks_env.build_opener', fake): env.http_get(url)` returned `ok=True status=200 body='REAL-SERVER'`, `mock called: False`, and the server logged a real GET. Same for `http_head_status` (real HEAD hit) and for the staff-UA/host_locked path (with `_allkeyshop_host` patched to allow 127.0.0.1 so no request left the box): the server received a real GET with `User-Agent: AKS/Staff` while the documented mocks were never invoked. Control: `mock.patch('src.aks_env._http_open', ...)` did intercept (server hits: []). Running the identical script against `184b2b8~1:src/aks_env.py` (whose `_http_open` ended in `return urlopen(request, timeout=timeout)`, old line 332) raised `Ass
- reread : confirmé (c=0.72, sév. minor, dir. OPEN) — Re-read at HEAD: docs/CONTRIBUTING.md:65 still says `unittest.mock.patch("src.aks_env.urlopen", …)` "to test http_get / http_head_status branches". src/aks_env.py:439-450 `_http_open` now dispatches to `_http_open_keepalive` (lines 369-425, `_SESSION.request(...)`) whenever `requests` imported (lines 330-345); `urlopen`/`build_opener` are only reached in `_http_open_urllib` (428-436) when `_SESSION is None`. Behavior change is real and introduced by 184b2b8: `git show 184b2b8~1:src/aks_env.py` lines 320-332 called `urlopen(request, ...)` directly for the default (follow_redirects=True, non-staff) path, so the documented patch DID intercept `http_get` before. Experiment (local 127.0.0.1 server, both `src.aks_env.urlopen` and `build_opener` patched with a raising fake): HEAD → `probe.ok True body REAL-SERVER | mock called: False | server hits: [Chrome UA]`; pre-184b2b8 copy → `AssertionErr
- materiality : RÉFUTÉ (c=0.85, sév. nit, dir. NEUTRAL) — Technically the `mock.patch("src.aks_env.urlopen", …)` example at docs/CONTRIBUTING.md:65 is stale, but it is immaterial for this project and not introduced by the commits under review. (1) Pre-existing: `git log -S'src.aks_env.urlopen' -- docs/CONTRIBUTING.md` shows the line dates from e313ee7 (well before 184b2b8), and at 184b2b8^ `src/aks_env.py:329-332` already routed the no-redirect and staff-UA paths through `build_opener(...).open(...)`, with bare `urlopen` only on the plain path — so the exact scenario in the finding (allkeyshop URL + AKS_STAFF_UA) already bypassed a `urlopen`-only patch before keep-alive landed. The keep-alive commit did not create the hazard. (2) The same doc already gives the correct seam: docs/CONTRIBUTING.md:86 "Mock the IO seams in tests: `src.aks_env._http_open`, …" (added in 226de3d), and every existing test uses it (tests/test_aks_env.py:208-279 all patc


### [minor/CLOSED] [stronger evidence for known #1] CI workflow (python 3.10, no `pip install -r requirements.txt`) runs the 3 requests-hard tests -> CI red; BROWSER_RUNBOOK §3 step 11 gate also goes red on a VPS without requests
`.github/workflows/ci.yml:19` — lentille(s) : tests · origine : finder:tests

**Constat.** The suite's new hard dependency on `requests` is not just a local nuisance: the project's own CI job has no dependency-install step, and the runbook's bring-up checklist requires a green suite before resuming operations.

**Scénario.** Push to origin -> `actions/setup-python@v5` (3.10) + `python -m unittest discover -s tests -v` with no pip step; setup-python toolcache interpreters ship only pip/setuptools (not verifiable offline, but the workflow installs nothing) -> `test_transport_error_becomes_urlerror`, `test_too_many_redirects_fails_closed` (ModuleNotFoundError) and `test_session_matches_urllib_redirect_ceiling` (`'NoneType' object has no attribute 'max_redirects'`) ERROR on every push since 184b2b8. On the VPS, if pip does not land on /usr/bin/python3 (known ops finding), ops/BROWSER_RUNBOOK.md:433 "11. Tests : python3 -m unittest discover -s tests — la suite doit être verte avant de reprendre l'exploitation" blocks for a reason unrelated to production.

**Preuve.** ci.yml full text: steps are checkout, setup-python 3.10, `run: python -m unittest discover -s tests -v`, secret scan — no `pip`, no `requirements`. Reproduced the failure shape locally with requests blocked: `ran=352 failures=0 errors=3` listing exactly those three tests. Tests import requests unguarded at tests/test_aks_env.py:374 and :400; :407 reads `env._SESSION.max_redirects` on None.

**Piste (non appliquée).** Minimal: decorate the two real-requests tests with `@unittest.skipUnless(src.aks_env._SESSION is not None, 'requests not installed')`, and drop `import requests` from the two exception tests — the backend's `except Exception` maps any exception, so builtin `ConnectionError('down')` and a local `class TooManyRedirects(Exception)` exercise the same lines stdlib-only. Alternatively add a `pip install -r requirements.txt` step to ci.yml (then CI never covers the fallback box).

**Votes.**
- reproduce : confirmé (c=0.9, sév. minor, dir. CLOSED) — Reproduced at HEAD. .github/workflows/ci.yml:13-18 has only checkout, setup-python@v5 (3.10) and `python -m unittest discover -s tests -v` — no pip/requirements step (file last changed at 226de3d, before 184b2b8). requirements.txt and src/aks_env.py:329-345 declare requests OPTIONAL with a stdlib fallback (`_SESSION = None`), yet tests/test_aks_env.py:374 and :400 `import requests` unguarded and :407 dereferences `env._SESSION.max_redirects`. Running tests.test_aks_env with `requests` blocked via a meta-path finder gives `_SESSION = None`, `ran=53 failures=0 errors=3`: test_session_matches_urllib_redirect_ceiling (AttributeError 'NoneType' has no attribute 'max_redirects'), test_too_many_redirects_fails_closed and test_transport_error_becomes_urlerror (ModuleNotFoundError). With requests present the same module is OK (53 tests). The remaining 50 tests pass without requests because the he
- reread : confirmé (c=0.9, sév. minor, dir. CLOSED) — Re-read at HEAD confirms the claim on every point. (1) `.github/workflows/ci.yml` steps are exactly checkout, `actions/setup-python@v5` python-version "3.10", `python -m unittest discover -s tests -v`, secret scan — no `pip install`, no reference to `requirements.txt`; `git log -- .github/workflows/ci.yml` shows it last changed in 226de3d, i.e. it was NOT updated when 184b2b8 introduced the `requests` dependency. (2) `tests/test_aks_env.py:375` and `:402` do an unguarded `import requests`; `:406-407` reads `env._SESSION.max_redirects` where `src/aks_env.py:343-345` sets `_SESSION = None` on import failure. `KeepAliveBackendTests` (line 317) carries no `skipUnless`/`skipIf`, and the `_open` helper only mocks `_SESSION` inside the `with` — it does not protect the three tests named. (3) Not pre-existing: `git show 184b2b8~1:tests/test_aks_env.py | grep -c requests` = 0 and same for `src/aks
- materiality : confirmé (c=0.55, sév. minor, dir. CLOSED) — Technically confirmed and reachable, but strictly fail-CLOSED with no production write path. Evidence: .github/workflows/ci.yml has only checkout / setup-python 3.10 / `python -m unittest discover -s tests -v` / secret scan — no pip step, no requirements.txt reference; the workflow has not been touched since commit 226de3d, i.e. predates the requests dependency added in 184b2b8 (2026-09-08). tests/test_aks_env.py:374 and :400 do `import requests` unguarded, and :406-407 reads `env._SESSION.max_redirects` where src/aks_env.py:343-344 sets `_SESSION=None` on ImportError. Reproduced locally with a blocking `requests` shim on PYTHONPATH: `python3 -m unittest tests.test_aks_env` -> `Ran 53 tests ... FAILED (errors=3)` naming exactly test_session_matches_urllib_redirect_ceiling, test_too_many_redirects_fails_closed, test_transport_error_becomes_urlerror; without the shim the module passes (OK)


### [minor/OPEN] Host-locked redirect loop internals are untested: hop progression, relative Location, ceiling, 3xx-without-Location
`src/aks_env.py:392` — lentille(s) : tests · origine : finder:tests

**Constat.** `test_host_locked_follows_same_domain_redirect` asserts only `sess.request.call_count == 2`, never the URL of hop 2, and no test covers a relative `Location`, a 3xx with no `Location`, or the 10-iteration ceiling — the loop's correctness is entirely unverified by the suite (it is correct today, per my local matrix).

**Scénario.** (a) Drop `current = nxt` (line 405): the fake side_effect list still yields 200 on the 2nd call -> test passes; real: the same 302 URL is re-requested 10 times then `URLError('too many redirects (host-locked)')` -> every same-domain-redirected staff resolve fails closed. (b) Replace `for _ in range(10)` with `while True`: a same-domain redirect loop hangs 03_match in an UNPACED staff-UA burst (~30 req/s — `AKS_PROBE_DELAY_S` sleeps in matcher.resolve_aks, outside this loop) -> ban exposure; suite green. (c) Use `urljoin(url, location)` instead of `urljoin(current, location)`: a relative Location on hop >= 2 resolves against the original URL -> a different AKS page's body is returned as the resolve -> wrong page parsed (wrong-entry class); suite green.

**Preuve.** tests/test_aks_env.py:364-371: `resps = [_FakeReqResp(302, b"", {"Location": "https://www.allkeyshop.com/blog/y"}), _FakeReqResp(200, b"ok")] ... self.assertEqual(sess.request.call_count, 2)` — no assertion on `call_args_list[1]`. Local matrix (both backends via http_get, staff UA): `302rel staff (True, 200, '', 'ok', 2)`, `302rel2 staff (True, 200, '', 'sub-ok', 2)`, `302 lowercase-loc staff (True, 200, ...)`, `302 noloc staff (True, 302, 'HTTP Error 302: Found', '', 1)` identical to urllib — so current code is right, but only my throwaway script shows it.

**Piste (non appliquée).** Extend the follow test to assert `sess.request.call_args_list[1].args[1] == 'https://www.allkeyshop.com/blog/y'`; add a relative-Location case (`{'Location': '/blog/y'}` -> hop 2 URL absolute), a `{'Location': ''}`/missing case -> HTTPError(302), and a side_effect of 11 same-domain 302s -> URLError (asserting call_count == 10).

**Votes.**
- reproduce : confirmé (c=0.95, sév. minor, dir. OPEN) — Not refuted — every element of the finding reproduces at HEAD (e7586f6). (1) tests/test_aks_env.py:364-371 `test_host_locked_follows_same_domain_redirect` asserts only `r.status == 200` and `sess.request.call_count == 2`; no assertion on the hop-2 URL. No other test in the repo exercises the host-locked loop at src/aks_env.py:390-408 (grep for `call_args_list`/`Location`/`too many redirects` across tests/ finds none besides those lines; `test_too_many_redirects_fails_closed` injects `requests.TooManyRedirects` as a side_effect and never enters the loop). (2) Mutation experiment in scratch copies of src/+tests/ (tracked files untouched), running the FULL tests.test_aks_env module (53 tests): base OK, mutant a (drop `current = nxt`) OK, mutant b (`while True`) OK, mutant c (`urljoin(url, location)`) OK — all three mutants survive. (3) Real behaviour of each mutant via `_http_open_keepalive
- reread : confirmé (c=0.92, sév. minor, dir. OPEN) — Confirmed. The host-locked redirect loop (src/aks_env.py:392-406) is NEW in 184b2b8 (`git show 184b2b8~1:src/aks_env.py | grep -c _http_open_keepalive` = 0), and the only test that exercises it is tests/test_aks_env.py:364-371 `test_host_locked_follows_same_domain_redirect`, which asserts `r.status == 200` and `sess.request.call_count == 2` and nothing about the hop-2 URL. I copied src/+tests/ to scratch and applied the finding's three mutations: (a) `current = nxt` -> `pass`; (b) `for _ in range(10)` -> `while True` with the `else: raise URLError` removed; (c) `urljoin(current, location)` -> `urljoin(url, location)`. All three mutants pass `KeepAliveBackendTests` (10/10) AND the full `tests.test_aks_env` module (53/53). A mock matrix shows the mutants diverge in real behavior: (a) hop 2 is re-requested at the ORIGINAL url `/blog/x` instead of `/blog/y` (with a real server this loops 10x
- materiality : RÉFUTÉ (c=0.7, sév. nit, dir. CLOSED) — Technically true: the existing test (tests/test_aks_env.py:364-371) asserts only call_count==2. My mutation experiment confirms mutants (a) drop `current = nxt` and (c) `urljoin(url, ...)` both pass the existing test scenario (hop-2 URL for (a) becomes '.../blog/x' again; for (c) only hop>=2 relative Locations differ). But materiality here is low: (a) fails CLOSED — every same-domain-redirected staff resolve ends in URLError('too many redirects (host-locked)') after 10 hops -> probe.ok=False -> AksProbeUnreliable/miss, no write unlocked, no green state. (b) `while True` is a contrived regression; even then it is a hang/ban exposure on a same-domain redirect LOOP (pathological on AKS), not a write path. (c) requires a regression AND a relative Location on hop>=2 (WordPress canonical redirects emit absolute Locations; hop>=2 relative is pathological) AND the mis-joined page must still yiel


### [minor/NEUTRAL] Keep-alive backend's outbound request shape (method, headers) and `_KeepAliveResponse.__exit__` close are never asserted
`tests/test_aks_env.py:331` — lentille(s) : tests · origine : finder:tests

**Constat.** `_open` always builds a GET `Request` and no test inspects `sess.request.call_args` (method / `headers=`), so the HEAD reachability probe and the UA that actually leaves the backend are unverified; `_FakeReqResp.closed` exists but no test reads it.

**Scénario.** Hardcode `"GET"` in `_SESSION.request(...)` or drop `headers=headers` -> `http_head_status` silently becomes a body-downloading GET (the AUDIT C1 method invariant CONTRIBUTING:66-68 says to assert), or the wire UA becomes `python-requests/2.32.3` so AKS anti-bot rejects every staff probe (fail-closed, but fingerprints the box) — suite green. `test_http_head_uses_head_method` (tests:243) checks the method on the Request handed to the MOCKED seam, not what the backend sends. Dropping `self._resp.close()` in `__exit__` (src/aks_env.py:364) also passes.

**Preuve.** tests/test_aks_env.py:331-332 `req = request if isinstance(request, Request) else Request(request, method="GET", headers={"User-Agent": AKS_STAFF_UA})`; no `call_args` / `.closed` assertion anywhere in KeepAliveBackendTests (:322-407). Wire check against a local server: keepalive sends `{'User-agent': 'AKS/Staff', 'Accept-Encoding': 'gzip, deflate', 'Accept': '*/*', 'Connection': 'keep-alive'}` (single UA header — requests' default `User-Agent` is overridden case-insensitively), urllib sends `{'Accept-Encoding': 'identity', 'User-Agent': 'AKS/Staff', 'Connection': 'close'}`; `HEAD ok/302 follow/302 nofollow/404` identical across backends. So behaviour is correct today, unasserted.

**Piste (non appliquée).** In the 2xx test assert `sess.request.call_args.args[:2] == ('GET', url)` and `call_args.kwargs['headers']['User-agent'] == AKS_STAFF_UA`; add one HEAD case (`Request(url, method='HEAD')` -> `'HEAD'` forwarded); assert `fake.closed` after the `with` block.

**Votes.**
- reproduce : confirmé (c=0.93, sév. minor, dir. NEUTRAL) — Reproduced the finding's mutation scenario against a scratch copy of HEAD (scratchpad/verify/f36-reproduce/m1..m3, tracked repo untouched). Baseline: `python3 -m unittest tests.test_aks_env.KeepAliveBackendTests tests.test_aks_env.HttpProbeTests` = 19 tests OK. Mutation 1 — hardcode the method in all three `_SESSION.request(method, ...)` calls to `"GET"` (src/aks_env.py:385,389,405 at HEAD; sed confirmed 3 replacements): `python3 -m unittest tests.test_aks_env` = 53 tests OK. Mutation 2 — strip `headers=headers,` from all three calls (grep confirms 0 remaining): 53 tests OK. Mutation 3 — replace `self._resp.close()` in `_KeepAliveResponse.__exit__` (src/aks_env.py:365) with `pass`: 53 tests OK. So the UA header and HTTP method that actually leave the keep-alive backend, and the response close on context exit, are unasserted, exactly as claimed. Static confirmation: in tests/test_aks_env.
- reread : confirmé (c=0.9, sév. minor, dir. NEUTRAL) — Confirmed by independent re-read and mutation experiment. The keep-alive backend (`_http_open_keepalive`, `_KeepAliveResponse`) is NEW in 184b2b8 (`git show 184b2b8~1:src/aks_env.py` has no `_SESSION`/`_KeepAlive*`), so this is introduced by the commits under review, not pre-existing. At HEAD, src/aks_env.py:383-385 forwards `method = request.get_method()` and `headers = dict(request.header_items())` into `_SESSION.request(...)` (lines 388, 393, 411) and `__exit__` (line 364-366) calls `self._resp.close()` — behaviour is correct, verified against a local 127.0.0.1 server: `HEAD` + `User-agent: AKS/Staff` and `GET` + the Chrome/149 UA arrive on the wire exactly as built, and the underlying raw response is closed after the `with` block. But tests/test_aks_env.py:322-407 (KeepAliveBackendTests) never inspects `sess.request.call_args` (only `call_count` at :372) and `_FakeReqResp.closed` (:3
- materiality : RÉFUTÉ (c=0.8, sév. nit, dir. CLOSED) — Technically true (no call_args/closed assertions in KeepAliveBackendTests, tests/test_aks_env.py:322-407) but immaterial here. (1) The "HEAD reachability probe" premise is wrong for production: http_head_status has zero callers outside src/aks_env.py:530 and tests; the invariant gate is http_get(AKS_DIRECT_URL, follow_redirects=False, user_agent=AKS_STAFF_UA) at src/invariants.py:66-69 and scripts/03_match.py:72 is also GET, so hardcoding "GET" in the backend changes nothing observable on the VPS. CONTRIBUTING C1 (docs/CONTRIBUTING.md:69-71, "assert the HTTP method used for the AKS probe so shell/Python gates can't diverge") is already enforced at the _http_open seam (tests:243-254). (2) Wire check against a local 127.0.0.1 server with the keep-alive backend: HEAD forwarded, single 'user-agent: AKS/Staff', raw.closed==True after the with block — current behaviour is correct; the finder c


### [minor/OPEN] Real Session cookie policy (`DefaultCookiePolicy(allowed_domains=[])`) is not covered — only `max_redirects` is asserted on the real `_SESSION`
`src/aks_env.py:338` — lentille(s) : tests · origine : finder:tests

**Constat.** The statelessness guarantee the CHANGELOG/commit rely on ("Cookies désactivés ... résolution déterministe") is enforced by one constructor argument that no test checks; the redirect test uses a faked `TooManyRedirects`, and `test_session_matches_urllib_redirect_ceiling` reads only `max_redirects`.

**Scénario.** Change `allowed_domains=[]` to `allowed_domains=None` (which means allow ALL domains) or drop the `set_policy` line -> a `Set-Cookie` (e.g. an AKS geo/region cookie) from probe N is replayed on probe N+1 of the same 03_match process -> the resolve page content differs from urllib's stateless read -> region/price resolution becomes order-dependent; suite stays green.

**Preuve.** tests/test_aks_env.py:405-407 asserts only `env._SESSION.max_redirects == 10`. Current policy verified locally: after `GET /setcookie` (`Set-Cookie: sid=abc`), `GET /echo` -> `{"cookie": null}` for both backends and `session cookie jar after all: []`. (Within one followed chain the plain-UA backend still forwards hop-1 Set-Cookie to hop 2 — `hop2 cookie (plain follow) ('sid=hop1',)` vs urllib `None` — already reported by parity, not re-raised here.)

**Piste (non appliquée).** A test that sets a cookie on the real `_SESSION.cookies` via `set_cookie`/`extract_cookies` and asserts the jar stays empty, or simply asserts `_SESSION.cookies.get_policy().is_not_allowed('allkeyshop.com')` / `allowed_domains == ()`. Guard with the same skipUnless as the ceiling test.

**Votes.**
- reproduce : confirmé (c=0.92, sév. minor, dir. OPEN) — Reproduced concretely, not refuted. (1) Coverage gap confirmed: `grep -rn "set_policy|allowed_domains|DefaultCookiePolicy|.cookies" tests/ scripts/ src/` hits only src/aks_env.py:331,336 — no test anywhere references the Session cookie policy; tests/test_aks_env.py:405-407 (`test_session_matches_urllib_redirect_ceiling`) asserts only `env._SESSION.max_redirects == 10`, and `test_too_many_redirects_fails_closed` (tests/test_aks_env.py:398-403) uses a faked `TooManyRedirects` side_effect on a Mock session. (2) Scenario reproduced against a local 127.0.0.1 server through the real `_http_open_keepalive` + real `_SESSION`: HEAD policy (`allowed_domains() == ()`) -> after GET /setcookie (`Set-Cookie: sid=abc`), GET /echo shows `jar: [] echo: {"cookie": null}`; with the policy mutated to `DefaultCookiePolicy(allowed_domains=None)` or plain `DefaultCookiePolicy()` (= dropping the set_policy line
- reread : confirmé (c=0.9, sév. minor, dir. OPEN) — Claim verified at HEAD. src/aks_env.py:333-342 creates the real `_SESSION`, and the statelessness guarantee rests solely on line 336 `_SESSION.cookies.set_policy(DefaultCookiePolicy(allowed_domains=[]))`. grep over tests/, scripts/, src/ finds no reference to `get_policy`, `allowed_domains`, or `_SESSION.cookies` anywhere; the only assertion on the real Session is tests/test_aks_env.py:405-407 (`max_redirects == 10`); every other KeepAliveBackendTests case mock.patches `_SESSION` with a Mock (line 333), so the cookie jar is never exercised. This is introduced by 184b2b8 (pre-commit `git show 184b2b8~1:src/aks_env.py` has no `_SESSION`/requests at all — urllib was stateless by construction, so no such guard was needed before). The backend is the production path whenever requests is installed (line 449 dispatch; requirements.txt lists `requests>=2.28`). Experiment with a local 127.0.0.1 se
- materiality : confirmé (c=0.65, sév. minor, dir. OPEN) — Technically confirmed and material enough to keep as a minor test-coverage finding. src/aks_env.py:336 is the ONLY thing enforcing statelessness of the shared keep-alive Session (`_SESSION.cookies.set_policy(DefaultCookiePolicy(allowed_domains=[]))`), and the property is explicitly advertised in commit 184b2b8's message ("Session cookies disabled") and in the code comment ("resolution must stay deterministic"). tests/test_aks_env.py:405-407 asserts the sibling property on the real session (`max_redirects == 10`) but nothing asserts the cookie policy; the keep-alive unit tests (tests/test_aks_env.py:322-334) all mock `_SESSION` entirely, so no test ever exercises the jar. Local repro under scratchpad/verify/f37-materiality/t.py: current policy -> after GET /set (Set-Cookie sid=abc) the follow-up GET /echo carries no Cookie header and the jar is empty; changing to `allowed_domains=None` OR


### [minor/CLOSED] Halved pacing + keep-alive with NO runtime throttle detection: a 429/503 window turns every offer into a silent 'probe unreliable' skip at ~400 req/min on one staff-UA connection, 03_match exits 0 and the safe-auto sweep plows on — the commit's only safeguard ('watched on a small batch') is manual
`src/matcher.py:85` — lentille(s) : critic · origine : critic

**Constat.** AKS_PROBE_DELAY_S 0.3→0.15 and keep-alive remove ~0.28 s of the ~0.43 s per-probe cost, but nothing in the code reacts to AKS pushing back: `resolve_aks` raises `AksProbeUnreliable` on ANY non-404/410 status of a guessed slug (matcher.py:1417-1419), `match_offer` turns it into a per-offer `SkippedOffer('AKS probe unreliable (throttled?)…')` (matcher.py:1948-1949) with no counter, no sleep, no stop; scripts/03_match.py checks AKS reachability ONCE before the loop (:72) and then `return 0` (:132) whatever the skip-reason mix; the sweep's `MatchOutcome.ok = (rc == 0)` (scripts/10:105-106, src/data_entry_auto.py:71-75) so a page whose offers were ALL throttled is 'clean' with 0 candidates and run_sweep moves to the next page. During a throttle window each offer costs exactly one probe (first slug → 429 → raise), so the process keeps issuing ~1/0.15 s ≈ 400 429-answered requests per minute with `User-agent: AKS/Staff` over a single persistent TLS connection until the extraction is exhausted (a full-feed 03_match on a 300-page feed = thousands of 429s), while every affected offer is false-skipped and never retried in that sweep. The pre-change code had the same shape at half the density; the commit message/CHANGELOG present the only mitigation as a one-time observation ('Watched on a small batch for 429/throttling before adopting; raise back if AKS pushes back', matcher.py:83-84) — nothing enforces it at runtime. src/invariants.py:58-63 attributes the 2026-08-28 IP ban to a bot-like probe pattern, which is exactly what a sustained 429 loop looks like. Distinct from the listed docs-rate finding: this is code behaviour (no detection/back-off/stop), not the documentation contradiction.

**Scénario.** safe-auto sweep on Kinguin, page 30 → 03_match starts, AKS begins answering 429 after the 40th probe → the remaining ~60 offers are each probed once (429) and skipped 'AKS probe unreliable' at 0.15 s spacing (≈9 s of continuous 429s), 03 exits 0, recap shows the page clean with 0 candidates, the sweep extracts page 29 and repeats — hours of 429 hammering across 30 pages, with every legitimate offer on those pages silently skipped; no halt, no alert, nothing in recap.json distinguishes 'throttled' from 'no candidates'.

**Preuve.** src/matcher.py:1417-1419 `if not (probe.ok and probe.status == 200 and probe.body): if probe.status not in (404, 410): raise AksProbeUnreliable(...)`; src/matcher.py:1948-1949 `except AksProbeUnreliable as exc: return SkippedOffer(offer, f"AKS probe unreliable (throttled?): {exc}")`; grep for `unreliable|throttl|consecutive|429` in scripts/03_match.py, src/data_entry_auto.py, scripts/10_data_entry_auto.py finds no handling (only the submit-side `ten_consecutive_failures`); scripts/03_match.py:72 single pre-flight probe, :132 `return 0`; scripts/10_data_entry_auto.py:105-106 `return MatchOutcome(ok=(rc == 0), candidates=n, …)`; src/matcher.py:83-84 comment 'Watched on a small batch for 429/throttling before adopting; raise back if AKS pushes back'; sleep only at matcher.py:1415/1439 (fixed 0.15 s, no adaptive component).

**Piste (non appliquée).** Deterministic guard in match_feed/03_match: count consecutive `AksProbeUnreliable` results (e.g. ≥5 in a row, or any 429) → abort the match stage with a distinct exit code / `aborted: aks_throttled` so the sweep halts fail-closed instead of recording a clean page; optionally a bounded exponential back-off before aborting. Record the unreliable-skip count in match_meta.json / recap so an operator can see a throttled page.

**Votes.**
- reproduce : confirmé (c=0.85, sév. minor, dir. CLOSED) — Reproduced concretely at HEAD (e7586f6) against a 127.0.0.1 server (404 x4 then 429 forever). (A) Real match_feed->resolve_aks->http_get (keep-alive Session active, real AKS_PROBE_DELAY_S=0.15 sleep): 60 Kinguin offers -> 0 candidates, 58 skipped 'AKS probe unreliable (throttled?)' (src/matcher.py:1417-1419 raise, :1948-1949 SkippedOffer); server saw 62 requests, 58 answered 429, over ONE client connection, UA 'AKS/Staff', mean gap 0.159s and last gap 0.160s = 377 req/min with no back-off growth. (B) scripts/03_match.py main() in-process with pre-flight 200: rc=0, stdout {candidates:0, skipped:20}, all skips 'unreliable'; match_meta.json has no throttle field (scripts/03_match.py:132 `return 0`). (C) run_sweep with MatchOutcome(ok=True, candidates=0) on every page: all 30 pages processed, halted=None, page entries carry only page/run/offers/candidates/created/offers_created (src/data_ent
- reread : confirmé (c=0.6, sév. minor, dir. CLOSED) — Core claim verified at HEAD: src/matcher.py:1415-1419 sleeps a fixed AKS_PROBE_DELAY_S=0.15 then raises AksProbeUnreliable on any non-404/410 status of the FIRST guessed slug; match_offer :1948-1949 turns it into a per-offer SkippedOffer; match_feed :2304-2318 loops all offers with no counter/back-off/abort; scripts/03_match.py:132 returns 0 regardless of skip mix; scripts/10_data_entry_auto.py:105 MatchOutcome(ok=(rc==0)). Grep for 429|throttl|consecutive|unreliable in 03_match.py, src/data_entry_auto.py, scripts/10 finds no handling. Local 127.0.0.1 server answering 429: keep-alive backend active (aks_env._SESSION set), one http_get = exactly 1 request, status 429 ok=False (no requests-level retry), so one 429 per offer as claimed. Pre-commit (git show 184b2b8~1:src/matcher.py:79,1399,1929) has the identical shape at 0.3 s — the finding explicitly marks it pre-existing, and the commits
- materiality : confirmé (c=0.6, sév. minor, dir. CLOSED) — Technically true and reproduced: with a fake http_get answering 429, match_feed on a 60-offer Kinguin feed yielded candidates=0, skipped=60 (all 'AKS probe unreliable (throttled?)'), 60 probes, no stop/back-off (src/matcher.py:1417-1419, :1948-1949); scripts/03_match.py:132 returns 0 and src/data_entry_auto.py:224-232 records the page clean with only `candidates` (no skip-reason mix), so a throttled page is indistinguishable from an empty one in recap.json. This is a genuine silent-coverage-loss gap in a sweep whose own design flags silent clean ends (coverage_incomplete_max_pages), and the commit doubled probe density with only a manual 'watched on a small batch' safeguard — so it is not immaterial or stylistic, and no AGENTS.md reviewed decision covers throttle detection (HANDOFF.md:71-74 actually says higher rates should come 'avec un rate-limiter'). However, materiality is bounded: (


### [minor/CLOSED] HANDOFF §7 'Move / tri' real-write example `06_move.py … --execute --mode safe` is refused by 06_move as written (needs --i-authorize-batch AND a prior learning-canary authorization); the doc omits the canary-first step that scripts/10 itself performs
`docs/HANDOFF.md:157` — lentille(s) : critic · origine : critic

**Constat.** HANDOFF §7 documents `python3 scripts/06_move.py runs/<id> --store-id 38 --execute --mode safe   # plan confirmé complet (WRITE) — sur GO` as the full-plan move command. scripts/06_move.py refuses exactly that invocation: with `write and args.mode == "safe"` it requires `--i-authorize-batch` (:244-250, exit 2 'batch (--execute --mode safe) requiert le flag explicite --i-authorize-batch (double intention)') AND `batch_authorized(...)` to be covered by a previous `--mode learning` canary (:241-243, :251-256, RV3). The argparse help itself states 'Without it, --execute --mode safe is refused' (:147-150) and the module docstring (:17) documents the learning canary as the first real step. scripts/10's own move-execute path passes `--i-authorize-batch` (scripts/10:275-276) and runs the canary phase first (:271), so only the HANDOFF text is wrong. Not covered by the existing HANDOFF §7 findings (which flag the 08/09 omission and the by-urls/10 lines).

**Scénario.** Fresh-session operator follows HANDOFF §7 after Romain's GO: runs the documented `--execute --mode safe` line → 06_move prints `{"aborted": true, "reason": "batch (--execute --mode safe) requiert le flag explicite --i-authorize-batch …"}` and exits 2; adding the flag by hand still fails with 'l'autorisation ne couvre pas ce plan' until a `--execute --mode learning` canary has been run on that extraction/list — a step the cheat-sheet never mentions.

**Preuve.** docs/HANDOFF.md:156-157; scripts/06_move.py:137-150 (`--i-authorize-batch` help: 'Required IN ADDITION to a valid canary authorization covering the plan (RV3). Without it, --execute --mode safe is refused.'), :228-256 (`write = args.execute` … `if write and args.mode == "safe": covered, why = batch_authorized(...)`; `if not args.i_authorize_batch: … return 2`; `if not covered: … return 2`), :17 docstring example `--execute --mode learning   # canary of 1 (REAL)`; scripts/10_data_entry_auto.py:271-276 `extra = (["--batch", "--deferred", "--i-authorize-batch"] if len(rows) >= 2 else ["--i-authorize-batch"])`.

**Piste (non appliquée).** Rewrite the §7 Move block as the real sequence: dry-run → `--execute --mode learning` canary (WRITE, sur GO) → `--execute --mode safe --i-authorize-batch` (full plan, WRITE, sur GO), and mention 08_sort_plan/09_sort_move for the tri flow.

**Votes.**
- reproduce : confirmé (c=0.97, sév. minor, dir. CLOSED) — Reproduced concretely against the real script at HEAD. Built a scratch run dir (raw.json store 38 / aks-merchant-feeds-9, skipped.json with offer 1, learning.json with one CONFIRMED disposition -> target list 16) and ran the HANDOFF §7 line verbatim: `python3 scripts/06_move.py <run> --store-id 38 --execute --mode safe` -> `{"aborted": true, "reason": "batch (--execute --mode safe) requiert le flag explicite --i-authorize-batch (double intention). De plus, l'autorisation ne couvre pas ce plan.", "batch_would_be_covered": false, "authorization": "aucune autorisation — lance un canary --mode learning ..."}`, exit=2. Adding `--i-authorize-batch` by hand: `"batch (--execute --mode safe) refusé — l'autorisation ne couvre pas ce plan : aucune autorisation — lance un canary --mode learning ..."`, exit=2. Both refusals fire in scripts/06_move.py:241-256 BEFORE the invariants/CDP gate (:258+), so
- reread : confirmé (c=0.95, sév. minor, dir. CLOSED) — Confirmed at HEAD. docs/HANDOFF.md:156-157 (added by e7586f6, the HEAD commit under review; `git log -- docs/HANDOFF.md` lists only 0394dfc/e7586f6) gives `06_move.py runs/<id> --store-id 38 --execute --mode safe` as the full-plan WRITE command with no --i-authorize-batch and no prior learning canary. scripts/06_move.py:241-256: `if write and args.mode == "safe":` calls batch_authorized() then `if not args.i_authorize_batch: … return 2` ("requiert le flag explicite --i-authorize-batch (double intention)") and `if not covered: … return 2` ("l'autorisation ne couvre pas ce plan … Valide … par un canary --mode learning"); this gate runs before the invariants gate so the documented line is refused unconditionally. src/move_auth.py:125-153 batch_authorized returns (False, "aucune autorisation — lance un canary --mode learning …") without a grant, so adding the flag by hand still fails until a
- materiality : RÉFUTÉ (c=0.8, sév. nit, dir. CLOSED) — Technically true: docs/HANDOFF.md:157 gives `06_move.py … --execute --mode safe` without `--i-authorize-batch`, and scripts/06_move.py:234-256 refuses that exact invocation (exit 2) unless the flag is present AND `batch_authorized(...)` is covered by a prior `--mode learning` canary. But it is immaterial for this project's risk model: (1) Fail direction is CLOSED — the operator gets `{"aborted": true, "reason": "batch (--execute --mode safe) requiert le flag explicite --i-authorize-batch …"}` and, if the flag is then added without coverage, `… Valide les listes/données manquantes par un canary --mode learning.` (06_move.py:245-255). The refusal text itself spells out both missing steps, so the operator is corrected at the first attempt; nothing is written, no state looks green, no writes are unlocked. (2) The doc line is already labelled "(WRITE) — sur GO", so intent to write is explicit


### [nit/NEUTRAL] Within one followed chain (plain UA) requests forwards a hop-1 Set-Cookie to hop 2; urllib never sends cookies
`src/aks_env.py:336` — lentille(s) : parity · origine : prior-run-2026-09-08

**Constat.** `_SESSION.cookies.set_policy(DefaultCookiePolicy(allowed_domains=[]))` makes the Session jar reject every cookie, so CROSS-call statelessness holds (verified). But requests' resolve_redirects uses a per-request jar (`extract_cookies_to_jar(prepared_request._cookies, req, resp.raw)` … `prepared_request.prepare_cookies(prepared_request._cookies)`) that is NOT governed by the Session policy, so inside a single allow_redirects=True call a Set-Cookie on hop 1 is sent on hop 2. urllib without HTTPCookieProcessor never sends a Cookie header. This only reaches the non-host-locked follow path (cdp_client, difmark, instant_gaming plain-UA GETs); the staff host-locked loop re-sends only `headers` and showed cookie=None. The line-334 comment 'match urllib's per-call statelessness' is therefore true across calls but not within a chain.

**Scénario.** difmark.com product page 302→/x with Set-Cookie on the 302: keep-alive fetches /x with the cookie (possibly a personalized/geo variant of the page), urllib fetches /x cookie-less. Body may differ; no AKS write path involved.

**Preuve.** Matrix `[GET follow plainUA] cookie set->echo => DIFF body / keepalive: body='cookie=sid=42' / urllib: body='cookie=None'`; server-side `UA/cookies keepalive=[...]/['sid=42'] urllib=[...]/[]`. Cross-call check: the later `[GET follow STAFF(lock)] cookie set->echo` (host-locked loop, same Session) saw `cookie=None` on both backends.

**Piste (non appliquée).** Either accept and note it in the comment, or in the non-locked path pass `cookies=RequestsCookieJar()` with the same rejecting policy per request (requests merges the per-request jar from the Session jar + `cookies` kwarg).

**Votes.**
- reproduce : confirmé (c=0.95, sév. nit, dir. NEUTRAL) — Reproduced with the real code at HEAD (src/aks_env.py:336 _SESSION policy; _http_open_keepalive non-locked branch at :401-402). Local 127.0.0.1 server: GET /start -> 302 Location:/x + Set-Cookie sid=42; /x echoes the Cookie header. Calling _http_open_keepalive(follow_redirects=True, host_locked=False) the server saw [('/start', None), ('/x', 'sid=42')] and the body was 'cookie=sid=42'; _http_open_urllib on the same URL saw [('/start', None), ('/x', None)] and returned 'cookie=None'. The Session jar was [] after the call and a second keep-alive call again started cookie-less, so cross-call statelessness (the line-334 comment) holds; only within-chain parity is broken. Root cause confirmed in requests 2.32.3 source: Session.prepare_request merges into a fresh RequestsCookieJar() with the default policy, and resolve_redirects does extract_cookies_to_jar(prepared_request._cookies, ...) + pre
- reread : confirmé (c=0.92, sév. nit, dir. NEUTRAL) — Re-read src/aks_env.py at HEAD (lines 323-400) and reproduced on a local 127.0.0.1 server (/a → 302 + Set-Cookie sid=42 → /b, /b echoes the Cookie header). Calling _http_open_keepalive(req, 5, follow_redirects=True, host_locked=False) twice returned body b'cookie=sid=42' both times; _http_open_urllib on the same chain returned b'cookie=None'; a standalone keep-alive GET of /b afterwards returned b'cookie=None' and _SESSION.cookies was empty. So the claim is exactly right: the Session policy at aks_env.py:336 enforces cross-call statelessness only; requests.sessions.resolve_redirects builds a per-request jar (extract_cookies_to_jar(prepared_request._cookies, ...) then prepare_cookies) that ignores the Session jar's policy, so a hop-1 Set-Cookie is replayed on hop 2 within one allow_redirects=True call. Not pre-existing: `git show 184b2b8~1:src/aks_env.py` has no requests backend at all (u
- materiality : RÉFUTÉ (c=0.9, sév. nit, dir. NEUTRAL) — Technically true (reproduced locally: keepalive follow-chain body 'cookie=sid=42' vs urllib 'cookie=None'; a SECOND keepalive call to /x sees 'cookie=None', so cross-call statelessness holds). Immaterial here: (1) Only the non-host-locked follow path is affected (src/aks_env.py:405-406), reached by cdp_client (127.0.0.1 /json/version, no redirects/cookies), difmark.py:170/180 and instant_gaming.py:137. The invariant gate uses follow_redirects=False (aks_env.py:380-388) and every AKS staff-UA probe uses the manual host-locked loop (aks_env.py:390-403) that re-sends only `headers` — verified cookie=None — so no write-unlock or AKS-page resolution is touched. (2) The fields the affected resolvers read are offer-intrinsic (Difmark top-offer JSON `offer_attributes` region/marketplace/offer_name; IG og:title '(<Platform>) - <Region>' suffix and unanimous data-platform), and any locale/geo vari


### [nit/NEUTRAL] Wire-shape differs under keep-alive (Accept-Encoding gzip, Accept, Connection, header-name case, trust_env/certifi) — 'identical behavior' claim overstated
`README.md:180` — lentille(s) : parity · origine : prior-run-2026-09-08

**Constat.** README:180 and requirements.txt:11 say the fallback has 'identical behavior'. Request COUNT is unchanged (verified n_req equal in every non-ceiling scenario), but the requests backend adds `Accept-Encoding: gzip, deflate`, `Accept: */*`, `Connection: keep-alive` and sends the header name as `User-agent` (urllib: `Accept-Encoding: identity`, `Connection: close`, `User-Agent`). requests also honors ~/.netrc auth and REQUESTS_CA_BUNDLE/CURL_CA_BUNDLE via trust_env and verifies TLS against certifi rather than the system store; urllib does neither. Content is transparently decoded so callers see the same bytes.

**Scénario.** An AKS/OVH WAF rule keyed on request fingerprint (compression/Accept headers) rather than the UA value could in principle classify the two backends differently; a stray ~/.netrc entry for allkeyshop.com on the VPS would inject an Authorization header only under keep-alive. Neither observed; no ~/.netrc and no proxy env on this box.

**Preuve.** Local server saw, for the same http_get(staff, no-follow): keepalive `{'User-agent': 'AKS/Staff', 'Accept-Encoding': 'gzip, deflate', 'Accept': '*/*', 'Connection': 'keep-alive'}` vs urllib `{'Accept-Encoding': 'identity', 'User-Agent': 'AKS/Staff', 'Connection': 'close'}`; exactly one UA header on both. `requests.utils.default_headers()` = {'User-Agent': 'python-requests/2.32.3', 'Accept-Encoding': 'gzip, deflate', 'Accept': '*/*', 'Connection': 'keep-alive'}; `Session.merge_environment_settings` reads REQUESTS_CA_BUNDLE/CURL_CA_BUNDLE when trust_env; `rebuild_auth` calls get_netrc_auth when trust_env.

**Piste (non appliquée).** Reword README/requirements.txt to 'same probe contract (status/ok/body), not byte-identical requests'; optionally set `_SESSION.trust_env = False` and `_SESSION.headers.pop('Accept-Encoding')` if wire parity with urllib is actually wanted.

**Votes.**
- reproduce : confirmé (c=0.9, sév. nit, dir. NEUTRAL) — Reproduced at HEAD with a scripted 127.0.0.1 server (scratchpad verify/f3-reproduce/repro.py) calling the real src.aks_env.http_get once per backend (toggling ae._SESSION). Keep-alive backend sent {'User-agent': ..., 'Accept-Encoding': 'gzip, deflate', 'Accept': '*/*', 'Connection': 'keep-alive'}; urllib backend sent {'Accept-Encoding': 'identity', 'User-Agent': ..., 'Connection': 'close'}. With HOME pointed at a dir containing a .netrc for 127.0.0.1, the keep-alive backend added 'Authorization: Basic Ym9iOnNlY3JldA==' while urllib did not (trust_env=True on _SESSION; requests.sessions rebuild_auth uses get_netrc_auth when trust_env, merge_environment_settings reads REQUESTS_CA_BUNDLE/CURL_CA_BUNDLE). So the literal 'identical behavior' wording at README.md:180, requirements.txt:11 and src/aks_env.py:7 is overstated at the wire level. However the finding's own caveat also held: a gzip-ne
- reread : confirmé (c=0.85, sév. nit, dir. NEUTRAL) — Re-read at HEAD and reproduced on a local 127.0.0.1 server. The three "identical behavior" statements are all NEW in 184b2b8 (README.md:180, requirements.txt:11 — file did not exist at 184b2b8~1 — and src/aks_env.py:7 docstring), so this is introduced by the commits under review, not pre-existing. Code path: src/aks_env.py:330-345 builds a bare `requests.Session()` (only cookie policy + max_redirects=10 adjusted; `trust_env` left at its default True, default headers untouched); `_http_open_keepalive` (371-424) passes `dict(request.header_items())`, which urllib capitalizes to 'User-agent', on top of requests' session defaults. Experiment (same `http_get(url, follow_redirects=False)`, backend swapped via `_SESSION=None`): keep-alive sent {'User-agent': <REQUIRED_UA>, 'Accept-Encoding': 'gzip, deflate', 'Accept': '*/*', 'Connection': 'keep-alive'}; urllib sent {'Accept-Encoding': 'identity
- materiality : RÉFUTÉ (c=0.85, sév. nit, dir. NEUTRAL) — Technically true, immaterial here. Confirmed on a local 127.0.0.1 server: keep-alive sends {'User-agent', 'Accept-Encoding: gzip, deflate', 'Accept: */*', 'Connection: keep-alive'} vs urllib {'Accept-Encoding: identity', 'User-Agent', 'Connection: close'}; _SESSION.trust_env=True, max_redirects=10. But everything the project actually consumes is identical across backends: ok/status/body contract (src/aks_env.py:369-374 docstring; both backends returned ok=True/200/same body), request count, redirect ceiling (aks_env.py:342), no cookie storage (aks_env.py:336), staff-UA host lock enforced in http_get before either backend (aks_env.py:472). The wire-shape difference cannot fail OPEN: the invariant gate (src/invariants.py:66-70) and resolve probes (src/matcher.py:1371, scripts/03_match.py:72) treat any non-200/transport error as ok=False, so a hypothetical WAF rule keyed on Accept-Encoding,


### [nit/CLOSED] Python 3.10 urllib has no http_error_308 — backends diverge on a same-domain 308 in follow mode (README allows 3.10)
`README.md:177` — lentille(s) : parity+ops · origine : prior-run-2026-09-08

**Constat.** README says 'Python 3.10+'. CPython added `http_error_308` to HTTPRedirectHandler in 3.11 (bpo-40321); on 3.10 urllib does not follow a 308 at all, so `_http_open_urllib` would return HTTPError(308) → ok=False (308 not in ACCEPTED_AKS_STATUSES) where the keep-alive backend (any Python) follows the 308 to the 200 → ok=True. Not exercisable here (only 3.13 installed) — stated from CPython history, so treat as PLAUSIBLE. The no-follow gate is unaffected on any version (308 surfaces as HTTPError(308) ok=False on both), and an off-domain 308 cannot leak the staff UA on 3.10 either (urllib never follows it). Production VPS is Debian 12 / Python 3.11 per ops/BROWSER_RUNBOOK.md §3, so moot there. [merged, ops lens] 'identical behavior' and the 308 handling written into _http_open_keepalive (:401) hold only on 3.11+ (the Debian 12 target); the same 'Python 3.10+' claim is repeated in docs/CONTRIBUTING.md:19 and ops/BROWSER_RUNBOOK.md:359.

**Scénario.** Someone runs the matcher on a Python 3.10 box WITHOUT requests: an AKS canonical 308 → 200 makes the resolve fail closed ('AKS probe unreliable') while the same box WITH requests resolves it. Pure environment-dependent divergence, CLOSED on the urllib side.

**Preuve.** Python 3.13 stdlib line verified: `http_error_301 = http_error_303 = http_error_307 = http_error_308 = http_error_302` and `code in (301, 302, 303, 307, 308)` in redirect_request; matrix on 3.13 shows `308 same->200 => same` in every mode. 3.10 absence of 308 support is from the CPython 3.11 changelog (not reproduced locally). [merged, ops lens] README.md:177 `**Python 3.10+**`; docs/CONTRIBUTING.md:19; ops/BROWSER_RUNBOOK.md:359 `**Python 3.10+**`; local check `python3 3.13.5: 'http_error_308' in HTTPRedirectHandler → True` (attribute added in 3.11).

**Piste (non appliquée).** Either bump README (and CONTRIBUTING / BROWSER_RUNBOOK) to 'Python 3.11+' (matches the VPS / Debian 12) or note the 3.10 308 caveat next to the requests-optional paragraph.

**Votes.**
- reproduce : confirmé (c=0.85, sév. nit, dir. CLOSED) — The divergence mechanism reproduces concretely against the real HEAD code. Scratch script /tmp/claude-0/-root-aks-code-executor/fb12d4f4-2a87-4577-a2d8-a5d07afbcaa7/scratchpad/verify/f4-reproduce/sim310.py runs a 127.0.0.1 server (`/p` → 308 Location `/final` → 200) and calls `aks_env.http_get` through both `_http_open_urllib` (src/aks_env.py:428-436) and `_http_open_keepalive` (src/aks_env.py:369-425). On 3.13 as-is all three modes agree (follow/host_locked → ok=True 200; no-follow → ok=False 308 on both). After removing `HTTPRedirectHandler.http_error_308` (the 3.10 handler shape; `urllib.request._opener=None` to rebuild the cached default opener) the urllib backend returns `ok=False status=308 err='HTTP Error 308: Permanent Redirect'` in BOTH follow and host_locked modes, while keep-alive still returns `ok=True status=200` — exactly the finding's divergence. Downstream, src/matcher.py
- reread : confirmé (c=0.8, sév. nit, dir. CLOSED) — Claim holds on re-read, and is not merely pre-existing: the urllib path is byte-identical pre/post (184b2b8~1 src/aks_env.py:329-332 == HEAD _http_open_urllib src/aks_env.py:433-436), but the 5 commits ADD (a) the requests backend that follows 308 in follow mode (src/aks_env.py:395 host_locked tuple includes 308; :411-412 allow_redirects=True), and (b) new doc text asserting the fallback has "identical behavior" while still stating "Python 3.10+" — README.md:177-181 (blame: 184b2b8), ops/BROWSER_RUNBOOK.md:359 (blame: 0394dfc), requirements.txt:11, src/aks_env.py:7. Only docs/CONTRIBUTING.md:19 predates the range (e313ee7d). Simulation on a local 127.0.0.1 server (308 /old -> 200 /new), with HTTPRedirectHandler.http_error_308 deleted and urllib._opener reset to emulate 3.10: requests backend follow=True -> status=200 ok=True; 3.13 urllib -> 200 ok=True; 3.10-simulated urllib -> status=30
- materiality : RÉFUTÉ (c=0.85, sév. nit, dir. CLOSED) — The finding is technically plausible (CPython added http_error_308 to HTTPRedirectHandler in 3.11; here 3.13.5 confirms the attribute exists, 3.10 not installed to reproduce) but immaterial for this project. (1) Not reachable in production: the VPS is Debian 12 / Python 3.11 and requirements.txt installs `requests>=2.28`, so the urllib backend never runs there and even if it did, 3.11 follows 308. (2) On the only affected configuration (Python 3.10 AND no requests), the divergence is fail-CLOSED: `_http_open_urllib` (src/aks_env.py:428-436) surfaces HTTPError(308), 308 is not in ACCEPTED_AKS_STATUSES (src/aks_env.py:129 = 200/301/302), and the matcher turns that into `SkippedOffer(... "AKS probe unreliable (throttled?)")` (src/matcher.py:1949) — a skip, never a submit, never a green gate. The invariant gate uses no-redirect mode (`_NoRedirectHandler`, src/aks_env.py:283) and is unaffecte


### [nit/NEUTRAL] Host-locked ceiling URLError is double-wrapped by the blanket except
`src/aks_env.py:409` — lentille(s) : parity · origine : prior-run-2026-09-08

**Constat.** `raise URLError("too many redirects (host-locked)")` sits inside the `try:` block, so `except Exception as exc: raise URLError(str(exc)) from exc` (lines 415-419) wraps it a second time; the error string callers log/skip on becomes '<urlopen error <urlopen error too many redirects (host-locked)>>'.

**Scénario.** Matcher skip reason for a >=10-hop staff chain reads 'AKS probe unreliable (throttled?): slug -> <urlopen error <urlopen error too many redirects (host-locked)>>' — noisy but harmless.

**Preuve.** Matrix: `[GET follow STAFF(lock)] chain 301 x10 … keepalive: … err='<urlopen error <urlopen error too many redirects (host-locke'`.

**Piste (non appliquée).** Move the for/else `raise URLError` outside the try, or add `except URLError: raise` before the blanket except.

**Votes.**
- reproduce : confirmé (c=0.97, sév. nit, dir. NEUTRAL) — Reproduced concretely against a local 127.0.0.1 server that 301-chains indefinitely (scratch script /tmp/claude-0/-root-aks-code-executor/fb12d4f4-2a87-4577-a2d8-a5d07afbcaa7/scratchpad/verify/f5-reproduce/repro.py, with aks_env._allkeyshop_host patched in-memory to accept 127.0.0.1 so the host_locked branch follows the chain). At HEAD src/aks_env.py:392-409 the for/else `raise URLError("too many redirects (host-locked)")` is inside the `try:` opened at line 379, and the blanket `except Exception as exc: raise URLError(str(exc)) from exc` (415-419) re-wraps it. Observed: `_http_open_keepalive` raises `URLError` with str='<urlopen error <urlopen error too many redirects (host-locked)>>' and `__cause__` = URLError; through the public seam `http_get(..., user_agent=AKS_STAFF_UA)` returns ok=False status=None error='<urlopen error <urlopen error too many redirects (host-locked)>>'. The match
- reread : confirmé (c=0.95, sév. nit, dir. NEUTRAL) — Confirmed at HEAD by re-reading src/aks_env.py: the `try:` opens at line 381; the host-locked for/else `raise URLError("too many redirects (host-locked)")` is at line 409, inside that try; the only narrow handler is `except StaffUaRedirectRefused: raise` (415-416), followed by the blanket `except Exception as exc: raise URLError(str(exc)) from exc` (417-419). URLError is an Exception subclass, so the ceiling error is caught and re-wrapped. Reproduced the resulting string in isolation: '<urlopen error <urlopen error too many redirects (host-locked)>>'. That string flows unchanged into HttpProbeResult.error via `error=str(exc)` (aks_env.py:491/495, 549/553) and into the matcher skip reason at src/matcher.py:1949 (f"AKS probe unreliable (throttled?): {exc}"). Introduced by the commits under review: `_http_open_keepalive` does not exist in 184b2b8~1 (grep of the pre-commit file returns nothi
- materiality : RÉFUTÉ (c=0.9, sév. nit, dir. CLOSED) — The finding is technically true but immaterial. Reproduced locally: nesting `raise URLError("too many redirects (host-locked)")` inside the blanket `except Exception as exc: raise URLError(str(exc)) from exc` (src/aks_env.py:409, 415-419) yields str(e) == '<urlopen error <urlopen error too many redirects (host-locked)>>' and the object is still an instance of URLError. Consequences: (1) both consumers, http_get (src/aks_env.py:494-495) and http_head_status (src/aks_env.py:552-553), catch `except URLError` and return HttpProbeResult(ok=False, status=None, error=str(exc)) — the probe fails CLOSED regardless of the wrapping; (2) the matcher turns that into SkippedOffer "AKS probe unreliable (throttled?): ..." (src/matcher.py:1949) — a skip, never a submit; (3) grep over src/, scripts/, tests/, docs/ shows no code branching on the strings 'too many redirects', 'host-locked' or 'urlopen error


### [nit/NEUTRAL] SweepConfig.max_pages=400 default is dead and its comment describes the old semantics
`src/data_entry_auto.py:59` — lentille(s) : ops · origine : prior-run-2026-09-08

**Constat.** The only production constructor (scripts/10:385-386) always passes args.max_pages, so the dataclass default 400 ('safety cap on pages processed (a full shop is fewer)') is unreachable in production and now contradicts the real default (30, a deliberate shallow cap, not a safety cap).

**Scénario.** A future caller (e.g. a daemon or the admin) constructs SweepConfig without max_pages and gets a 400-page 'safety cap' while the documented product default is 30.

**Preuve.** src/data_entry_auto.py:59 `max_pages: int = 400      # safety cap on pages processed (a full shop is fewer)`; grep shows SweepConfig( only in scripts/10:385 (passes max_pages=args.max_pages) and tests (400/3).

**Piste (non appliquée).** Align the dataclass default with the script (or make it required) and rewrite the comment to describe the shallow-index rationale.

**Votes.**
- reproduce : confirmé (c=0.9, sév. minor, dir. NEUTRAL) — Confirmed at HEAD. src/data_entry_auto.py:59 still reads `max_pages: int = 400      # safety cap on pages processed (a full shop is fewer)` (set by a7d6fb2, untouched by 184b2b8), while scripts/10_data_entry_auto.py:296 sets `--max-pages default=30` with a 7-line help text describing a deliberate shallow-index cap ("submit index is only productive on the ~28-30 shallowest pages ... Raise it for a deliberate deep sweep"). The two documented defaults/rationales contradict each other. Reachability: the only non-test constructor is scripts/10:385-386, which always passes `max_pages=args.max_pages`; the admin console path (src/admin/submit_manager.py:733-735 start_data_entry_auto) also spawns scripts/10 and only appends `--max-pages` when given, so an omitted value falls to argparse's 30, never to the dataclass 400. `python3 -c "from src.data_entry_auto import SweepConfig; print(SweepConfig(m
- reread : confirmé (c=0.8, sév. nit, dir. NEUTRAL) — Re-read at HEAD: src/data_entry_auto.py:59 still reads `max_pages: int = 400      # safety cap on pages processed (a full shop is fewer)` (identical to 184b2b8~1). The only non-test constructor is scripts/10_data_entry_auto.py:385-386, which always passes `max_pages=args.max_pages`; tests pass it explicitly too (tests/test_data_entry_auto.py:221,367). So the dataclass default is unreachable in production — true both before and after. What the 5 commits changed: 184b2b8 moved the script default from 200 (`git show 184b2b8~1:scripts/10_data_entry_auto.py` line 295) to 30, with a new help text (scripts/10:296-304) and CHANGELOG.md:48-50 describing a deliberate shallow-index cap ("submit index only productive on ~28-30 shallowest pages"). Before the commit, both 200 and 400 were 'a full shop is fewer'-style safety caps, so the library comment was merely a different number; after it, the libr
- materiality : RÉFUTÉ (c=0.85, sév. nit, dir. NEUTRAL) — Technically true but immaterial. The only production constructor is scripts/10_data_entry_auto.py:385-386, which always passes max_pages=args.max_pages (argparse default 30 at scripts/10:296), so the dataclass default at src/data_entry_auto.py:59 is never reached in production. The hypothetical future caller scenario cannot fail OPEN: max_pages is a read-side page cap on a sweep whose submit stage is still gated by the validated approved.json per page; a 400 cap only means MORE pages are extracted/matched (slower, more 404 probes), never a wrong offer or a green-looking bad state. Exhausting the cap is flagged coverage_incomplete_max_pages (src/data_entry_auto.py:30-31, 283-289), so even the wider sweep is honest. Tests (tests/test_data_entry_auto.py:221, 367) pass max_pages explicitly. The comment ('safety cap... a full shop is fewer') is stale wording that no operator reads as an entry


### [nit/NEUTRAL] HANDOFF.md: dead pointer for the daemon decision, stale commit ref, '04 report' misnames the stage
`docs/HANDOFF.md:62` — lentille(s) : ops · origine : prior-run-2026-09-08

**Constat.** §4 says 'Synthèse complète : voir le CHANGELOG / l'historique de session' for the H24 daemon audit / 'Archi A', but docs/CHANGELOG.md has no daemon/H24/'Archi A' entry at all (0 hits outside HANDOFF) and session history is exactly what does not migrate — the doc's own premise. Line 7 cites 'commit 9ddf185' although the file lands in 0394dfc (HEAD). §1 lists '`04 report`' whereas scripts/04 is 04_validate.py and report.txt is written by 03_match (scripts/03_match.py:119). '38/39 findings' is not stated elsewhere (CHANGELOG:391 says 39 confirmed) — plausible but unverifiable in-repo. All other cited paths/services/routes/rule ids exist (see verified list); '1322 tests' is exactly right.

**Scénario.** Claude on the new server follows §4's pointer to find the daemon mandate synthesis and finds nothing.

**Preuve.** grep -rn 'Archi A' docs/ src/ scripts/ ops/ README.md AGENTS.md CLAUDE.md → only docs/HANDOFF.md; grep -in 'daemon\|H24' docs/CHANGELOG.md → only the openvpn daemon entries (:2043, :2062); `ls scripts/` → 04_validate.py; docs/HANDOFF.md:7 `commit \`9ddf185\``, HEAD=0394dfc.

**Piste (non appliquée).** Add a short CHANGELOG entry for the daemon audit/Archi A decision (or inline the synthesis in HANDOFF §4), reference HEAD generically, and write '03 match (+ report.txt) → 04 validate'.

**Votes.**
- reproduce : confirmé (c=0.9, sév. nit, dir. NEUTRAL) — Doc finding checked against HEAD (e7586f6). Core claims confirmed: (1) docs/HANDOFF.md:70 says "Synthèse complète : voir le CHANGELOG / l'historique de session" for the H24 daemon / Archi A decision, but `grep -in 'daemon\|H24\|mandat' docs/CHANGELOG.md` hits only the openvpn daemon entries (:2043, :2062) and `grep -rn 'Archi A'` across docs/ src/ scripts/ ops/ README/AGENTS/CLAUDE returns only HANDOFF.md:66 — the pointer is dead and "session history" is by the doc's own premise (HANDOFF.md:4-5) exactly what does not migrate. (2) HANDOFF.md:17 lists "`04 report`" but `ls scripts/` shows 04_validate.py; report.txt is written by scripts/03_match.py:119, and README.md:134-135 / docs/ARCHITECTURE.md:33-34 name the stages "03 match → report" and "04 validate" — HANDOFF's own §7 (line 135) even says 03_match → report.txt, contradicting §1. (3) '38/39' at HANDOFF.md:50 is only partially backed 
- reread : confirmé (c=0.8, sév. nit, dir. NEUTRAL) — Two of the four sub-claims are verified true at HEAD and are specific to the new file (0394dfc + e7586f6): (1) docs/HANDOFF.md:70 points to "le CHANGELOG / l'historique de session" for the H24 daemon / Archi A synthesis, but docs/CHANGELOG.md has zero daemon/H24/mandat/'Archi A' entries (only openvpn daemon at :2043/:2062) and session history is exactly what HANDOFF.md:3-5 says does not migrate; (2) HANDOFF.md:17 lists "`04 report`" while scripts/04_validate.py is the stage-04 script and report.txt is written by scripts/03_match.py:119 (README.md:135, docs/ARCHITECTURE.md:34 both call stage 04 "validate"). Two sub-claims are wrong or weak: the `9ddf185` ref (HANDOFF.md:7) is the last code commit and a doc commit cannot cite its own hash — defensible, not stale; and "38/39 unverifiable" is false — CHANGELOG:391 (39 confirmés) + CHANGELOG:379 ([7] DÉCLINÉ) yields 38/39 in-repo. 1322 tests 
- materiality : RÉFUTÉ (c=0.85, sév. nit, dir. NEUTRAL) — All three factual claims check out but none is material. (1) Dead pointer: docs/HANDOFF.md:62 'Synthèse complète : voir le CHANGELOG / l'historique de session' — grep 'Archi A' outside HANDOFF returns nothing and docs/CHANGELOG.md 'daemon' hits are only the openvpn entries (:2043, :2062). But the load-bearing decision is already inline in §4 (lines 55-62): mandate DEFERRED, no daemon code until Romain ratifies in writing, Archi A preference, single Chrome tab + machine-wide lock, park fail-closed on dead session, never auto-repair. A fresh Claude following the pointer loses only a longer write-up, not the decision; the missing content biases toward NOT building anything (fail-closed), so it cannot unlock a write or make a bad state look green. (2) 'commit 9ddf185' at line 7 with the file landing in 0394dfc/e7586f6: it reads as 'state described as of 9ddf185' (the last code commit), a one


### [nit/NEUTRAL] by-urls resolve loop has no inter-URL pacing; keep-alive makes it the densest staff-UA burst (pre-existing)
`scripts/11_data_entry_by_urls.py:328` — lentille(s) : ops · origine : prior-run-2026-09-08

**Constat.** resolve_pinned is called back-to-back per operator URL (only the transient-retry backoff sleeps), and AKS_PROBE_DELAY_S does not apply there; with keep-alive the gap between staff-UA GETs drops from ~134 ms to ~32 ms (~1800 rpm theoretical on a long list). Pre-existing lack of pacing, made denser by 184b2b8; not covered by the '0.15 s politeness budget' story.

**Scénario.** Operator pastes 60 AKS URLs into /games → 60 staff-UA GETs in ~2 s.

**Preuve.** scripts/11:328-330 `for url in urls: … resolution = resolve_pinned(url, http_get_fn)` with no sleep; resolve_pinned sleeps only `RESOLVE_RETRY_WAIT_S` on transient (:131-132). Parity experiment: keep-alive reused 1 client port across 5 GETs.

**Piste (non appliquée).** Optional: apply AKS_PROBE_DELAY_S between URLs when http_get_fn is http_get.

**Votes.**
- reproduce : confirmé (c=0.9, sév. nit, dir. NEUTRAL) — Reproduced concretely against the real code at HEAD. scripts/11_data_entry_by_urls.py:328-330 calls resolve_pinned(url, http_get_fn) in a bare `for url in urls:` loop with no sleep; the only sleep in resolve_pinned is the transient-retry backoff at :131-132 (RESOLVE_RETRY_WAIT_S, only on 429/5xx/None). `grep -c AKS_PROBE_DELAY_S scripts/11_data_entry_by_urls.py` = 0, so the matcher's 0.15 s politeness budget (src/matcher.py:85, applied at :1415/:1439) does not cover this loop. Experiment (scratch script, local 127.0.0.1 ThreadingHTTPServer serving a parseable product page, real aks_env.http_get with the keep-alive backend active, host lock monkeypatched only in the scratch process to accept 127.0.0.1): run_plan with 60 URLs produced 60 staff-UA GETs; with HTTP/1.1 server (1 reused client port, i.e. real keep-alive) the resolve loop took 3.14 s, mean inter-request gap 52 ms, ~1146 rpm equ
- reread : confirmé (c=0.85, sév. nit, dir. NEUTRAL) — Re-read at HEAD confirms the claim. scripts/11_data_entry_by_urls.py:328-330 loops `for url in urls: resolution = resolve_pinned(url, http_get_fn)` with no sleep between URLs; resolve_pinned (:108-138) sleeps only RESOLVE_RETRY_WAIT_S on a transient retry (:131-132). AKS_PROBE_DELAY_S is defined and applied only in src/matcher.py (:85, :1415, :1439) — grep shows no import or use of it in scripts/11. `git diff 184b2b8~1 HEAD -- scripts/11_data_entry_by_urls.py` is empty, so the missing pacing is genuinely pre-existing, as the finding states. What the 5 commits DID change is that keep-alive is wired into the single seam `_http_open` (src/aks_env.py:449 `backend = _http_open_keepalive if _SESSION is not None else _http_open_urllib`), and http_get (:458+) has no pacing of its own — so every http_get caller, including script 11's staff-UA resolve loop (AKS_PROBE_UA = AKS_STAFF_UA, matcher.py:
- materiality : RÉFUTÉ (c=0.8, sév. nit, dir. NEUTRAL) — Technically true but immaterial here. (1) Pre-existing: the unpaced loop `for url in urls: resolution = resolve_pinned(url, http_get_fn)` (scripts/11_data_entry_by_urls.py:328-330) predates the reviewed commits (introduced in 91d9050; none of 184b2b8/54f1f88/9ddf185 touch scripts/11). 184b2b8 only shrinks the TCP/TLS setup gap, it adds no requests. (2) Bounded blast: the /games console entry point caps a run at 200 URLs (src/admin/app.py:592-594 `too_many_urls`), so the worst case is ~200 staff-UA GETs in a few seconds, once, followed by the browser-driven feed searches which dominate the run — nowhere near a sustained 1800 rpm; the matcher's paced path is the real bulk consumer (hundreds of probes per page, src/matcher.py:1415/1439), and that one IS paced at 0.15 s. (3) Fail direction is CLOSED, not OPEN: a 429/5xx on any URL goes through the transient retry (scripts/11:122-134, RESOLVE


### [nit/NEUTRAL] HANDOFF §7 '--mode learning' line is marked WRITE but not 'sur GO', unlike the safe line just above
`docs/HANDOFF.md:138` — lentille(s) : handoff-cmds · origine : lead-spotcheck

**Constat.** Line 137 (safe submit) says 'WRITE (safe: lot validé complet) — sur GO'; line 138 (learning canary) says 'canary de 1 (WRITE)' without 'sur GO', although CLAUDE.md requires Romain's explicit go for any submit mode (learning/advanced DO write).

**Scénario.** Reader infers learning-mode canaries need no GO.

**Preuve.** docs/HANDOFF.md:137-138; CLAUDE.md scope-separation paragraph ('submit: only on Romain's explicit go').

**Piste (non appliquée).** Append '— sur GO'.

**Votes.**
- reproduce : confirmé (c=0.6, sév. nit, dir. NEUTRAL) — Confirmed the textual asymmetry at HEAD (e7586f6): docs/HANDOFF.md:137 ends with "# WRITE (safe: lot validé complet) — sur GO" while docs/HANDOFF.md:138 ends with "# canary de 1 (WRITE)" and has no "sur GO" suffix. The two other WRITE command lines in §7 (line 151 by-urls submit, line 157 06_move --execute) both carry "sur GO", so line 138 is the only WRITE command in the section without the marker. However the statement itself is not wrong or stale: it correctly labels the learning canary as WRITE with a canary of 1 (matches CLAUDE.md scope-separation), and the GO requirement is stated generically for all submits in §8 at docs/HANDOFF.md:172 ("Submit : seulement sur go EXPLICITE de Romain"), which covers learning/advanced. So the finding is a real cosmetic inconsistency, not a factual inaccuracy; the "reader infers no GO" scenario is mitigated by §8 and by the other three WRITE lines. S
- reread : confirmé (c=0.8, sév. nit, dir. NEUTRAL) — Verified at HEAD: docs/HANDOFF.md:137 reads "... --submit   # WRITE (safe: lot validé complet) — sur GO" and line 138 reads "... --submit --mode learning   # canary de 1 (WRITE)" with no "— sur GO". The same "— sur GO" suffix is used consistently on the other WRITE command lines (line 157 for 06_move --execute, line 151 for by-urls submit), so line 138 is the one WRITE command in §7 that omits it. Line 138 was introduced by e7586f6 (HEAD doc commit; e7586f6.diff line 47), so it is in scope. The claim is accurate and not pre-existing. Mitigation: §8 line 172 states the general rule "Submit : seulement sur go EXPLICITE de Romain", and line 138 is still tagged "(WRITE)", so a reader of the whole document would not conclude learning canaries are exempt — but the per-line inconsistency within §7 is real. Docs-only, no runtime effect: severity nit, NEUTRAL fail direction stand.
- materiality : RÉFUTÉ (c=0.8, sév. nit, dir. NEUTRAL) — The finding is technically true (docs/HANDOFF.md:138 says "canary de 1 (WRITE)" with no "— sur GO", while :137 and :157 carry it), but it is immaterial in this project. (1) The GO requirement is stated universally for ALL submits in the same document: docs/HANDOFF.md:172 "**Submit** : seulement sur go EXPLICITE de Romain ; **jamais fire-and-forget**", with no mode carve-out; the header (HANDOFF.md:4-7) directs the reader to AGENTS.md + CLAUDE.md next, which restate it. (2) Line 138 is explicitly labelled "(WRITE)" directly beneath a line labelled "WRITE ... — sur GO"; a reader cannot mistake it for a read-only/dry-run command, so the only possible misreading is "write without GO", which :172 forecloses. (3) The command itself is not reachable without deliberate operator action: scripts/05_submit.py:186 requires the explicit --submit flag (default dry-run), an approved.json validation fil


### [nit/NEUTRAL] HANDOFF §7 minor inaccuracies: 'Move / tri' omits scripts 08/09, '~6 min sur 4 cœurs' (unittest is single-process), 03_match also writes skipped.json + match_meta.json
`docs/HANDOFF.md:119` — lentille(s) : handoff-cmds · origine : lead-spotcheck

**Constat.** Small factual gaps: the 'Move / tri' block (:154-158) lists only 06_move while 08_sort_plan.py / 09_sort_move.py exist for the tri flow; the unittest comment (:119) attributes the ~6 min to '4 cœurs' although unittest discover runs single-process; the 03_match comment (:135) lists candidates.json + report.txt but 03 also writes skipped.json and match_meta.json (scripts/03_match.py:88-98).

**Scénario.** Reader mis-estimates the suite time / misses the tri scripts / looks for outputs in the wrong place.

**Preuve.** ls scripts/ → 08_sort_plan.py, 09_sort_move.py; scripts/03_match.py:88-98; docs/HANDOFF.md:119, :135, :154-158.

**Piste (non appliquée).** Wording fixes.

**Votes.**
- reproduce : confirmé (c=0.8, sév. nit, dir. NEUTRAL) — Checked each cited statement against the doc and the code at HEAD. (1) docs/HANDOFF.md:135 says `03_match.py ... # → candidates.json, report.txt`; scripts/03_match.py:88-98 also writes `skipped.json` (line 91) and `match_meta.json` (line 98, the R24-mode sidecar that 05_submit/admin gate on) — confirmed omission of two real outputs, one of them load-bearing for the submit gate. (2) docs/HANDOFF.md:154-158 header is "Move / tri de listes" but lists only 06_move.py; `ls scripts/` shows 08_sort_plan.py and 09_sort_move.py, whose docstrings describe them as the all-stores tri (sort-plan / sort-move) flow, and `grep -n "08_\|09_\|sort_plan\|sort_move" docs/HANDOFF.md` returns nothing — the tri scripts are absent from the whole handoff. (3) docs/HANDOFF.md:119 "~6 min sur 4 cœurs": `python3 -m unittest discover` is single-process, and no test module uses multiprocessing/ProcessPool (only subpr
- reread : confirmé (c=0.85, sév. nit, dir. NEUTRAL) — All three factual points hold at HEAD, and §7 was introduced entirely by e7586f6 (in scope). (1) docs/HANDOFF.md:154-158 "Move / tri de listes" lists only scripts/06_move.py, while scripts/08_sort_plan.py (Stage 8 sort plan) and scripts/09_sort_move.py (Stage 9 sort-move writer) exist and are the documented tri flow (README.md:139-140, :370-397; EXECUTOR_RULES.md:1399); HANDOFF.md:22 even names the `/tri` tab. (2) docs/HANDOFF.md:119 says "~6 min sur 4 cœurs"; `python3 -m unittest discover` is single-process and grep of tests/ finds no multiprocessing/concurrent.futures/ThreadPool runner, so the core count does not affect suite time — the phrasing misleads a reader into expecting speed-up/slow-down by core count. (3) docs/HANDOFF.md:135 comment "→ candidates.json, report.txt" omits skipped.json and match_meta.json, both written unconditionally by scripts/03_match.py:88-104 (match_meta.js
- materiality : RÉFUTÉ (c=0.85, sév. nit, dir. NEUTRAL) — The three factual gaps are real (scripts/08_sort_plan.py and 09_sort_move.py exist; scripts/03_match.py:91-99 writes skipped.json and match_meta.json in addition to candidates.json/report.txt at :119; `python3 -m unittest discover` is single-process so "sur 4 cœurs" is a loose timing note), but none is material for this project. docs/HANDOFF.md:113 explicitly frames §7 as a cheat-sheet where "Les `--help` de chaque script font foi", and HANDOFF.md:19 points the reader to README.md, which fully documents 08/09 (README.md:139-140, :370-397, including their --execute/--mode gating). Every command listed in §7 is dry-run by default; the omissions cannot unlock a write, make a bad state look green, or push an operator toward an unintended WRITE. skipped.json/match_meta.json are consumed automatically by 05/admin (the "→" lists the human-facing outputs), so a reader "looking in the wrong place


### [nit/CLOSED] [fail-closed] Keep-alive now advertises Accept-Encoding gzip/deflate and decodes in-process: a corrupt/truncated gzip body is status=None (gate red / matcher AksProbeUnreliable) where urllib (identity) reports 200
`src/aks_env.py:419` — lentille(s) : security · origine : finder:security

**Constat.** requests' default headers add `Accept-Encoding: gzip, deflate` (verified on the wire; no brotli/zstandard installed here so no br/zstd) and `Session.send` decodes the body inside `r.content` (sessions.py:746). A decoding failure raises `ContentDecodingError`, which the blanket `except Exception … raise URLError(str(exc))` turns into ok=False/status=None. urllib sends `Accept-Encoding: identity` and never decodes, so the same response is a 200 with the raw bytes. Not toward green, but it is a new red/skip mode and the README's 'identical behavior' does not hold; it also means the body is decompressed in memory (unbounded, like the raw read) — only the keep-alive client can be gzip-bombed.

**Scénario.** AKS/CDN serves a truncated gzip page (mid-deploy, cache corruption) for the gate URL or a product slug: keep-alive → gate `aks_direct.status null` (red), matcher `AksProbeUnreliable` → offer skipped; urllib → status 200 (gate green; matcher proceeds to `_resolution_from_body` on the raw bytes and most likely `AksNameUnreadable`/no product id → also a skip, but a different reason).

**Preuve.** Experiment H2 (`Content-Encoding: gzip` with body `\x1f\x8b\x08\x00not-really-gzip`): keep-alive `ok:false status:null error:"<urlopen error ('Received response with content-encoding: gzip, but failed to decode it.' …"`; urllib `ok:true status:200`. Wire headers (experiment W): keep-alive `Accept-Encoding: gzip, deflate`, `Connection: keep-alive`, `User-agent: AKS/Staff`; urllib `Accept-Encoding: identity`, `Connection: close`, `User-Agent: AKS/Staff` (urllib title-cases in `do_open`, request.py:1305). `requests.utils.default_headers()` → `{'Accept-Encoding': 'gzip, deflate', 'Accept': '*/*', 'Connection': 'keep-alive', …}`.

**Piste (non appliquée).** If wire parity matters, pass `headers={..., 'Accept-Encoding': 'identity'}` from `_http_open_keepalive` (keep-alive still works); otherwise document the divergence in README/EXECUTOR_RULES instead of claiming identical behaviour.

**Votes.**
- reproduce : confirmé (c=0.92, sév. nit, dir. CLOSED) — Reproduced with the real code at HEAD (e7586f6). Scratch script at /tmp/claude-0/-root-aks-code-executor/fb12d4f4-2a87-4577-a2d8-a5d07afbcaa7/scratchpad/verify/f24-reproduce/repro.py stood up a 127.0.0.1 HTTP server returning 200 + `Content-Encoding: gzip` with body `\x1f\x8b\x08\x00not-really-gzip`, then called the real `http_get` with `_http_open` bound to each backend. Keep-alive (`_http_open_keepalive`, src/aks_env.py:419 blanket `except Exception → URLError`): `ok=False status=None err="<urlopen error ('Received response with content-encoding: gzip, but failed to decode it.'…"`. urllib (`_http_open_urllib`): `ok=True status=200 body='\x1f\x8b\x08\x00not-really-gzip'`. Wire headers seen by the server: keep-alive `Accept-Encoding: gzip, deflate`, `Connection: keep-alive`; urllib `Accept-Encoding: identity`, `Connection: close` — exactly as the finding states. Downstream effects confir
- reread : confirmé (c=0.9, sév. nit, dir. CLOSED) — Confirmed at HEAD by re-reading and by a local experiment. src/aks_env.py:333 builds a plain `requests.Session()` and _http_open_keepalive (src/aks_env.py:369-419) passes only `dict(request.header_items())` (User-Agent) so requests' default `Accept-Encoding: gzip, deflate` goes on the wire; `Session.send` accesses `r.content` when stream is unset (requests 2.32.3 sessions.py, `if not stream: r.content`), so a ContentDecodingError is raised inside `_SESSION.request(...)`, caught by the blanket `except Exception` at src/aks_env.py:415-419 and re-raised as URLError → http_get returns ok=False/status=None (src/aks_env.py:494-495). urllib's http.client adds `Accept-Encoding: identity` and never decodes, so the same response is ok=True/status=200 with raw bytes. Experiment (local 127.0.0.1 server, Content-Encoding: gzip, body b"\x1f\x8b\x08\x00not-really-gzip"): keepalive → `ok=False status=No
- materiality : RÉFUTÉ (c=0.85, sév. nit, dir. CLOSED) — Accepting the finding as technically true (requests' default `Accept-Encoding: gzip, deflate` + in-process decode in `_KeepAliveResponse.read` → `resp.content`, src/aks_env.py:359-360; blanket `except Exception → URLError` at src/aks_env.py:414-419), it is immaterial for this project. (1) Fail direction is strictly CLOSED: a decode failure becomes ok=False/status=None, which reds the invariant gate (src/invariants.py:66 uses http_get) or raises AksProbeUnreliable in the matcher — neither unlocks a write stage, submits a wrong offer, or makes a bad state look green. (2) The scenario (corrupt/truncated gzip from AKS/CDN) is a rare server-side pathology, and in the same corruption scenario the urllib/identity path returns a 200 with a truncated/garbled body that the matcher also cannot parse (`AksNameUnreadable`/no product id → skip), so the operator-visible outcome (skip, no write) converg


### [nit/CLOSED] [pre-existing, outside the 5 commits] urllib backend lets ValueError escape http_get's 'never raises' contract on a Location with an invalid IPv6 literal — even in no-redirect gate mode; the keep-alive backend is the SAFER one here (URLError → status None)
`src/aks_env.py:431` — lentille(s) : security · origine : finder:security

**Constat.** In `_http_open_urllib`, both `_NoRedirectHandler` and `_StaffUaHostGuardRedirectHandler` only override `redirect_request`, but stdlib `HTTPRedirectHandler.http_error_302` calls `urlparse(newurl)` BEFORE `redirect_request`, so a `Location:` like `http://[::1/x` raises `ValueError('Invalid IPv6 URL')` out of `urlopen` — not HTTPError/URLError/OSError — and `http_get` (lines 482-499) does not catch it. The keep-alive backend wraps the same ValueError (raised inside requests' `resolve_redirects(yield_requests=True)`, sessions.py:739-740) in URLError (line 419) → ok=False/status=None. So the two backends diverge, but toward red on both sides (crash vs None); reported so the 'identical behavior' claim is calibrated and because the crash path is the STDLIB gate path.

**Scénario.** Gate probe (staff UA, follow_redirects=False) receives a 301 whose Location has an unbalanced `[`: urllib backend → `ValueError` propagates out of `http_get` into `guard.run_step` / scripts/01 (no JSON report); keep-alive backend → `aks_direct.status null`, ok=false JSON. Neither is green.

**Preuve.** Experiment D (`/ipv6bad` → 301 `Location: http://[::1/x`, follow_redirects=False, staff UA): keep-alive `ok:false status:null error:'<urlopen error Invalid IPv6 URL>'`; urllib `EXC: ValueError: Invalid IPv6 URL` (escaped http_get). D2 (follow mode) identical. Stdlib request.py `http_error_302`: `urlparts = urlparse(newurl)` precedes `new = self.redirect_request(...)`.

**Piste (non appliquée).** Add `ValueError` to the catch in `http_get`/`http_head_status` (map to ok=False, status=None, error='ValueError: …'), matching the keep-alive mapping.

**Votes.**
- reproduce : confirmé (c=0.95, sév. nit, dir. CLOSED) — Reproduced concretely with a 127.0.0.1 server returning `301 Location: http://[::1/x` (script at /tmp/claude-0/-root-aks-code-executor/fb12d4f4-2a87-4577-a2d8-a5d07afbcaa7/scratchpad/verify/f25-reproduce/repro.py). Calling the real `src.aks_env.http_get` with each backend patched in as `_http_open`: keep-alive → `ok=False status=None error='<urlopen error Invalid IPv6 URL>'` for both follow_redirects=False and True; urllib → `EXC ValueError: Invalid IPv6 URL` escaping `http_get` for both modes. Calling the backends directly with host_locked=True (staff UA) gives the same split (URLError vs ValueError). Root cause matches the claim: stdlib `HTTPRedirectHandler.http_error_302` calls `urlparse(newurl)` before `redirect_request`, so `_NoRedirectHandler` (src/aks_env.py:283-292) and `_StaffUaHostGuardRedirectHandler` (:302-321) never get a chance to return None/refuse; `http_get`'s except cla
- reread : confirmé (c=0.9, sév. nit, dir. CLOSED) — Claim verified by independent re-read and local reproduction, and it is correctly labelled pre-existing. (1) Code: src/aks_env.py:281-289 `_NoRedirectHandler` and :300-318 `_StaffUaHostGuardRedirectHandler` override only `redirect_request`; stdlib `HTTPRedirectHandler.http_error_302` (Python 3.13 urllib/request.py, printed via inspect) runs `urlparts = urlparse(newurl)` BEFORE calling `self.redirect_request(...)`, so an unbalanced-bracket Location raises `ValueError('Invalid IPv6 URL')` regardless of the handler's mode. `http_get` (src/aks_env.py:481-499) catches only HTTPError / URLError / (HTTPException, TimeoutError, OSError) — ValueError is not covered. The keep-alive backend (src/aks_env.py:414-419) wraps `except Exception` into `URLError`, so it maps to ok=False/status=None. (2) Reproduction against a local 127.0.0.1 server returning `301 Location: http://[::1/x`: HEAD keep-alive f
- materiality : RÉFUTÉ (c=0.9, sév. nit, dir. CLOSED) — Accepting the finding as technically true, it is immaterial for this project and outside the 5 commits under review. (1) Production path: requirements.txt installs `requests>=2.28`, so on the Debian VPS `_SESSION` is non-None and `_http_open` (src/aks_env.py:442-444) selects `_http_open_keepalive`, whose blanket `except Exception → URLError` (src/aks_env.py:415-419) maps the ValueError to ok=False/status=None — the reviewer's own Experiment D confirms this. The urllib crash path only exists when requests is missing/broken, i.e. the non-default fallback. (2) Even on the fallback path, the failure is strictly CLOSED: `StepGuard.run_step` records the exception as a failure and re-raises (src/step_guard.py:317-319); `build_report` (src/invariants.py:62-69) is not wrapped, so scripts/01_check_invariants.py aborts with a traceback and a non-zero exit — no JSON report, hence no `ok:true` / `aut


### [nit/NEUTRAL] docs/EXECUTOR_RULES.md §4.1 [R01]/[R28] (the authoritative skill→code bridge) still describes tokenization as 'NFKC-normalize, then apostrophes' — the new pre-NFKC symbol strip is not documented there
`docs/EXECUTOR_RULES.md:194` — lentille(s) : matcher · origine : finder:matcher

**Constat.** CLAUDE.md/AGENTS.md make docs/EXECUTOR_RULES.md the authoritative per-stage rule spec that must be read before changing any stage; §4.1 now describes a tokenization that differs from the code (the strip runs BEFORE the NFKC it describes, and it changes what R01 compares). CHANGELOG and HANDOFF were updated, EXECUTOR_RULES was not.

**Scénario.** A builder following EXECUTOR_RULES §4.1 to re-derive or port tokenize would reintroduce the 'Company™' → 'COMPANYTM' R01 false-skip; an auditor checking code against the spec sees a divergence.

**Preuve.** docs/EXECUTOR_RULES.md:194-196 'Tokenize the AKS product name (NFKC-normalize, then apostrophes U+2019/U+2018 → \'). Every meaningful word …'; :198-209 [R28] NFKC paragraph; `grep -n -E "™|trademark|symbol|_NFKC_LETTER" docs/EXECUTOR_RULES.md` → no hits. Code: src/matcher.py:502 `text = _NFKC_LETTER_SYMBOL_RE.sub(" ", text)` before `unicodedata.normalize("NFKC", text)`.

**Piste (non appliquée).** Add a short [R28b] sentence to §4.1: 'Trademark/legal symbols whose NFKC form is letters (™→TM, ℠→SM, №→No, ℡→TEL) plus © ® ℗ are replaced by a space BEFORE NFKC (Eneba "Company™" 2026-09-08); Roman numerals and letterlike math symbols are left to NFKC.'

**Votes.**
- reproduce : confirmé (c=0.92, sév. nit, dir. NEUTRAL) — Confirmed at HEAD. docs/EXECUTOR_RULES.md:194-196 says "Tokenize the AKS product name (NFKC-normalize, then apostrophes U+2019/U+2018 → ')" and :198-209 [R28] "NFKC first" — no mention of the symbol strip (grep for ™/trademark/symbol/_NFKC_LETTER in the doc: zero hits; last commit touching EXECUTOR_RULES is 5a90c64, before 184b2b8). Code: src/matcher.py:480 `_NFKC_LETTER_SYMBOL_RE = re.compile("[©®℀℁℅℆№℗℠℡™]")`, :502 `text = _NFKC_LETTER_SYMBOL_RE.sub(" ", text)` runs BEFORE `unicodedata.normalize("NFKC", ...)` in normalize_apostrophes (introduced in commit 184b2b8, combined.diff lines 553/564). The strip is documented in docs/HANDOFF.md:54-55 and CHANGELOG, but not in the authoritative spec. Scenario reproduced: a tokenizer written strictly per §4.1 (NFKC → apostrophes → [A-Z0-9']+) gives tokenize("Company™") == ['COMPANYTM'] and reports 'COMPANY' missing (R01 false-skip), whereas the r
- reread : confirmé (c=0.92, sév. nit, dir. NEUTRAL) — Confirmed by re-reading HEAD. src/matcher.py:480-481 defines _NFKC_LETTER_SYMBOL_RE ("[©®℀℁℅℆№℗℠℡™]") and src/matcher.py:502 applies it as a space-replace BEFORE unicodedata.normalize("NFKC") inside normalize_apostrophes, which tokenize() (src/matcher.py:508-513) and therefore R01 missing_aks_words consume. This strip did not exist pre-commit: `git show 184b2b8~1:src/matcher.py` has 0 hits for _NFKC_LETTER_SYMBOL_RE/™; it was introduced in 184b2b8 (re.sub(r"[™℠®©℗]"," ",name) in tokenize) and moved into normalize_apostrophes + widened in 54f1f88. docs/EXECUTOR_RULES.md:194-196 still says "Tokenize the AKS product name (NFKC-normalize, then apostrophes ...)" and the [R28] paragraph (:198-209) says NFKC runs first "before both"; grep for ™/trademark/symbol/_NFKC_LETTER in that file yields nothing, and `git log 184b2b8~1..HEAD -- docs/EXECUTOR_RULES.md` shows no commits, whereas docs/CHANGE
- materiality : RÉFUTÉ (c=0.72, sév. nit, dir. CLOSED) — Confirmed facts: docs/EXECUTOR_RULES.md:194-209 (§4.1 [R01]/[R28]) describes tokenization as "NFKC-normalize, then apostrophes" with no mention of the pre-NFKC symbol strip; src/matcher.py:480-503 strips [©®℀℁℅℆№℗℠℡™] to a space BEFORE unicodedata.normalize("NFKC"). So the spec/code divergence is real. Why it is immaterial here: (1) Fail direction is CLOSED, not OPEN — the only consequence of a builder re-deriving tokenize from §4.1 is reintroducing "Company™" → COMPANYTM, which makes R01 (necessary-condition name match) FALSE-SKIP a genuine offer; it cannot unlock writes, submit a wrong offer, or make a bad state look green. (2) The behaviour is locked by a unit test — tests/test_matcher.py:115-137 test_trademark_symbol_does_not_glue_into_a_token asserts tokenize("Company™") == ["COMPANY"] and that COMPANYTM never appears — so a spec-driven regression would fail the suite before reachin


### [nit/NEUTRAL] Comment/commit-message drift around _NFKC_LETTER_SYMBOL_RE: the ℅/℀/℁/℆ 'glue' rationale is wrong (they decompose with a '/', never glued), the commit message lists a 5-symbol set while the code strips 11, and the normalize_apostrophes docstring does not mention the strip
`src/matcher.py:473` — lentille(s) : matcher+tests · origine : finder:matcher+finder:tests

**Constat.** The comment cites '"℅"→"c/o"' as an example of NFKC gluing letters onto the adjacent word; the '/' in c/o, a/c, a/s, c/u breaks the token, so old tokenize gave separate stray 'C','O' tokens, not a glued word (stripping them is harmless and even drops spurious extra-word tokens, but the stated reason is inaccurate). Commit 184b2b8's message says 'tokenize now strips ™ ℠ ® © ℗ BEFORE NFKC' whereas the code strips `©®℀℁℅℆№℗℠℡™` inside normalize_apostrophes (the CHANGELOG top entry has the right set). The docstring at :485 still opens 'NFKC-normalize, then fold curly quotes' with no mention that a symbol class is removed first — the only place a reader of `help(normalize_apostrophes)` would look. [merged, tests lens] The regex header says the listed symbols' NFKC decomposition "is a LETTER sequence", but ©, ®, ℗ have no decomposition at all and ℀℁℅℆ decompose to letter/slash/letter; the function docstring still opens with "NFKC-normalize, then fold curly quotes" and "NFKC first" although the strip now runs before NFKC.

**Scénario.** Documentation-only: a reader trusting the docstring or commit message mis-models the function (e.g. expects ® to survive in cleaned_title output, or expects № to be kept). [merged, tests lens] A maintainer trusting the comment removes © ® ℗ as 'redundant with NFKC' (they are not — they are stripped so a glued `Halo®Deluxe` splits, per the test) or reorders the strip after NFKC 'to match the docstring', re-creating the `COMPANYTM` glue.

**Preuve.** Experiment: old tokenize('Game ℅ Steam Key') == ['GAME','C','O','STEAM','KEY'] (not glued), new == ['GAME','STEAM','KEY']; NFKC('℅')=='c/o'. src/matcher.py:481-482 `re.compile("[©®℀℁℅℆№℗℠℡™]")`; `git show 184b2b8` body: 'tokenize now strips ™ ℠ ® © ℗ BEFORE NFKC'. Docstring :485-499 unchanged apart from the code line. [merged, tests lens] `unicodedata.normalize('NFKC', ch)`: U+00A9 '©' -> '©' (NO decomposition), U+00AE '®' -> '®' (NO), U+2117 '℗' -> '℗' (NO), ℀ -> 'a/c', ℁ -> 'a/s', ℅ -> 'c/o', ℆ -> 'c/u', № -> 'No', ℠ -> 'SM', ℡ -> 'TEL', ™ -> 'TM'. src/matcher.py:473 `# Trademark / legal / abbreviation symbols whose NFKC compatibility decomposition is a LETTER sequence`; :485 `"""NFKC-normalize, then fold curly quotes to ASCII `'`.`; :487 `NFKC first (Eneba escape, 2026-07-16)`. (tests/test_matcher.py:120-121 already states the ®/© fact correctly.) Note only, not a request to widen: ℻ U+213B -> 'FAX' is the same glue family and is not in the set.

**Piste (non appliquée).** One-line docstring addition ('Trademark/legal symbols (see _NFKC_LETTER_SYMBOL_RE) are replaced by a space first' / 'Legal/trademark symbols are blanked BEFORE NFKC'); reword the comment to "symbols that NFKC either glues into letters (™ ℠ № ℡ ℀ ℁ ℅ ℆) or leaves as a non-token character that can glue words (© ® ℗)" and fix the ℅ example (or drop ℀℁℅℆ — they never glued).

**Votes.**
- reproduce : confirmé (c=0.93, sév. nit, dir. NEUTRAL) — All four documentation claims hold at HEAD. (1) src/matcher.py:473-474 says the listed symbols' "NFKC compatibility decomposition is a LETTER sequence" and cites '"℅"→"c/o"' as a glue example; experiment: NFKC('℅')=='c/o', NFKC('℀')=='a/c', NFKC('℁')=='a/s', NFKC('℆')=='c/u' — the '/' is outside tokenize's `[A-Z0-9']+` class, so pre-184b2b8 tokenize('Game ℅ Steam Key') gave ['GAME','C','O','STEAM','KEY'] (stray tokens, never glued); HEAD gives ['GAME','STEAM','KEY']. Stripping is harmless but the stated rationale is wrong for those four. (2) NFKC('©')=='©', NFKC('®')=='®', NFKC('℗')=='℗' — no decomposition at all, so "decomposition is a LETTER sequence" is false for 3 of the 11 symbols in `_NFKC_LETTER_SYMBOL_RE` (src/matcher.py:481-482 `"[©®℀℁℅℆№℗℠℡™]"`). (3) `git show 184b2b8` body: "tokenize now strips ™ ℠ ® © ℗ BEFORE NFKC" — 5 symbols, attributed to tokenize; code strips 11 inside n
- reread : confirmé (c=0.92, sév. nit, dir. NEUTRAL) — Re-read src/matcher.py:472-505 at HEAD and compared with `git show 184b2b8~1:src/matcher.py` (no strip existed before; the regex, comment and code line are all introduced by 184b2b8). Every factual assertion in the finding holds: (1) comment at :473-474 says the listed symbols' NFKC decomposition "is a LETTER sequence" and cites "℅"→"c/o" as a glue example, but unicodedata.normalize('NFKC') gives © -> ©, ® -> ®, ℗ -> ℗ (no decomposition at all) and ℀/℁/℅/℆ -> 'a/c','a/s','c/o','c/u' (a '/' that splits the token — old tokenize('Game ℅ Steam Key') == ['GAME','C','O','STEAM','KEY'], not glued; new == ['GAME','STEAM','KEY']). (2) `git show 184b2b8` body says "tokenize now strips ™ ℠ ® © ℗ BEFORE NFKC" while the code strips 11 symbols `[©®℀℁℅℆№℗℠℡™]` inside normalize_apostrophes (not tokenize); docs/CHANGELOG.md:21 has the correct 11-symbol set. (3) The normalize_apostrophes docstring (:485-4
- materiality : RÉFUTÉ (c=0.9, sév. nit, dir. NEUTRAL) — Technically true but immaterial. Verified: src/matcher.py:473-475 comment claims every listed symbol's NFKC decomposition "is a LETTER sequence" and cites "℅"→"c/o"; ©/®/℗ have no decomposition and ℀℁℅℆ decompose with a '/', so the rationale is loose. src/matcher.py:485-487 docstring still opens "NFKC-normalize, then fold curly quotes" / "NFKC first" while :501 runs the strip before NFKC. Commit 184b2b8 body says "strips ™ ℠ ® © ℗" vs the 11-char class at :481-482. All three are comment/commit-text drift with zero runtime effect: the strip behaviour is deterministic and correct (glued "Company™"/"Halo®Deluxe" split), and it cannot unlock writes, alter a submit decision, or make a bad state look green — no operator path reads this code comment (entry point is docs/HANDOFF.md; CHANGELOG already has the right set). The only conceivable harm — a maintainer reordering the strip after NFKC or 


### [nit/NEUTRAL] `# pragma: no cover - trivial import guard` is a foreign convention here and mislabels a block that sets two behavioural knobs
`src/aks_env.py:329` — lentille(s) : tests · origine : finder:tests

**Constat.** It is the only `pragma` in src/scripts/tests, no coverage tooling exists in the repo (no .coveragerc/pyproject/tox, `coverage` not installed), and the guarded block is not trivial: it configures the cookie policy (:338) and the redirect ceiling (:344) whose values are exactly what 9ddf185 added a test for.

**Scénario.** If coverage.py is adopted later, per its documented rule an excluded line that introduces a block excludes the whole compound statement, so the Session-configuration lines never appear as (un)covered; the `except Exception` fallback branch (the production path on a stdlib-only box) is hidden the same way.

**Preuve.** `grep -rn pragma src scripts tests --include=*.py` -> only `src/aks_env.py:329:try:  # pragma: no cover - trivial import guard`; `ls .coveragerc setup.cfg pyproject.toml tox.ini` -> none; `python3 -c 'import coverage'` -> ModuleNotFoundError. Block body: `_SESSION.cookies.set_policy(DefaultCookiePolicy(allowed_domains=[]))` and `_SESSION.max_redirects = 10`.

**Piste (non appliquée).** Drop the pragma (nothing consumes it), or move it to the single `import requests as _requests` line.

**Votes.**
- reproduce : confirmé (c=0.8, sév. nit, dir. NEUTRAL) — Every factual claim checks out at HEAD. src/aks_env.py:329 reads `try:  # pragma: no cover - trivial import guard` and `grep -rn pragma src scripts tests --include=*.py` returns only that line (introduced by 184b2b8, `git log -S`). No .coveragerc/setup.cfg/pyproject.toml/tox.ini exist, `import coverage` -> ModuleNotFoundError, no coverage package anywhere in site-packages, and docs mention "coverage" only in the feed-sweep sense — nothing consumes the pragma. The guarded body is not a trivial import guard: it builds `_SESSION`, sets `DefaultCookiePolicy(allowed_domains=[])` (:338) and `max_redirects = 10` (:344), and tests/test_aks_env.py:398-407 specifically asserts `env._SESSION.max_redirects == 10`, so the comment mislabels behaviour-bearing code. One sub-point of the scenario is overstated: coverage.py's suite-exclusion ends when indentation returns to the `try:` level, so the `excep
- reread : confirmé (c=0.7, sév. nit, dir. NEUTRAL) — Re-read src/aks_env.py:329-347 at HEAD: `try:  # pragma: no cover - trivial import guard` guards not just `import requests` but `_SESSION = requests.Session()`, `_SESSION.cookies.set_policy(DefaultCookiePolicy(allowed_domains=[]))` (:338) and `_SESSION.max_redirects = 10` (:344) — two behavioural knobs, the second explicitly tested by 9ddf185. `grep -rn pragma src scripts tests --include=*.py` returns only this line; `.coveragerc/setup.cfg/pyproject.toml/tox.ini` are absent; `import coverage` fails. `git log -S"pragma: no cover" -- src/aks_env.py` shows it was introduced in 184b2b8 (pre-commit file has no _SESSION/requests code), so it is in scope. Coverage.py's documented rule (an excluded line that starts a block excludes the whole block) makes the label misleading if coverage is ever adopted. No runtime or test behaviour is affected, so this is a comment-accuracy nit, NEUTRAL fail dir
- materiality : RÉFUTÉ (c=0.92, sév. nit, dir. NEUTRAL) — Facts hold: src/aks_env.py:329 carries the only `# pragma: no cover` in src/scripts/tests; no .coveragerc/pyproject.toml/setup.cfg/tox.ini exist; coverage is not in requirements and not importable. The guarded block does set `_SESSION.cookies.set_policy(DefaultCookiePolicy(allowed_domains=[]))` (:338) and `_SESSION.max_redirects = 10` (:344), so the "trivial import guard" label understates the body. But nothing consumes the pragma: it is a comment with zero runtime effect on Python 3.11/Debian, on the invariant gate, resolve probes, or admin subprocesses. It cannot unlock writes, alter a submit, or make a red state look green. The only hypothetical impact is a future coverage.py adoption misreporting these lines, which is a tooling-hygiene concern with no operator-facing surface (HANDOFF.md does not mention coverage). It does not touch any AGENTS.md reviewed decision. Purely stylistic/co


### [nit/NEUTRAL] CHANGELOG "9 findings confirmés": only findings 1-4 are traceable; 5-9 have no trace in the diff, CHANGELOG, AUDIT.md or commit messages
`docs/CHANGELOG.md:8` — lentille(s) : tests · origine : finder:tests

**Constat.** The adversarial-pass record names the critical (branch order), minors 2/3 (strip move + broadening) and 4 (except mapping); the other five confirmed findings are not enumerated anywhere in the repo, so whether they were fixed, declined, or dropped is unknowable from the codebase.

**Scénario.** A later audit (or the next resume from HANDOFF.md) cannot tell if e.g. the 9-vs-10 hop divergence or the hop-2 cookie forwarding (both re-found today by the parity reviewer) were among the five and consciously accepted, so they get re-derived and re-argued each pass — exactly what AGENTS.md's "Reviewed decisions" section exists to prevent.

**Preuve.** docs/CHANGELOG.md:8-18 mentions `1 CRITIQUE`, `(2/3) le strip ... déplacé ... et élargi`, `(4) l'except keepalive`; `grep -n -i 'keep-alive|keepalive|requests' docs/AUDIT.md` -> no hits; commit 54f1f88 body lists the same three minors only.

**Piste (non appliquée).** Add a 5-line list of findings 5-9 with their disposition (fixed in <sha> / declined: reason / deferred) to the CHANGELOG entry or AUDIT.md, and move any declined ones into AGENTS.md "Reviewed decisions".

**Votes.**
- reproduce : confirmé (c=0.9, sév. nit, dir. NEUTRAL) — Doc finding, checked against HEAD. docs/CHANGELOG.md:8 states "9 findings confirmés, dont 1 CRITIQUE"; the same entry then enumerates only the critical (branch order, lines 9-18), "(2/3) le strip ... déplacé ... et élargi" (lines 19-21) and "(4) l'except keepalive" (lines 21-22). Nothing numbered 5-9 appears there. Commit 54f1f88's body lists exactly the same three items (critical + two "Minors from the same pass" bullets). 9ddf185's body/CHANGELOG lines 24-30 describe a *separate* re-verification pass ("re-vérif adversariale ... Seul reste un écart pré-existant"), not the original nine. `git grep -n -i -E 'findings? (5|6|7|8|9)|9 findings|neuf findings'` over tracked files hits only CHANGELOG:8 (this line) and CHANGELOG:391 (the unrelated 39-finding Fable campaign). `grep -i 'keep-alive|keepalive|max_redirects'` finds no entry in docs/AUDIT.md (its last sections are G5/G6/SS1-SS7, unrel
- reread : confirmé (c=0.88, sév. nit, dir. NEUTRAL) — Claim verified against HEAD. docs/CHANGELOG.md:8 (entry introduced in 54f1f88, inside the reviewed range; absent in `git show 184b2b8~1:docs/CHANGELOG.md`) says "9 findings confirmés"; lines 8-18 enumerate only the critical branch-order fail-open, "(2/3)" the NFKC-symbol strip move/broadening, and "(4)" the keep-alive except mapping. Lines 20-25 attribute the 30-vs-10 max_redirects gap to a separate later "re-vérif" and mark it pré-existant, so it is not one of the nine. Commit 54f1f88's body lists exactly the same critical + 3 minors; 9ddf185 covers only the redirect ceiling. grep over docs/AUDIT.md finds no keep-alive/requests/2026-09-08 entries; docs/HANDOFF.md:56-59 and §5 (lines 78-86, Reviewed decisions) mention only the critical fix, R25 and the software-region catch-all; src/aks_env.py:383,418 and tests/test_aks_env.py:380,398 annotate only the critical, "minor #4" and the re-ver
- materiality : RÉFUTÉ (c=0.8, sév. nit, dir. NEUTRAL) — The claim is technically accurate: docs/CHANGELOG.md:8 says "9 findings confirmés" and the entry (lines 8-24) plus the commit bodies of 54f1f88 and 9ddf185 only enumerate the critical branch-order bug, minors (2/3) strip move/broadening, (4) except mapping, and the residual redirect-ceiling item; docs/AUDIT.md has no keep-alive entry, docs/HANDOFF.md:56-59 only summarizes "fail-open critique ... corrigé (54f1f88), puis durci (9ddf185)". So 4-5 of the 9 dispositions are not traceable in the repo.

Materiality: this is purely a documentation-traceability gap with no production path. It cannot unlock a write stage, cannot make a bad state look green, and cannot mislead an operator into a write (HANDOFF §7 command listings and the invariant gate are unaffected). Fail direction is NEUTRAL; severity is nit as claimed. The concrete re-litigation risk cited (9-vs-10 hop, hop-2 cookie forwarding)


### [nit/CLOSED] Host-locked keep-alive loop mishandles a non-ASCII Location: latin-1 byte → URLError/status None (urllib follows to 200); UTF-8 bytes → hop-2 path double-encoded to mojibake (urllib requests the right URL) — verified locally
`src/aks_env.py:395` — lentille(s) : critic · origine : critic

**Constat.** The host-locked branch re-implements redirect following (`location = resp.headers.get("Location")` → `urljoin` → `_SESSION.request(...)`) without urllib's `http_error_302` re-quoting (`quote(newurl, encoding="iso-8859-1", safe=string.punctuation)`), and without requests' own `get_redirect_target` latin1→utf8 re-decode that the non-locked path gets for free. Two divergences result, both on the staff-UA resolve path and one also on the no-redirect gate path: (1) a Location carrying a lone ISO-8859-1 byte (0xE9) makes requests raise `UnicodeDecodeError` from `sessions.py:124 get_redirect_target` → `_internal_utils.py:33 to_native_string(location, 'utf8')`, reached via `sessions.py:739 send: r._next = next(self.resolve_redirects(..., yield_requests=True))` — i.e. requests prepares the NEXT hop even with `allow_redirects=False`, so this fires in every mode; the blanket except maps it to URLError → http_get status=None. urllib re-quotes the byte as `%E9` and follows to 200. (2) a Location carrying UTF-8 bytes (C3 A9 — what WordPress emits for a non-ASCII permalink) is decoded latin-1 by http.client ('Ã©'), and urllib3's `_encode_invalid_chars` re-encodes that mojibake as UTF-8, so hop 2 is requested as `/blog/buy-pok%C3%83%C2%A9mon-…` (a 404 on any real site) while urllib re-encodes iso-8859-1 and requests the correct `%C3%A9` path. Consequences (all fail-closed, no UA leak — hop 2 stays on-domain): in `resolve_aks` (host-locked, follow) case 1 → status None → `AksProbeUnreliable` skip, case 2 → 404 → `continue` → eventually 'no AKS product page found' false-skip where urllib would have matched; in the invariant/03_match gate (no-follow) case 1 → status None → RED where urllib reports HTTPError(301/302) → ok=True GREEN; in scripts/11 `resolve_pinned` case 1 → 3 'transient' retries then abort. AKS slugs are ASCII (`[a-z0-9-]`, `quote()` for `?s=`), so realism on AKS is low, but this contradicts the 'exact mirror / identical behavior' claims and is not covered by any existing test or by the listed parity findings.

**Scénario.** Staff-UA resolve probe GET https://www.allkeyshop.com/blog/buy-x-cd-key-compare-prices/ → AKS answers 301 with `Location: /blog/buy-pokémon-cd-key-compare-prices/` as raw UTF-8 bytes → keep-alive requests `/blog/buy-pok%C3%83%C2%A9mon-cd-key-compare-prices/` → 404 → slug loop continues → offer skipped 'no AKS product page found (slug not 200)'; urllib fallback on the same box requests `/blog/buy-pok%C3%A9mon-…` → 200 → candidate. With a lone 0xE9 byte instead, keep-alive returns status=None in ALL modes (gate red / probe-unreliable skip) where urllib follows to 200 or reports the accepted 301.

**Preuve.** Local http.server experiment (scratchpad/critic/exp_location_nonascii2.py, `_allkeyshop_host` monkeypatched, 127.0.0.1 only): `/start-latin1 keep-alive -> URLError: <urlopen error 'utf-8' codec can't decode byte 0xe9 in position 13…>; server saw ['/start-latin1']` vs `urllib -> status 200; server saw ['/start-latin1', '/blog/buy-pok%E9mon-cd-key-compare-prices/']`; `/start-utf8 keep-alive -> status 200; server saw […, '/blog/buy-pok%C3%83%C2%A9mon-cd-key-compare-prices/']` vs `urllib -> […, '/blog/buy-pok%C3%A9mon-cd-key-compare-prices/']`. Chained traceback (exp_tb.py): `src/aks_env.py:393 _http_open_keepalive: resp = _SESSION.request(…)` → `requests/sessions.py:589 request` → `sessions.py:739 send: r._next = next(` → `sessions.py:175 resolve_redirects: url = self.get_redirect_target(resp)` → `sessions.py:124 get_redirect_target: return to_native_string(location, "utf8")` → `_internal_utils.py:33 to_native_string: out = string.decode(encoding)`. Code: src/aks_env.py:395-398 uses the raw header value; no non-ASCII Location test exists in tests/test_aks_env.py KeepAliveBackendTests.

**Piste (non appliquée).** In the host-locked loop mirror urllib: take the raw value, `location.encode('latin-1')`, then quote it the way `HTTPRedirectHandler.http_error_302` does (`quote(newurl, encoding='iso-8859-1', safe=string.punctuation)`) before `urljoin`; add a KeepAliveBackendTests case with a UTF-8 and a latin-1 Location asserting the hop-2 URL passed to `sess.request`. Optionally catch `UnicodeDecodeError` explicitly so the error string names the cause.

**Votes.**
- reproduce : confirmé (c=0.92, sév. minor, dir. CLOSED) — Independent repro (scratchpad/verify/c0-reproduce/repro.py, 127.0.0.1 http.server, raw Location bytes, `_allkeyshop_host` patched, requests 2.32.3 / Py 3.13.5) driving the REAL `src.aks_env.http_get` in all three modes, keep-alive vs urllib backend:

- Latin-1 byte (0xE9) in Location: keep-alive -> status=None ok=False err="<urlopen error 'utf-8' codec can't decode byte 0xe9 in position 13" in nofollow, staff/host_locked AND plain-follow; urllib -> 301 ok=True (nofollow) / 200 ok=True (follow, hop 2 requested as `/blog/buy-pok%E9mon-…`). Confirms requests prepares the next hop even with allow_redirects=False (sessions.py send -> resolve_redirects -> get_redirect_target -> to_native_string(…,'utf8')), and the blanket `except Exception` at src/aks_env.py:411-416 maps it to URLError.
- UTF-8 bytes (C3 A9) in Location: keep-alive host_locked (src/aks_env.py:390-404, raw `resp.headers.get("Lo
- reread : confirmé (c=0.85, sév. nit, dir. CLOSED) — Independently reproduced at HEAD with a local ThreadingHTTPServer on 127.0.0.1 (scratchpad/verify/c0-reread/exp2.py, `_allkeyshop_host` monkeypatched), calling `_http_open_keepalive` and `_http_open_urllib` directly from src/aks_env.py:377-458. Results: (1) Location with lone 0xE9 byte: keep-alive → `URLError: 'utf-8' codec can't decode byte 0xe9 in position 13` [cause UnicodeDecodeError @ requests/_internal_utils.py:33 to_native_string] in ALL three modes (host-locked follow, no-follow, plain follow) — requests prepares `_next` even with allow_redirects=False; urllib → status 200 (followed to `/blog/buy-pok%E9mon-...`) on follow modes and HTTPError 301 on no-follow. (2) Location with UTF-8 bytes C3 A9: host-locked keep-alive loop (aks_env.py:395-407, `urljoin(current, resp.headers.get("Location"))` with no latin-1 re-quote) requests hop 2 as `/blog/buy-pok%C3%83%C2%A9mon-cd-key-compare-
- materiality : RÉFUTÉ (c=0.85, sév. nit, dir. CLOSED) — Accepting the finding as technically true, it is immaterial here. (1) Reachability: every URL the host-locked keep-alive loop probes is ASCII by construction — src/matcher.py:1136-1137 builds slug candidates with re.sub(r"[^a-z0-9]+", "-", ...), and the R30 search-fallback regex at src/matcher.py:1380 only admits `[a-z0-9-]` slugs; the invariant gate URL is the fixed ASCII https://www.allkeyshop.com/blog/. AKS/WordPress product permalinks are ASCII slugs, so a non-ASCII Location header on any probed URL is a hypothetical the critic itself rates as low realism. (2) Fail direction: both divergences land CLOSED. Case 1 (lone latin-1 byte) → blanket except at src/aks_env.py:414-419 → URLError → http_get status None → src/matcher.py:1419 raises AksProbeUnreliable → src/matcher.py:1948 SkippedOffer; in the gate it is RED, not GREEN; in scripts/11 (line 128) it is 'transient' retry then abort. 


## 2. Indécis / en attente de vérification (0)

Findings dont la majorité n'est pas encore acquise (vérificateurs coupés par la limite de session ou en cours). Le constat et le scénario sont ceux du chasseur, NON vérifiés.

## 3. Findings réfutés (7)

### [minor/NEUTRAL] Runbook 'pip install -r requirements.txt' will not land on the interpreter the VPS actually uses (Debian 12 PEP 668 + /usr/bin/python3 unit), and nothing logs which backend is active
`ops/BROWSER_RUNBOOK.md:361` — lentille(s) : ops · origine : prior-run-2026-09-08

**Constat.** ops/aks-admin.service:9 runs `/usr/bin/python3` and every child stage uses `sys.executable` (src/admin/submit_manager.py:257, scripts/10:68). Debian ≥12 marks the system interpreter EXTERNALLY-MANAGED, so a bare `pip install -r requirements.txt` errors ('externally-managed-environment'); a venv would be ignored by the unit. Because the fallback to urllib is silent and no script prints which _http_open backend is in use, the operator cannot tell that the advertised ~1.85× did not land. docs/HANDOFF.md:37-38 repeats the same step.

**Scénario.** Fresh Debian 12 VPS, operator follows §3 verbatim → pip refuses (or installs into a venv the unit never uses) → all sweeps run on urllib at the old per-request cost with the new 0.15 s pacing, and nobody notices.

**Votes.**
- reproduce : RÉFUTÉ (c=0.7, sév. nit, dir. NEUTRAL) — The harm scenario (keep-alive silently absent on the VPS because `pip install -r requirements.txt` fails under PEP 668) does not reproduce on the documented target. ops/INSTALL_ADMIN.md:28-36 (runbook §3 step 7, BROWSER_RUNBOOK.md:412-414) uses the Debian `certbot` package (`certbot.timer`), and offline `apt-cache depends` shows certbot -> python3-certbot -> python3-requests (direct dep) and python3-acme -> python3-requests. So /usr/bin/python3 (ops/aks-admin.service:9; sys.executable at src/admin/submit_manager.py:257, scripts/10_data_entry_auto.py:68) already has requests (bookworm 2.28.1 >= 2.28) on the current VPS and on any rebuild following the runbook; src/aks_env.py:449 then selects the keep-alive backend regardless of the pip step. _http_open_keepalive only uses stable requests APIs (Session.request/max_redirects, cookies.set_policy, status_code/headers/content/close) that exist
- reread : confirmé (c=0.9, sév. minor, dir. NEUTRAL) — Confirmed at HEAD. (1) ops/BROWSER_RUNBOOK.md:362 and docs/HANDOFF.md:37 both instruct a bare `pip install -r requirements.txt`; both lines are additions in the reviewed commits (combined.diff lines 144, 302), and `git show 184b2b8~1:src/aks_env.py` has 0 mentions of requests / no requirements.txt, so the issue is introduced here, not pre-existing. (2) ops/aks-admin.service:9 runs `/usr/bin/python3`; children inherit it via sys.executable at src/admin/submit_manager.py:257 and scripts/10_data_entry_auto.py:68, so a venv install would be ignored by the unit. (3) On this Debian box: /usr/lib/python3.13/EXTERNALLY-MANAGED is present (standard refusal text pointing to apt/venv), there is no pip/pip3 binary and `python3 -m pip` prints "No module named pip"; requests 2.32.3 is apt's python3-requests. The documented step therefore does not work on the system interpreter as written (no pip, or P
- materiality : RÉFUTÉ (c=0.72, sév. nit, dir. NEUTRAL) — Technically accurate but immaterial for this project's safety model. (1) Fail direction is strictly NEUTRAL: src/aks_env.py:449 picks `_http_open_keepalive` only when `_SESSION is not None`, and both backends were deliberately aligned to the same contract (no-redirect precedes host_locked at :382-388, max_redirects=10 at :342, cookie policy denies storage at :336). Running on urllib cannot unlock a write, submit a wrong offer, or turn a bad invariant state green — it only forfeits the ~1.85x probe speed-up. The invariant gate (scripts/01_check_invariants.py:72, staff UA + follow_redirects=False) is explicitly documented as stdlib-only (requirements.txt header; aks_env.py:5-7, 324-328). (2) The runbook step fails LOUDLY, not silently: on this Debian box `python3 -m pip install --dry-run -r requirements.txt` prints `/usr/bin/python3: No module named pip`; on a bookworm box with pip it prin


### [minor/OPEN] scripts/10 recap derives created_ok from a post_save substring ('gone' in ps.lower()) whereas scripts/12 uses 05_submit's boolean 'submitted' (pre-existing, outside the 5 commits)
`scripts/10_data_entry_auto.py:172` — lentille(s) : handoff-cmds · origine : lead-aside-preexisting

**Constat.** The safe-auto recap counts an offer as created when the post_save string contains 'gone' (substring, case-insensitive) instead of using the deterministic `submitted` boolean that 05_submit sets only from the prove-gone check — exactly the pattern scripts/12 says it avoids ('never re-derived here from a post_save substring (EXECUTOR_RULES)', scripts/12:47-50). A post_save text such as 'NOT gone from feed' / 'still present, not gone' would count as created. Whether a real 05_submit post_save string can contain 'gone' while submitted=False must be checked in src/submitter.py (post_save wording).

**Scénario.** 05_submit records post_save='offer NOT gone from refreshed feed' for a failed submit → scripts/10 recap shows created+1 for that offer; Romain's post-run audit of recap.json trusts a wrong 'created' count (the submit result itself / submit_plan.json is unchanged).

**Votes.**
- reproduce : RÉFUTÉ (c=0.6, sév. nit, dir. OPEN) — The cited code is real: scripts/10_data_entry_auto.py:171-172 does `created_ok = "gone" in ps.lower()` while scripts/12_data_entry_by_urls_submit.py:47-70 uses `submitted is True`. But the finding's concrete scenario cannot happen at HEAD: 05_submit never writes 'offer NOT gone from refreshed feed'. src/submitter.py only assigns post_save in three places — 1512-1515: `"gone from feed (available=…)" if gone else "STILL in feed (available=…) — FAILED"` (the failed wording contains no 'gone'); 1489: `"create not confirmed: {status} — {reason}"`; 1338-1341: `"feed/CDP unreadable — … : {exc}"`. `entry["submitted"] = gone` (1510) and the 'gone from feed' string are set from the same boolean, so on the prove-gone path the substring and the boolean agree exactly. Scratch experiment (/tmp/…/verify/f20-reproduce/repro.py, monkeypatched _run_child + hand-written submit_plan.json) confirms: 'STILL i
- reread : confirmé (c=0.7, sév. minor, dir. OPEN) — Claim is accurate as a code description and is genuinely pre-existing (outside the 5 commits). scripts/10_data_entry_auto.py:171-172 at HEAD: `ps = str(e.get("post_save") or ""); created_ok = "gone" in ps.lower()`; identical in `git show 184b2b8~1:scripts/10_data_entry_auto.py` (line 172) — the only 184b2b8 change to that file is the `--max-pages` default/help (lines 292-303). scripts/12_data_entry_by_urls_submit.py:63-66 does `ok = bool(e.get("submitted"))` with an explicit "[13] Fable re-audit 2026-09-06" comment stating the substring pattern is untrustworthy; that audit fix was never ported to scripts/10. Both scripts read the same 05_submit `submit_plan.json` (scripts/10 invokes 05 with `--submit`, line 145-147, so `submitted`/`post_save` are populated). Materiality check: I enumerated every `post_save` writer in src/submitter.py — :1338 ("feed/CDP unreadable … : {ExcName}: {exc}"), 
- materiality : RÉFUTÉ (c=0.8, sév. nit, dir. NEUTRAL) — Technically true but immaterial here, and pre-existing (git log -S: introduced 3a5110d, 2026-08-04, safe-auto sweep — not in the 5 commits under review). (1) No write or continuation decision consumes the substring-derived count: SubmitOutcome.clean() (src/data_entry_auto.py:87-95) uses only ok/aborted/stopped; `created`/`offers` flow solely into recap.json / stdout (src/data_entry_auto.py:172,247-248,413-416). (2) Every authoritative created-set path uses 05_submit's `submitted` boolean, not the substring: submit_manager.py:206 (`entry.get("submitted") and entry.get("post_save") and entry.get("offer_id")` → sticky 'created' history / already_created gate), scripts/05_submit.py:126,509,538. So the phantom-creation / already_created protections are unaffected. (3) The misfire is not reachable with the code's own wording: src/submitter.py:1512-1515 emits 'gone from feed (available=…)' only


### [minor/CLOSED] [adds to lead (4)] No whitespace collapse after the ' ' substitution: a symbol inside a multi-word platform collocation is still not rescued, and cleaned_title emits double-space / ' :' strings used as the R30 site-search query and the scripts/11 admin name-search term
`src/matcher.py:502` — lentille(s) : matcher · origine : finder:matcher

**Constat.** `_NFKC_LETTER_SYMBOL_RE.sub(" ", text)` leaves two spaces where a symbol sat between words, so (a) the single-space regexes in explicit_platform (`\bEA (?:APP|PLAY|ORIGIN)\b`, `"MICROSOFT STORE" in t`) and the single-space phrases in _TRAILING_NOISE_PHRASES ("UBISOFT CONNECT", "EPIC GAMES STORE") still miss when a ®/™ splits the collocation, and (b) cleaned_title's text — the R30 WP search query (matcher.py:1367 `quote(query)`) and scripts/11's `search[search]` name term (11_data_entry_by_urls.py:231, also written to `meta.name_term`) — now carries '  ' and ' :' artefacts. Not a regression vs. old (® was never stripped, so those collocations were already broken), but the commit's stated intent ('a glued symbol still splits') is only met on the token path; the collocation path stays broken.

**Scénario.** Merchant title "Assassin's Creed Mirage Ubisoft® Connect Key" → cleaned_title "Assassin's Creed Mirage Ubisoft  Connect" (UBISOFT CONNECT not stripped) → tier-1 slug 'assassins-creed-mirage-ubisoft-connect' probed (404) instead of 'assassins-creed-mirage'. "EA SPORTS FC 25 EA® App Key" → explicit_platform None (falls to the default-STEAM guess path). scripts/11: if an AKS og:title carried ® (e.g. "Tom Clancy's Rainbow Six® Siege"; no fixture in tests/ has one, unverifiable offline) the admin name term becomes "Tom Clancy's Rainbow Six  Siege" (double space) — before the change it was "…Six® Siege", which at least matched feed rows carrying the ® verbatim. Fewer rows found → smaller preview / missed offers, never a wrong write → CLOSED.

**Votes.**
- reproduce : confirmé (c=0.92, sév. minor, dir. CLOSED) — Reproduced with the real HEAD src/matcher.py (loaded alongside 184b2b8^ for old/new comparison). src/matcher.py:502 `_NFKC_LETTER_SYMBOL_RE.sub(" ", text)` has no whitespace collapse, and the single-space consumers still miss: cleaned_title("Assassin's Creed Mirage Ubisoft® Connect Key") -> "Assassin's Creed Mirage Ubisoft  Connect" (UBISOFT CONNECT not stripped; slugs ['assassins-creed-mirage-ubisoft-connect', ...] vs the plain-title control giving ['assassins-creed-mirage', ...]); explicit_platform("EA SPORTS FC 25 EA® App Key") -> None old and new while the plain "EA App Key" -> 'EA' (regex at :755); "Some Game Microsoft® Store Key" -> None/None (:760); quote(cleaned_title("Company™: The Game Steam Key GLOBAL")) -> 'Company%20%3A%20The%20Game' (old 'CompanyTM%3A%20The%20Game'); "Tom Clancy's Rainbow Six® Siege" -> "Tom Clancy's Rainbow Six  Siege". tokenize is unaffected, so the commi
- reread : RÉFUTÉ (c=0.8, sév. nit, dir. CLOSED) — Re-read src/matcher.py at HEAD (normalize_apostrophes L484-503, explicit_platform L731-760, _strip_trailing_phrases L1082-1094, cleaned_title L1097-1108, search_aks_slugs L1367) vs `git show 184b2b8~1:src/matcher.py`, and reran the old-vs-new experiment. The finding's raw facts reproduce, but neither half is a material finding against these commits. (a) Collocation path: for "…Ubisoft® Connect Key" old and new both give explicit_platform=UBISOFT and the IDENTICAL slug list ['assassins-creed-mirage-ubisoft-connect', …]; "EA SPORTS FC 25 EA® App Key" → None in BOTH; "Microsoft® Store" → None in BOTH. The old ® glyph blocked the single-space regexes exactly as the new double space does, so there is zero behavior delta — this is pre-existing and the finding itself concedes "Not a regression". The "stated intent not met" framing misreads the commit: 184b2b8's message says "tokenize now strips
- materiality : RÉFUTÉ (c=0.75, sév. nit, dir. CLOSED) — Technically true but immaterial for this review's gate. (1) No write path is touched: `cleaned_title`/`normalize_apostrophes` are consumed only by src/matcher.py (slug guessing at :1097-1130, R30 site search at :1367) and scripts/11_data_entry_by_urls.py:231 (read-only stage-1 preview, header line 12: "The submit half is a separate, explicitly-gated step"). `grep cleaned_title src/submitter.py` is empty — the post-save prove-gone walk (submitter.py:43-95) never sees these strings, so no false "disappeared" success is possible. (2) Not a regression: ® has no NFKC decomposition, so old and new both fail the collocation regexes identically (experiment: explicit_platform("EA SPORTS FC 25 EA® App Key") None/None; "Some Game Microsoft® Store Key" None/None); ™ went from a glued "COMPANYTM" (broke R01 identity — the actual bug fixed) to "Company : The Game", strictly better for WP search which 


### [minor/CLOSED] Remaining glue cases the new class does not cover: superscript modifier-letter 'ᵀᴹ' (U+1D40 U+1D39 → 'TM') and circled Ⓡ/ⓒ (U+24C7/U+24D2 → 'R'/'c') still NFKC-glue onto the word — same R01 false-skip class as the Eneba bug
`src/matcher.py:481` — lentille(s) : matcher · origine : finder:matcher

**Constat.** A unicodedata scan (all codepoints < U+30000) for NFKC results that are ASCII letters shows the only plausible-in-merchant-title glue sources outside the new class are the superscript 'ᵀᴹ' pair (the common way storefronts/social copy render a raised TM without U+2122), the modifier 'ᴿ' (U+1D3F → 'R'), and the circled Ⓡ / Ⓒ / ⓒ (used as ®/© on CJK-locale pages). Everything else that decomposes to 2+ ASCII letters is either desired (ﬁ/ﬂ ligatures, Ǆ/Ǳ digraphs, full-width letters, Roman numerals — kept on purpose) or implausible (₨, ㉐ PTE, ㋏ LTD, 🅪 MC, 🄭 CD). Pre-existing gap, not a regression, but the fix is incomplete for the exact failure class it targets.

**Scénario.** Merchant title "Gameᵀᴹ Steam Key GLOBAL" vs AKS name "Game" → tokenize → ['GAMETM', …] → missing_aks_words ['GAME'] → SKIP 'name mismatch, missing AKS words' (and slug 'gametm' probed → 404 → search fallback 'GameTM'). "GameⓇ Deluxe Edition Steam Key" → ['GAMER', …] → same false skip. Fail-closed (offer not entered), never a wrong write.

**Votes.**
- reproduce : confirmé (c=0.8, sév. minor, dir. CLOSED) — Reproduced with the real HEAD functions (scratch script calling src.matcher.tokenize / missing_aks_words / build_slug_candidates): 'Gameᵀᴹ Steam Key GLOBAL' -> tokens ['GAMETM','STEAM','KEY','GLOBAL'], missing_aks_words('Game', …) == ['GAME'], slug candidates ['gametm']; 'GameⓇ Deluxe Edition Steam Key' -> ['GAMER','DELUXE','EDITION','STEAM','KEY'], missing ['GAME'], slugs ['gamer-deluxe-edition','gamer']. By contrast 'Game™ Steam Key GLOBAL' and 'Company® Halo' are correctly handled by the new _NFKC_LETTER_SYMBOL_RE class at src/matcher.py:481-482 ("[©®℀℁℅℆№℗℠℡™]"), which does not include U+1D40/U+1D39/U+1D3F modifier letters or the U+24B6-24E9 circled letters. unicodedata confirms NFKC of ᵀ ᴹ ᴿ Ⓡ ⓒ is 'T' 'M' 'R' 'R' 'c'. So the exact failure class the commit targets (NFKC letter-glue of a trademark marker onto the game name -> R01 false skip / wrong slug) still occurs for these codepo
- reread : RÉFUTÉ (c=0.6, sév. nit, dir. CLOSED) — The technical observation is accurate but the finding is not material to, nor affected by, the commits under review. Re-read at HEAD: src/matcher.py:478-479 defines `_NFKC_LETTER_SYMBOL_RE = "[©®℀℁℅℆№℗℠℡™]"`, applied at :502 before NFKC in normalize_apostrophes; tokenize (:508-512) is otherwise unchanged from 184b2b8~1 (old lines 488-492: plain NFKC then `[A-Z0-9']+`). Experiment (inlined old tokenize vs `src.matcher.tokenize` at HEAD): 'Gameᵀᴹ Steam Key GLOBAL' → old ['GAMETM',…] / new ['GAMETM',…]; 'GameⓇ Deluxe…' → old/new ['GAMER',…]; 'Gameⓒ…' → ['GAMEC',…]; 'Gameᴿ…' → ['GAMER',…]; in every case missing_aks_words('Game', title) == ['GAME'] both before and after, i.e. behavior is byte-identical pre/post commit — nothing introduced or regressed. The only R01 consumer is :1983 (`missing = missing_aks_words(...)` → SkippedOffer with an explicit 'name mismatch, missing AKS words' reason),
- materiality : RÉFUTÉ (c=0.85, sév. nit, dir. CLOSED) — Technically reproduced: with HEAD's src/matcher.py:480-481 (`_NFKC_LETTER_SYMBOL_RE = "[©®℀℁℅℆№℗℠℡™]"`), tokenize('Gameᵀᴹ Steam Key GLOBAL') -> ['GAMETM', ...] and tokenize('GameⓇ Deluxe Edition Steam Key') -> ['GAMER', ...], while 'Game™ ...' -> ['GAME', ...]. But it is immaterial for this project: (1) Fail direction is strictly CLOSED — the only outcome is an R01 'missing AKS words' SKIP that lands in the report with its reason; no write, no wrong offer, no green-looking bad state, no operator misdirection toward a write. (2) It is a pre-existing gap, not a regression introduced by 184b2b8; the commit under review fixed the one glue source actually observed in production (™ in an Eneba title). The finding itself presents ᵀᴹ/Ⓡ/ⓒ as 'plausible', not observed — no run/feed corpus in this checkout contains any U+1D00–1D7F or U+24B6–24E9 codepoint (search over runs/ and logs/ returned nothi


### [nit/CLOSED] № (U+2116) is now dropped rather than read as 'No' — a title using № where the AKS name spells 'No.' flips from match to R01 false-skip
`src/matcher.py:481` — lentille(s) : matcher · origine : finder:matcher

**Constat.** Unlike ™/℠/©/®, № carries lexical meaning ("No."): before the change tokenize('Game № 5') → ['GAME','NO','5'] which matched an AKS name 'Game No. 5' (tokens GAME/NO/5); now → ['GAME','5'] → missing_aks_words ['NO'] → SKIP. The 'glued' form '№5' was broken before too (→ 'NO5'), so for that spelling the change is neutral-to-better (slug 'game-5' vs 'game-no5'). Plausibility is low (few English game names use №), so nit.

**Scénario.** Merchant 'Game № 5 Steam Key', AKS page name 'Game No. 5': old missing_aks_words == [] (candidate); new == ['NO'] → 'name mismatch, missing AKS words: [\'NO\']' — fail-closed skip, not a wrong write.

**Votes.**
- reproduce : confirmé (c=0.92, sév. nit, dir. CLOSED) — Reproduced with a scratch script calling the real functions from src/matcher.py at HEAD (/tmp/claude-0/-root-aks-code-executor/fb12d4f4-2a87-4577-a2d8-a5d07afbcaa7/scratchpad/verify/f31-reproduce/repro.py): tokenize('Game № 5 Steam Key') → ['GAME','5','STEAM','KEY']; tokenize('Game No. 5') → ['GAME','NO','5']; missing_aks_words('Game No. 5', 'Game № 5 Steam Key') → ['NO'] (R01 skip); build_slug_candidates → ['game-5']. Simulating the pre-change path (pure NFKC, no symbol strip) gives ['GAME','NO','5','STEAM','KEY'] → missing [] (match), and the glued '№5' → 'NO5' as the finding says. Cause: src/matcher.py:480-481 `_NFKC_LETTER_SYMBOL_RE = re.compile("[©®℀℁℅℆№℗℠℡™]")` applied at :502 in normalize_apostrophes, replacing № with a space before NFKC. Note the № entry was added in 54f1f88 (the 184b2b8 tokenize strip was only [™℠®©℗]), and 54f1f88 also added tests/test_matcher.py:138 `assertEqu
- reread : RÉFUTÉ (c=0.65, sév. nit, dir. CLOSED) — The mechanical claim is accurate: src/matcher.py:480-481 `_NFKC_LETTER_SYMBOL_RE = re.compile("[©®℀℁℅℆№℗℠℡™]")` is applied with `.sub(" ", text)` at :503 before NFKC, so № no longer decomposes to "No". Experiment (HEAD vs `git show 184b2b8~1:src/matcher.py`): tokenize('Game № 5 Steam Key') old=['GAME','NO','5','STEAM','KEY'] / missing_aks_words vs 'Game No. 5' = []; new=['GAME','5','STEAM','KEY'] / missing=['NO'] → R01 skip. Glued 'Game №5' old → 'NO5' (missing ['NO','5'], already a skip), new missing ['NO'] — neutral. However this is not an oversight introduced silently: it is a deliberate, tested, documented decision of commit 54f1f88 — tests/test_matcher.py:135-138 explicitly says "№ (→"No") … likewise" and asserts `tokenize("Game № 5") == ["GAME", "5"]`; src/matcher.py:474 lists '"№"→"No"' by name in the rationale comment; docs/CHANGELOG.md:21 records the widened class `™®©℠℗℡№℅℀℁℆`.
- materiality : RÉFUTÉ (c=0.9, sév. nit, dir. CLOSED) — Technically true but immaterial. Verified: tokenize('Game № 5 Steam Key') -> ['GAME','5','STEAM','KEY'] vs AKS 'Game No. 5' -> ['GAME','NO','5'], so a spaced-№ title would R01-skip with "missing AKS words: ['NO']". But (1) this is a deliberate, tested choice — tests/test_matcher.py:138 explicitly asserts tokenize("Game № 5") == ["GAME","5"], and src/matcher.py:475-481 documents № in the letter-symbol class; docs/CHANGELOG.md:21 records the widened set. (2) The only outcome is a fail-closed SKIP with an explicit reason string — no wrong page probed, no wrong offer submitted, no green state faked. (3) Reachability is negligible: № appears in zero run/log files in this repo, and the pre-change behaviour for the more common glued spelling '№5' was already broken ('NO5' token, slug 'game-no5'), so the change is neutral-to-better there. (4) Restoring '№'->' No ' would be a re-tighten of a just


### [nit/NEUTRAL] "Single, patchable IO seam" docstring is stale — tests now also patch `_SESSION` and call the backend directly; "Sprint 1 tooling" wording retained
`src/aks_env.py:441` — lentille(s) : tests · origine : finder:tests

**Constat.** The module now has two test seams (`_http_open` and the module global `_SESSION`), while the docstring still promises one; the reworded module docstring keeps the pre-existing "used by Sprint 1 tooling" phrase although the probes now back 03_match/05_submit-era stages.

**Scénario.** Reader/auditor relying on the docstring assumes patching `_http_open` isolates all HTTP; a future test that patches only `_http_open` while exercising `_http_open_keepalive` helpers directly (as KeepAliveBackendTests do) reaches the real Session.

**Votes.**
- reproduce : RÉFUTÉ (c=0.85, sév. nit, dir. NEUTRAL) — The docstring at src/aks_env.py:441 ("Single, patchable IO seam for all read-only HTTP in this module") is still accurate at HEAD: every production HTTP call in the module goes through `_http_open` — `http_get` (src/aks_env.py:478) and `http_head_status` (src/aks_env.py:535) are the only call sites, and the backends `_http_open_keepalive`/`_http_open_urllib` are only invoked from `_http_open` (line 449). Nothing in src/ or scripts/ references `_SESSION` or the backends directly (grep over src, scripts, tests: only tests/test_aks_env.py:325/333 do). So patching `_http_open` does isolate all HTTP for any caller of the module's public API; the claimed scenario ("a test that patches only `_http_open` while calling `_http_open_keepalive` directly reaches the real Session") is a self-inflicted contradiction (deliberately calling a backend beneath the facade), not a consequence of the docstring
- reread : RÉFUTÉ (c=0.85, sév. nit, dir. NEUTRAL) — Re-read at HEAD: `src/aks_env.py:439-450` `_http_open` remains the only path every production HTTP call in the module takes — `http_get` at :478 and the second probe at :535 both call `_http_open(...)`; the only other network call sites (`_SESSION.request` at :388/:393/:411, `build_opener`/`urlopen` at :433-436) live inside the two backends that are reached exclusively via `_http_open`'s dispatch at :449. So "Single, patchable IO seam for all read-only HTTP in this module" is still literally true for any caller of the public API: patching `_http_open` isolates all HTTP, exactly as the 14 existing `mock.patch("src.aks_env._http_open", ...)` tests (tests/test_aks_env.py:208-297, :535) rely on. `_SESSION` is not a second seam; it is a backend dependency that `KeepAliveBackendTests` (tests/test_aks_env.py:317-334) patches precisely because they deliberately unit-test `_http_open_keepalive` B
- materiality : RÉFUTÉ (c=0.92, sév. nit, dir. NEUTRAL) — Technically true but immaterial. src/aks_env.py:441-450: `_http_open` is still the single seam every production caller (http_get -> invariant gate / resolve probes) passes through; patching it does isolate all HTTP for callers. tests/test_aks_env.py:325,333 patch `_SESSION` only because they import the private backend `_http_open_keepalive` directly (white-box test below the seam), which the docstring never claims to cover. The scenario is a hypothetical future test-hygiene slip with zero production reach: cannot unlock writes, cannot flip authoritative state, cannot mislead an operator (entry point is docs/HANDOFF.md, not this docstring). "Sprint 1 tooling" at src/aks_env.py:4 is pre-existing wording (combined.diff shows it on both the -/+ lines) and the same label persists in src/cdp_client.py:1,108,110 — purely stylistic, no reader impact. No Romain-reviewed decision is touched. Pure 


### [nit/CLOSED] [different scenario from known chain/loop finding] Non-HTTP-scheme `Location` (e.g. `javascript:`): urllib -> ok=True/302, keep-alive -> ok=False
`src/aks_env.py:399` — lentille(s) : tests · origine : finder:tests

**Constat.** Another input where the two backends of the seam disagree: urllib's HTTPRedirectHandler refuses a non-http(s)/ftp redirect with `HTTPError(302, '... Redirection to url ... is not allowed')` (302 is in ACCEPTED_AKS_STATUSES -> ok=True), whereas the keep-alive path yields `URLError` (plain: requests InvalidSchema) or `StaffUaRedirectRefused` (staff: hostname '' fails `_allkeyshop_host`) -> ok=False. Also on a same-URL loop urllib gives up after 5 requests (`max_repeats=4`) while keep-alive issues 11.

**Scénario.** AKS answers a follow-mode probe with `Location: javascript:...`/`data:` (pathological; never observed): urllib backend reports the resolve as ok (302), keep-alive as a miss — the fail-closed direction, so no wrong write, but the two boxes disagree.

**Votes.**
- reproduce : confirmé (c=0.95, sév. nit, dir. CLOSED) — Reproduced concretely at HEAD (e7586f6) with a scripted 127.0.0.1 server and the real `src.aks_env.http_get`, swapping `_SESSION` between the requests Session and None (urllib fallback), with `_allkeyshop_host` extended to accept 127.0.0.1 so the staff/host_locked branch runs locally (redirect targets `javascript:`/`data:` still have hostname '' → not allkeyshop). Output:
- `302 Location: javascript:alert(1)`, plain follow: keepalive ok=False status=None err="No connection adapters were found for 'javascript:alert(1)'" (requests InvalidSchema → URLError at src/aks_env.py:415-419) vs urllib ok=True status=302 err="Redirection to url 'javascript:alert(1)' is not allowed" (plain HTTPError(302), 302 ∈ ACCEPTED_AKS_STATUSES line 129 → ok=True at line 488).
- same, staff UA: keepalive ok=False status=302 "AKS/Staff redirect off allkeyshop.com refused" (StaffUaRedirectRefused raised at src/aks_
- reread : RÉFUTÉ (c=0.8, sév. nit, dir. CLOSED) — Re-read src/aks_env.py at HEAD (keep-alive backend lines ~361-420, http_get lines ~455-500) and reproduced the finding's matrix against a local 127.0.0.1 server (scratchpad verify/f41-reread/exp.py). The raw seam observation is accurate: for a `javascript:`/`data:` Location in follow mode, urllib yields ok=True/status=302 (HTTPError raised by urllib's own scheme check, 302 in ACCEPTED_AKS_STATUSES) while keep-alive yields ok=False (URLError from requests InvalidSchema, or StaffUaRedirectRefused for staff UA). However the finding's material claim — "urllib backend reports the resolve as ok (302)" — misreads the consumers: every follow-mode caller of http_get gates on `probe.ok and probe.status == 200 and probe.body` (src/matcher.py:1372, 1417, 1441; src/merchants/difmark.py:171,181; src/merchants/instant_gaming.py:140), so ok=True/302 is rejected exactly like ok=False; in resolve_aks both
- materiality : RÉFUTÉ (c=0.92, sév. nit, dir. CLOSED) — Immaterial. (1) The invariant gate runs http_get with follow_redirects=False (src/invariants.py:66-67); in that mode neither backend inspects Location — local experiment with a server answering 302 `Location: javascript:alert(1)` gives identical results on both backends (keepalive: ok=True/302; urllib: ok=True/302; same for http_head_status). So the gate cannot flip. (2) In follow mode the backends disagree only on the `ok` flag (urllib ok=True/302 vs keepalive ok=False/None or StaffUaRedirectRefused 302), but every production consumer additionally requires `status == 200` and a body: src/matcher.py:1372 (search), :1417 (guessed slug -> raises AksProbeUnreliable for any non-404/410, i.e. for 302 AND for None), :1441 (fallback continue); src/merchants/instant_gaming.py:139 and src/merchants/difmark.py:171,:181 raise *PageUnreadable. Both backends therefore produce the same fail-closed out


## 4. Points vérifiés CORRECTS par les chasseurs (109)

- [parity] No-redirect mode is identical on both backends for every scenario (200, 204, 404, 500, 301/302/303/307/308 same-domain, relative '/x', lowercase 'location', 3xx without Location, off-domain 302/307, chains x9/x10/x11, A<->B loop, Set-Cookie 302, dropped connection, read timeout) for GET plain UA, GET staff UA (invariants gate src/invariants.py:66-69 and scripts/03_match.py:72) and HEAD: the 54f1f88 reorder holds — a same-domain 3xx surfaces as HTTPError(3xx) with exactly 1 request, ok per ACCEPTED_AKS_STATUSES, never followed.
- [parity] Host-locked staff path refuses an off-domain 302/307 identically on both backends: StaffUaRedirectRefused(3xx), ok=False, status=3xx, exactly 1 request, Location header preserved in probe.headers; the local server saw exactly ONE User-Agent header = 'AKS/Staff' under both backends (urllib Request 'User-agent' key is merged case-insensitively by requests, no python-requests UA leaks).
- [parity] Host-locked same-domain redirects (301/302/303/307/308 -> 200, relative Location, lowercase 'location' from a CDN-style server, 3xx without Location, chains up to 9 hops) match urllib exactly (ok, status, body, request count). Location lookup is case-insensitive on both (requests CaseInsensitiveDict, urllib HTTPMessage).
- [parity] Non-host-locked follow (cdp_client plain UA, difmark, instant_gaming) and HEAD follow: `_SESSION.max_redirects = 10` matches urllib's max_redirections=10 exactly — both follow a 10-hop chain to the 200 (11 requests) and both refuse the 11th hop; only the reported status differs (finding 2).
- [parity] Transport failure (server drops the connection) and read timeout map to ok=False/status=None on both backends; requests exceptions are wrapped to URLError, and even an unwrapped one would be caught because `requests.exceptions.RequestException` subclasses OSError (MRO verified), so http_get/http_head_status keep their never-raises contract. Body of a non-2xx is readable via HTTPError.read() on both (body 'nope'/'boom' identical).
- [parity] 204 handling: both backends return ok=True, status=204, empty body; 404/500 identical (ok=False, status, body, error text).
- [parity] Session cookie statelessness ACROSS calls holds (DefaultCookiePolicy(allowed_domains=[]) rejects every cookie): a /cookie/echo fetched later in the same process saw no cookie under keep-alive.
- [parity] Every production caller was checked for None-vs-3xx sensitivity: matcher resolve/search (src/matcher.py:1372, 1417-1419, 1441) require status==200 and raise/skip otherwise; difmark.py:171/181 and instant_gaming.py:140 require status==200; cdp_client.py:59 checks probe.ok then JSON-parses; invariants/03_match use validate_aks_direct_status(status) in no-follow mode. No caller can turn a backend divergence into a write unlock or a wrong submit; scripts/11:128 is the only retry-path difference (finding 2).
- [parity] HEAD method keeps HEAD across redirects on both backends (urllib redirect_request 'HEAD' if m == 'HEAD'; requests rebuild_method leaves HEAD); http_head_status never passes a staff UA so the host-locked+HEAD combo is unreachable in production.
- [parity] No request left the sandbox: the matrix and header checks used only a 127.0.0.1 http.server (with 'localhost' as the synthetic off-domain host); no allkeyshop.com URL appears in the experiment scripts.
- [parity] Test suite (python3 -m unittest discover -s tests -t ., with requests installed): still running at report time with 0 FAIL/ERROR lines so far; the '1320 green' claim could not be fully confirmed before the report deadline, and the lead's finding that 3 KeepAliveBackendTests ERROR without requests stands.
- [ops] 54f1f88 fail-open fix verified by local experiment (127.0.0.1 http.server, _allkeyshop_host patched): staff-UA + follow_redirects=False on same-domain 301/302/303/307/308 → keep-alive surfaces HTTPError(3xx) exactly like urllib (rows staff+nofollow: identical ok/status for all 15 paths); the invariant gate (src/invariants.py:66-69) and scripts/03_match.py:72 both use follow_redirects=False, so no spurious green.
- [ops] Off-domain staff-UA hop refused on both backends in host-locked follow mode (/off 302 and /off308 → StaffUaRedirectRefused, ok=False); the server never received a request on the off-domain host in staff mode, and the UA seen on the wire was 'AKS/Staff' (urllib's 'User-agent' header casing is merged correctly by requests).
- [ops] Keep-alive really reuses one TCP connection: 1 distinct client port over 5 sequential GETs vs 5 for urllib; requests sends Connection: keep-alive + Accept-Encoding: gzip, deflate (urllib: close/identity) — body content identical after decoding.
- [ops] 2xx/4xx/5xx/no-Location-3xx/HEAD parity between backends (identical ok, status, body prefix) for /ok, /404, /503, /noloc and http_head_status; _KeepAliveResponse exposes .read()/.status/.headers/.items() as _response_to_probe and http_get's HTTPError branch expect; http_get keeps its never-raises contract (StaffUaRedirectRefused re-raised, any other exception → URLError).
- [ops] Session cookies disabled (DefaultCookiePolicy(allowed_domains=[])) and _SESSION.max_redirects = 10 as claimed; test_session_matches_urllib_redirect_ceiling asserts the attribute.
- [ops] Resolution is still serial: no ThreadPool/asyncio/concurrent.futures anywhere in src/ or scripts/; the only threading.Thread uses are admin process supervision (src/admin/submit_manager.py:959, :1082).
- [ops] Pacing placement: time.sleep(AKS_PROBE_DELAY_S) runs BEFORE every guessed-slug probe (src/matcher.py:1414-1416) and every search-fallback probe (:1438-1440), gated on `http_get_fn is http_get`; scripts/03_match.py:82 passes resolve_aks unwrapped (no cache/wrapper), so the identity check holds and pacing is active in production. Unpaced AKS calls: the single site-search GET (:1371, 20 s timeout) and scripts/11 resolve_pinned. Sustained worst case ≈ 60/(0.15+0.032) ≈ 330 req/min (measured 255), previously ≈138; 8 s timeouts only lower it.
- [ops] --max-pages does NOT leak into the submit: scripts/10 _make_stages.submit builds 05_submit argv with --page-hint (+ --pace-pages/--pace-offers) only (scripts/10:146-158); 05_submit derive_max_pages auto = max(40, ceil(feed_last_page×1.3)) read from the page-run's offers.json, whose feed_last_page is the extractor's nav maximum (src/extractor.py:622), i.e. the whole feed; prove-gone uses ctx['max_pages'] in a whole-feed scan (src/submitter.py:1492-1502) and page_hint is a locate-only window (:1223-1237). Mover TARGET_SCAN_MAX_PAGES=2000 untouched; recap.json only records the sweep cap; admin /auto passes --max-pages only when the operator sets it (submit_manager.py:735-736), start_sort_scan keeps its own 800 default.
- [ops] --start-page > 1 interaction: top=min(feed_last, start+max_pages-1), walk top→start; the cap keeps the 30 pages from start_page upward and sets capped when feed_last > top (src/data_entry_auto.py:196-202, 283-289).
- [ops] Full suite on Python 3.13.5 with requests 2.32.3: `Ran 1322 tests in 358.9s — OK (skipped=2)`, exit 0 — HANDOFF's '1322 tests' is exact; 184b2b8 (1318) and 54f1f88 (1320) counts are consistent with the 2 tests 9ddf185 added.
- [ops] HANDOFF.md citations that exist and match the code/runbook: /etc/aks-executor.target + marker_authorizes (src/aks_env.py), aks-admin.service + nginx auth_basic (ops/), hermes-cdp-proxy.service, aks-chromium.service, Chromium 149 hold / 150 SIGTRAP, UA-Switcher policy, state/browser.lock, _url_key (submitter), precheck_skip, resolve_software_region, extract_aks_name, compare-and-buy, R31/R39/R40 (EXECUTOR_RULES:522, :637), EXECUTOR_RULES §6 = Submitter and §7 = Post-save verification, admin routes /executor/ /tri /auto /games (src/admin/app.py:234-238), scripts 11/12, docs/LOGIN_SPEC.md, ops/INSTALL_ADMIN.md, 01_check_invariants JSON keys ok/authoritative (src/invariants.py:91-94), pre-order = status (src/matcher.py:567-568), categorical skips (gift card/soundtrack/skin, matcher.py:197-200, EXECUTOR_RULES:214), BROWSER_RUNBOOK §3 step order (11 steps: Chromium→hold→UA-Switcher→aks-chromium→socat→firewall→nginx+admin→FC2 marker→invariants→cookies→tests) matches HANDOFF §2; R25-retired and finding-[7]-declined wording consistent with AGENTS.md 'Reviewed decisions' (not re-flagged).
- [ops] git: main == origin/main, 0 unpushed commits — HANDOFF's 'tout est poussé sur origin/main' holds.
- [ops] Rule docs impose no numeric AKS probe-rate policy: AGENTS.md/CLAUDE.md/EXECUTOR_RULES.md mention only the extractor's --pace 2-5 s page pacing (EXECUTOR_RULES:165, unchanged), so the 0.3→0.15 change contradicts no written rule (only the in-repo ban narrative, see finding).
- [security] (a) Staff-UA never reaches requests-managed redirect following: `host_locked=(user_agent == AKS_STAFF_UA)` (src/aks_env.py:479) routes every staff caller to the no-redirect branch (invariants.py:66-69, scripts/03_match.py:72) or the host-locked loop (matcher.py:1371/1416/1440 via AKS_PROBE_UA=AKS_STAFF_UA, scripts/11:123); the `else` (allow_redirects=True) branch is reached only by plain-UA callers — cdp_client.py:58 (172.17.0.1 proxy), merchants/instant_gaming.py:137 (REQUIRED_USER_AGENT), merchants/difmark.py:170/180 (default UA) and `http_head_status` (REQUIRED_USER_AGENT; no callers in src/ or scripts/). The parser-differential finding above is the only staff leak found.
- [security] (b) Cross-call cookie statelessness holds empirically: after a `Set-Cookie: sid=abc` on call N, call N+1 through `_SESSION` carried no `Cookie` header on both the staff host-locked path and the plain path (experiments A1/A1b), and `len(_SESSION.cookies) == 0` afterwards — `DefaultCookiePolicy(allowed_domains=[])` → `is_not_allowed` returns True for every domain. The staff host-locked loop also does NOT forward a hop-1 Set-Cookie to hop 2 (A3: `/chain-target` received no Cookie), because each hop is a separate `Session.request(allow_redirects=False)` with a fresh per-request jar. Confirmed the known [parity] nit for the plain `else` branch only: A2 shows `Cookie: hop=1` on hop 2 (sessions.py:240-242 `extract_cookies_to_jar(prepared_request._cookies, …)`; affected callers: difmark, instant_gaming, cdp_client); urllib sent none.
- [security] (c) Proxy env parity: requests `get_environ_proxies` delegates to stdlib `urllib.request.getproxies()` (requests/utils.py:829), the same source as urllib's default ProxyHandler, so `https_proxy`/`no_proxy` route the staff UA through a proxy in BOTH backends — pre-existing, not introduced by these commits. CA bundle: on this Debian box `certifi.where()` = `/etc/ssl/certs/ca-certificates.crt` (python3-certifi is patched to the system store), so trust roots coincide with urllib's OpenSSL defaults; `REQUESTS_CA_BUNDLE`/`CURL_CA_BUNDLE` (sessions.py:768) vs `SSL_CERT_FILE` are both env-controllable trust anchors — same risk class, different variable names. No proxy/netrc/CA env vars are set in this sandbox.
- [security] (d) Wire shape measured on both backends (experiment W): keep-alive sends `Accept-Encoding: gzip, deflate` (no brotli/brotlicffi/zstandard importable → no br/zstd), `Accept: */*`, `Connection: keep-alive`, header name `User-agent` (urllib `Request.add_header` capitalize(), preserved by requests' CaseInsensitiveDict merge); urllib sends `Accept-Encoding: identity`, `Connection: close`, `User-Agent` (title-cased in `do_open`, request.py:1305). Header-name case is RFC-insignificant; AKS/WAF-side behaviour cannot be verified offline (no network).
- [security] (e) Timeout semantics are equivalent: an int timeout becomes `TimeoutSauce(connect=timeout, read=timeout)` (adapters.py:664) — per socket operation like urllib's socket timeout. J1 (2.5s before headers, timeout=1): both time out (`Read timed out` vs `TimeoutError: timed out`). J2 (6-byte body dripped over 3×0.7s, timeout=1): both succeed in 2.8s wall — neither backend bounds total time; no new slow-drip exposure.
- [security] (f) Exception mapping is complete: `requests.RequestException` is a subclass of `IOError`/`OSError` (verified `issubclass(...) → True`), so even without the blanket `except Exception → URLError`, `http_get`'s `except (HTTPException, TimeoutError, OSError)` would catch it. With `stream=False` the body is read inside `Session.send` (`sessions.py:746 r.content`) i.e. inside `_SESSION.request` inside the try; `_KeepAliveResponse.read()` returns the cached `_content` (models.py:891-904, no I/O); `__exit__`/`resp.close()` → `Response.close` → urllib3 `release_conn` → `_put_conn`, which swallows `AttributeError`/`queue.Full` and raises `FullPoolError` only `if self.block` (adapter block=False). Non-2xx branch: `resp.content` is cached, `resp.close()` safe.
- [security] (g) `_KeepAliveResponse` interface is sufficient: the only consumers of `_http_open`'s return are `_response_to_probe` (`.read()`, `.status`, `.headers.items()`) and `http_head_status` (`.status`, `.headers.items()`), both inside `with`; grep of src/ and scripts/ found no `.geturl()`, `.info()`, `.getheader`, `.reason`, `.msg`, `.fp`, `.closed`, `len()` or iteration on the response, and no consumer reads `Location` from `HttpProbeResult.headers` (dict, case-sensitive) — so the dict `hdrs` in `HTTPError`/`StaffUaRedirectRefused` is safe (experiment F: 404 → status 404, body 'nope', error 'HTTP Error 404: Not Found' on both backends; existing test `test_host_locked_refuses_off_domain_redirect` exercises the refused path).
- [security] (h) Import-time: `import requests` + `Session()` at src/aks_env import has no file/network side effects; any `RequestsDependencyWarning`/urllib3 warnings go to stderr, and the admin's `_post_invariants` parses `proc.stdout` only (src/admin/app.py:496-505 `json.loads(proc.stdout)`), scripts/01 prints the report to stdout. Attributes used (`Session.max_redirects`, `RequestsCookieJar.set_policy` — inherited from `http.cookiejar.CookieJar`, `Response.status_code/reason/headers/content/close`) all exist in requests 2.28.x; bookworm's urllib3 1.26 has the same `_URI_RE`, so the backslash finding applies there too. The `except Exception` import guard correctly falls back to urllib if requests is absent/broken.
- [security] (i) Pool staleness after a clean restart is handled: G2 (server shut down and rebound on the same port between two probes) → keep-alive second probe ok=True/200 via `is_connection_dropped` (connectionpool.py:288). Only the concurrent-close race (finding above) yields a spurious failure, and it fails CLOSED (ok=False/None), never a wrong success. `HEAD` through the keep-alive path works (`http_head_status` → 200, empty body).
- [security] (k) Invariant-gate divergence enumeration for the actual call (`http_get(AKS_DIRECT_URL, follow_redirects=False, user_agent=AKS_STAFF_UA)`, constant ASCII URL, status judged by `validate_aks_direct_status(status)` = 200/301/302): keep-alive no-redirect branch maps 2xx → `status` = code, any 3xx/4xx/5xx → `HTTPError(code)` → `status` = code (urllib3 `redirect=False`, `Retry(0)` never follows/retries on status), transport/decoding failure → None; urllib maps identically except the divergences listed above, all toward red (bad gzip → None vs 200; unparsable Location → None vs ValueError crash). The no-redirect check precedes `host_locked` (line 388-389) as the 54f1f88 fix says, verified by `test_no_follow_under_host_lock_surfaces_3xx_not_followed`. No path found where keep-alive reports 200/301/302 and urllib does not for this URL; requests' URL requoting could only matter for operator-supplied URLs with spaces (scripts/11), where urllib raises InvalidURL → None.
- [security] Thread/fork safety: the admin server never calls `http_get` in-process (src/admin/app.py imports no aks_env HTTP helper; invariants run as a `subprocess.run` of scripts/01), `login_manager.py` imports only `OFFICIAL_CDP_ENDPOINT`, `submit_manager.py` nothing from aks_env; stages are subprocesses, so `_SESSION` is per-process and single-threaded in practice.
- [security] matcher ™ strip (`_NFKC_LETTER_SYMBOL_RE`, src/matcher.py) is not in this lens; only noted that it runs before NFKC inside `normalize_apostrophes` as claimed (no security surface).
- [tests] Test-count claims reconcile exactly: `def test_` at 184b2b8~1 = 1311; 184b2b8 adds 7 (-> 1318, matches its commit message), 54f1f88 adds 2 (-> 1320, matches), 9ddf185 adds 2 (-> 1322, matches HANDOFF.md:48); full suite on this box (run once, background): `Ran 1322 tests in 354.703s`, `OK (skipped=2)`, exit 0 — consistent with the lead's run.
- [tests] `requests` is optional in production code end-to-end: the only `import requests` under src/ or scripts/ is the guarded one at src/aks_env.py:330 inside try/except Exception; with requests blocked, `_SESSION is None` and all non-KeepAlive tests (349/352 of the targeted modules) pass; the invariant gate process imports fine. Only tests/test_aks_env.py:374 and :400 import it unguarded (known #1).
- [tests] No pre-existing test patches `src.aks_env.urlopen`/`build_opener` expecting to intercept network: the sole urlopen patch (tests/test_cdp_session.py:193) targets `src.cdp_session.urlopen`, and src/cdp_session.py:182/200 call `urlopen` directly, not through the aks_env seam — so nothing in the suite is bypassed onto the real network; no test was run that could reach allkeyshop.com.
- [tests] Stale 'stdlib only' claims: apart from docs/CONTRIBUTING.md:19-21 (known), none found. README.md:177-181, docs/HANDOFF.md:37-38, ops/BROWSER_RUNBOOK.md:362-366 and requirements.txt already describe `requests` as optional; the remaining 'Standard library only' strings are per-module docstrings (src/admin/app.py:8, src/validation.py:10, src/contracts.py:1/12, src/cdp_session.py:1, src/browser_lock.py:17, src/run_log.py:9, src/admin/{learning_io,validation_io,runs,submit_manager}.py, src/step_guard.py:90/370, docs/DATA_CONTRACTS.md:11, docs/ARCHITECTURE.md:139, ops/INSTALL_ADMIN.md:6) that remain true for those modules; docs/AUDIT_2026-07-17.md is a dated audit; ops/aks-admin.service runs /usr/bin/python3 with no dependency assumption.
- [tests] Backend parity (local 127.0.0.1 server, both backends driven through the real http_get/http_head_status, `_allkeyshop_host` monkeypatched): identical (ok, status, error, body, request-count) for 200; 302 follow / no-follow with plain and staff UA; relative Location `/ok` and `sub/ok`; lowercase `location:` header name; 3xx without Location; off-domain refusal under staff (StaffUaRedirectRefused, ok=False); 303 and 308 follow (Python 3.13 here); 9-hop chain; HEAD 200 / HEAD 302 follow / HEAD 302 no-follow / HEAD 404; cross-probe cookie statelessness (`GET /echo` after a Set-Cookie -> cookie null, Session jar empty). The only divergences are the already-known ones (10-hop staff 9-vs-10, 11-hop chain and loop -> status None vs 3xx ok=True, hop-2 cookie forwarded within a plain followed chain) plus the javascript:-scheme nit above.
- [tests] Wire headers through the keep-alive backend are correct: a single `User-agent: AKS/Staff` reaches the server (urllib's `Request` capitalizes to `User-agent`; requests merges its default `User-Agent: python-requests/...` case-insensitively, so no python-requests UA leaks). urllib sends `Accept-Encoding: identity`/`Connection: close`, keep-alive `gzip, deflate`/`keep-alive` (known wire-shape nit).
- [tests] Fake fidelity of `_FakeReqResp`: its plain-dict `.headers` is STRICTER than the real `CaseInsensitiveDict` for `.get('Location')` and equivalent for `dict(resp.headers)`, so no test can pass on the fake yet fail on the real object in that direction (real lowercase `location:` verified followed); `.reason` None is handled by `resp.reason or ''`; because `Session.request(..., stream=False)` consumes `.content` inside the try block, `_KeepAliveResponse.read()` cannot raise later, matching the fake's eager `content`.
- [tests] Exception mapping: `except StaffUaRedirectRefused: raise` precedes `except Exception` (so the refusal is not masked); `Exception` not `BaseException`, so KeyboardInterrupt still propagates; the ceiling `URLError` raised inside the try is re-wrapped once (known parity nit); with `-W error` the import still succeeds and nothing is printed to stderr on import.
- [tests] Import-time cost of the module-level Session: `import src.aks_env` 154-215 ms with requests vs 70-116 ms with it blocked (3 runs each) — roughly +60-90 ms per process for every importer (scripts 01/02/03/05/06/08/09/11, diag scripts, src/admin/login_manager -> admin server, cdp_client, cdp_session, invariants, matcher, merchants, tests); no SSL context or connection is created until the first request, no warnings emitted.
- [tests] Imports/leftovers: `io` (BytesIO at :404/:424) and `urljoin` (:399) are used; `_requests` is used once (`_requests.Session()` :333) and doubles as the availability flag set to None in the except; `_http_open_urllib`'s body is the pre-change `_http_open` code verbatim (only the def/docstring lines differ in the diff).
- [tests] Commit-message test claims hold: 'Regression test added (host_locked + follow_redirects=False -> HTTPError)' = tests/test_aks_env.py:379-389 (verified the fake 307 surfaces as HTTPError 307 and `_SESSION.request` is called with allow_redirects=False); 'Tests: TooManyRedirects -> URLError, ceiling==10' = :397-407 (the former via a faked `requests.exceptions.TooManyRedirects`, the latter reading the real Session).
- [tests] test_matcher.py additions are sound: `tokenize('Company™') == ['COMPANY']`, the Eneba title, `missing_aks_words(...) == []`, `Widget℠ Pro`, glued `Halo®Deluxe` splitting (symbol -> space), `cleaned_title('Company™')` not containing COMPANYTM, and `Game № 5` all pass in the suite; the test comment correctly states ®/© have no letter decomposition (the src comment does not).
- [tests] scripts/10 `--max-pages` semantics (top = min(feed_last, start+max-1), walked top -> start) were established by the lead and not re-derived here; the new help text quoted in the diff matches that behaviour.
- [matcher] Old-vs-new corpus (58 titles × tokenize/cleaned_title/build_slug_candidates/explicit_platform/detect_edition/precheck_skip/detect_region/is_software_title + 38 AKS/merchant pairs × missing_aks_words/extra_significant_words/dangerous_qualifier): 152 diffs, ALL in the fix direction — every glued TM/SM/TEL token is gone (COMPANYTM→COMPANY, MANTM→MAN, WARSTM→WARS, 000TM→000, COMETM→COME, SPORTSTM→SPORTS, SIMULATORTM→SIMULATOR, BALLTM→BALL), and every corresponding missing_aks_words/extra_significant_words result went from a false skip to []. No regression found on any ™/℠ title.
- [matcher] Zero old/new diffs in precheck_skip, detect_region, detect_edition, is_software_title, dangerous_qualifier — they operate on raw `.upper()` text (never NFKC), where ™ is a non-word char so `\b`/space-padded checks already worked; the change cannot alter them (so the 'precheck covered' claim is harmless, not a bug).
- [matcher] Stripping © and ® changes NOTHING in tokenize, build_slug_candidates, explicit_platform, detect_edition/region (the `[A-Z0-9']+` / `[^a-z0-9]+` regexes already discarded them — 'Halo®Deluxe', 'F1® 24', 'NBA 2K24©', 'Civilization® VI' identical old/new on those paths). Only cleaned_title's text changes, i.e. the R30 site-search query and scripts/11 name_term; for a TRAILING ©/® this is a genuine fix (old cleaned_title kept 'NBA 2K24 ©' because © is not in _SEPARATOR_CHARS → a stray '©' WP search term).
- [matcher] explicit_platform gains correct declarations for symbol-suffixed platform words: 'Some Game Steam™ Key' old None → new STEAM, 'GOG™ Key' → GOG, 'Epic™ Games Key' → EPIC, 'Game™Steam Key' → STEAM (old fell to the None → default-STEAM guess gated by R20) — a fix.
- [matcher] Roman-numeral NFKC fix preserved: 'Road to Empress Ⅱ' / 'Civilization® Ⅵ' / 'Kingdom Come™ Deliverance Ⅱ' → II/VI identical old/new; ﬁ ligature ('The ﬁnal Frontier' → FINAL) and full-width 'ＦＩＮＡＬ ＦＡＮＴＡＳＹ ＸＶＩ' decompose correctly with no diff; the class contains no letterlike math symbols and no Roman numerals, as its comment claims (all 11 members checked against unicodedata: ™→TM, ℠→SM, №→No, ℡→TEL, ℅→c/o, ℀→a/c, ℁→a/s, ℆→c/u; ©/®/℗ have no decomposition).
- [matcher] Whitespace interplay with the leading ' ' is safe for the trailing-phrase stripper and the head split: 'Some Game Steam™ Key GLOBAL' → cleaned_title 'Some Game' (KEY stripped, rstrip, STEAM stripped — old left 'Some Game SteamTM'); 'Game™- Steam Key - GLOBAL' → 'Game'; 'Halo™ (PC) - Steam Key - GLOBAL' → 'Halo'; slugs collapse any run of non-alnum to one '-' so double spaces never reach a slug.
- [matcher] No caller uses normalize_apostrophes output as a JSON/state key: outputs land in tokens, ASCII-collapsed slugs, the R30 search query, edition-name token sets (matcher.py:1700), and scripts/11 `meta.name_term` (display/log only).
- [matcher] Feed offer names are html-unescaped at extraction (src/extractor.py:201-229 unescape_attribute → json.loads), and extract_aks_name html.unescape()s og:title (matcher.py:1172), so an `&trade;` entity reaches the matcher as U+2122 and is stripped; AKS-side symbols (if any og:title carries them) are handled symmetrically since both sides go through tokenize.
- [matcher] 'Game HD™' now yields extras ['HD'] instead of ['HDTM'] — both skip as a different product (HD is not NOISE), harmless; 'Game Remastered™' vs AKS 'Game Remastered' now matches (old missing ['REMASTERED']) while dangerous_qualifier still fires against an AKS 'Game' (substring on raw upper) — fail-closed behavior intact.
- [matcher] tests.test_matcher: Ran 292 tests, OK (0.14s). 6 of the 7 assertions in the new test fail on the pre-commit code, i.e. they pin the ™/℠/№ token fix and the missing_aks_words repro; the docs edits (CHANGELOG 2026-09-08 entries, HANDOFF §5 line 54) describe the strip location and set consistently with the code.
- [documentation] EXECUTOR_RULES §4.1 symbol list `™ ℠ № ℡ © ® ℗ ℅ ℀ ℁ ℆` == the set in `_NFKC_LETTER_SYMBOL_RE` (`[©®℀℁℅℆№℗℠℡™]`, 11 symbols, verified by set comparison); order strip→NFKC→apostrophes matches normalize_apostrophes (src/matcher.py:509-510); every decomposition claim in EXECUTOR_RULES/matcher comment verified with unicodedata (™→TM, ℠→SM, №→No, ℡→TEL, ℀→a/c, ℁→a/s, ℅→c/o, ℆→c/u, ©/®/℗ identity).
- [documentation] HANDOFF §7 command forms all parse against the working-tree argparse: 02 `--merchant/--store-id` (→ runs/<id>/offers.json, 02:96,144); 03 positional offers (→ candidates.json, skipped.json, match_meta.json, report.txt, 03:101-131); 04 `template <candidates>` writes validation.template.json next to candidates (04:43) and `check <candidates> <validation>` writes approved.json (04:60); 05 positional approved + `--merchant --store-id [--submit] [--mode learning]`, and 05 re-verifies approved.json against sibling candidates.json + validation.json (05:13-16, 330-333; src/validation.py:122-130); 06 `run_dir --store-id --execute --mode learning|safe --i-authorize-batch` with the RV3 wording matching `--help`; 08 `--run-id` → runs/<id>/sort_plan.json + report.txt (08:156-158); 09 `run_dir --list 8 [--execute --mode … --i-authorize-batch]`; 10 `--targets --run-id [--dry-run|--max-pages|--triage]`; 11 `--run-id` required + `--urls-file` / `--urls` (whitespace/comma split, 11:314); 12 exists with `--from-run/--run-id`.
- [documentation] manual_launch/run_executor.sh exposes prepare / check / dry-run / submit and `check` writes approved.json (usage text lines 11-19) — HANDOFF §7 preamble is correct.
- [documentation] Store ids in HANDOFF §7 / scripts/10 docstring (Kinguin 58, Eneba 19, G2A 38, Driffle 127) match src/admin/auto_merchants.py; scripts/10:355 and src/admin/app.py:565 enforce `rejection_reason`, so `Eneba:70` would abort (the BILAN claim). The only remaining `Eneba:70` is tests/test_admin_submit_manager.py:788, a manager argv-passthrough test (the manager does not enforce the allowlist) — semantically fine.
- [documentation] `coverage` / `coverage_incomplete` field names and the values `incomplete_max_pages (feed has N pages)` / `incomplete_feed_grew (a→b pages)` are consistent across src/data_entry_auto.py:171,297-299, scripts/10:366,404-407,422-423 (stdout JSON too), scripts/10 --help text, src/admin/static/auto.js:146-147,180-182,194, and the module docstrings; exit code stays 0 on a cap (scripts/10:428); tests pin both values and the batch continuation (test_data_entry_auto.py:333-334,367; test_data_entry_auto_cli.py:62-84).
- [documentation] auto.html placeholder "30 (défaut)" is truthful: the admin passes `--max-pages` only when the field is set (src/admin/submit_manager.py:736-737) → scripts/10 default 30; `SweepConfig.max_pages` default is now 30 too (removes the dead 400). HANDOFF §6 "/auto l'affiche comme note de couverture, pas ARRÊTÉ" matches auto.js ("TERMINÉ — couverture partielle", "Sweep terminé — couverture partielle : …", per-merchant "couverture : …"). The /auto recap route serves the raw runs/<id>/recap.json (app.py:618-630), so `coverage_incomplete` reaches the JS.
- [documentation] Throttle guard contract: `AksThrottled` is not a subclass of `AksProbeUnreliable`; match_offer catches only AksProbeUnreliable / AksNameUnreadable / AksPageUnparseable (matcher.py:2006-2010; the `except Exception` at :1871 is the merchant-page read, before resolution), so the abort propagates to 03 (exit 2, `match_aborted` JSONL) and scripts/10's match stage maps rc≠0 → `MatchOutcome(ok=False)` → `halted=match_failed_pN` (scripts/10:104). Counter resets on any non-raising resolution (incl. None); precheck-skipped offers never touch it; account-page resolutions share the guard with the production resolver. Transport errors (status None) count toward the 5 — 5 consecutive 8 s timeouts is not a healthy AKS, so the bar is reasonable. scripts/11's own `AksProbeUnreliable(...)` calls without `status` keep working (default None).
- [documentation] `match_meta.json.probe_unreliable`: all consumers (scripts/05:402-411, src/admin/submit_manager.py:457-467, src/admin/runs.py:38, scripts/12:95) read `data_entry_mode` only and tolerate extra keys — no contract break.
- [documentation] trust_env / proxy statements: `_SESSION.trust_env = False` (aks_env.py:357) and `_urllib_proxies()` (getproxies + proxy_bypass per request) verified by experiment — env proxies applied to the AKS URL, `no_proxy` host bypassed; unit tests assert `proxies={}`; the real local-server netrc test would FAIL on the old code (trust_env=True reads $NETRC and injects Authorization), so it pins the fix. RUNBOOK/requirements/README wording (no ~/.netrc, no REQUESTS_CA_BUNDLE/CURL_CA_BUNDLE, proxies mirrored) matches the code apart from the precision nit reported.
- [documentation] requests 2.28.1 compatibility of every attribute used: Session.trust_env, Session.max_redirects, cookies.set_policy/get_policy (RequestsCookieJar inherits http.cookiejar.CookieJar; get_policy present since 2.x), Session.request(method, url, headers=, allow_redirects=, timeout=, proxies=), Response.status_code / reason / headers (case-insensitive) / content / close() — all exist in 2.28 (none were introduced after it); `requests>=2.28` is satisfied by Debian 12's python3-requests 2.28.1.
- [documentation] RUNBOOK §3 verification one-liner works from the repo root (`python3 -c 'import src.aks_env as e; …'` → "keep-alive" here); `aks-admin.service` really runs `/usr/bin/python3` (ops/aks-admin.service:9) so the "a venv would not be seen" claim is right; Debian 12 = Python 3.11; the PEP 668 refusal on the system interpreter is accurate.
- [documentation] Python floor consistency: README.md:177, docs/CONTRIBUTING.md:19, ops/BROWSER_RUNBOOK.md:359 and .github/workflows/ci.yml:27 all say 3.11; README's justification (urllib HTTPRedirectHandler handles 308 only since 3.11) verified in the stdlib source.
- [documentation] CONTRIBUTING's seam advice is correct: `src.aks_env._http_open` is the single seam used by both http_get and http_head_status (aks_env.py:600), and patching `urlopen` would indeed miss the keep-alive backend.
- [documentation] CI matrix: stdlib-only + with-requests, `fail-fast: false`, 3.11; the suite behaves as documented — `tests.test_aks_env` passes with requests (67 OK) and with `sys.modules['requests']=None` (67 run, OK, 2 skipped = the two real-Session tests, `_SESSION is None` confirmed); tests.test_data_entry_auto (39 OK), tests.test_data_entry_auto_cli + tests.test_data_entry_by_urls (56 OK), tests.test_matcher.MatchFeedTests + TokenizeTests (15 OK). No hard `import requests` remains in tests. Local server tests bind 127.0.0.1:0, `shutdown()` + `server_close()` in tearDownClass, daemon thread — no port/thread leak; nothing leaves the box.
- [documentation] HANDOFF §1 pipeline matches scripts/ (02 → 03 outputs → 04 validate `check` → approved.json → 05 → prove-gone); R40 exists (EXECUTOR_RULES:530); HANDOFF §4 "il n'existe pas d'entrée CHANGELOG dédiée" for the H24 daemon verified (CHANGELOG 'daemon' hits are only the openvpn entry); HANDOFF §3 audit numbers (38 confirmés / 7 réfutés) match state.json + REVIEW.md.
- [documentation] No stale `coverage_incomplete_max_pages`, `(tout le feed)` placeholder, or `Eneba:70` remains in tracked docs/code outside historical CHANGELOG entries (2026-09-08 :50; T1 :2504 '3.10') and the manager passthrough test; the `index.html:89` "Full shop (tout le feed)" is the extractor mode label, unrelated to the cap.
- [documentation] The strict `_allkeyshop_host` regex accepts every URL shape the code actually builds or receives: AKS_DIRECT_URL, `AKS_COMPARE_URL` slugs (`[a-z0-9-]` only), search URLs (query is outside the netloc), admin URLs, `:443` ports, mixed-case hosts; operator-pasted URLs without a scheme were already refused by the old hostname check. The urls-file parser strips whitespace/commas (11:314-315).
- [documentation] `_pace_between_urls` sleeps AKS_PROBE_DELAY_S only for the real `http_get` and only from the second URL; the pacing test asserts both negative cases and the positive call (would fail on the old code, which had no such function).
- [documentation] "Saisir" is a real console button (src/admin/static/urls.html:90) offered only on a finished, non-aborted dry-run with candidates (urls.js:301) — HANDOFF §7's by-urls line is accurate.
- [documentation] CHANGELOG entry (NOT yet written — the lead must state ALL of these): (1) heading `2026-09-09 — audit adversarial 184b2b8→e7586f6 : correctifs`, pointer to docs/audit_2026-09-09_last-commits/ (BILAN/REVIEW/state.json), counts 45 findings / 38 confirmés / 7 réfutés (3 added by the completeness critic), 2 majors; (2) [major OPEN] `_allkeyshop_host` strict: http(s) only + netloc `^[A-Za-z0-9.-]+(?::\d{1,5})?$` (refuses userinfo `@`, backslash, whitespace, IPv6 literal, javascript:/data:) — urlsplit-vs-urllib3 parser differential; it guards the staff-UA send (http_get), the keep-alive host-locked loop, the urllib redirect handler and scripts/11's pinned-URL check; (3) host-locked loop now follows exactly 10 redirects (`range(11)`) like urllib, re-quotes Location like `http_error_302` (latin-1 → %XX, no double-encoding), raises the ceiling URLError outside the blanket except (no `<urlopen error <urlopen error` double wrap); residual, documented fail-closed divergences (ceiling → URLError/status None vs HTTPError(3xx); hop-1 Set-Cookie forwarded within one plain-follow call; non-UTF-8 Location fails inside requests); (4) `_SESSION.trust_env = False` (no ~/.netrc/$NETRC Authorization injection, no REQUESTS_CA_BUNDLE/CURL_CA_BUNDLE) + `_urllib_proxies()` mirroring urllib's ProxyHandler (http(s)_proxy / no_proxy) per request; (5) [major CLOSED] cap `--max-pages` / feed-grew = recap field `coverage` (`incomplete_max_pages (feed has N pages)` / `incomplete_feed_grew (a→b pages)`) instead of `halted=coverage_incomplete_*` — batch continues to the next merchant, exit 0; scripts/10 batch recap + stdout gain `coverage_incomplete: ["<merchant>: …"]`; `SweepConfig.max_pages` 400 → 30; /auto: placeholder "30 (défaut)", pill "TERMINÉ — couverture partielle", per-merchant "couverture : …"; explicitly name the OLD flag so recap greps are updated; (6) throttle backstop: `AksProbeUnreliable.status`; `AksThrottled` + `_ThrottleGuard` in `match_feed` — abort on the FIRST 429 or on `THROTTLE_MAX_CONSECUTIVE_UNRELIABLE = 5` consecutive unreliable probes (reset by any clean resolution; account-page resolutions share the guard with the production resolver); 03_match exit 2 with `{"aborted": true, "reason": "aks_throttled"}` + JSONL `match_aborted`; safe-auto halts `match_failed_pN`; `match_meta.json.probe_unreliable` counter; state that by-urls (11) is NOT guarded (or fix it); (7) scripts/11 `_pace_between_urls` (AKS_PROBE_DELAY_S between operator URLs, never before the first, never under a stub); (8) tests: real two-backend 127.0.0.1 http.server tests (dispatcher + urllib fallback now executed; no-redirect never follows; staff same-host follow / off-host refusal never reaches the wire; plain-UA follow + status mapping; keep-alive stateless across calls + netrc-blind), host-locked loop tests (hop progression, relative Location, lowercase header, no Location, exactly-10 hops, 11th refused single-wrapped, backslash/userinfo/javascript refused, refused Location never requested, latin-1 re-quote), HostGuardTests, real-Session properties (max_redirects, trust_env, cookie policy), throttle tests (429 immediate, N consecutive, reset), cap tests (coverage not halt; batch continues exit 0), pacing test, `Halo™Deluxe` + `build_slug_candidates` assertions replacing the vacuous `®` one, stdlib-only exception stand-ins (no hard `import requests`); count 1322 → 1342; (9) CI: matrix stdlib-only + with-requests, Python 3.10 → 3.11 (fixes the CI red since 184b2b8 where 3 tests hard-imported requests); (10) docs: Python 3.11+ floor (README/CONTRIBUTING/RUNBOOK), `sudo apt install python3-requests` (PEP 668; aks-admin runs /usr/bin/python3) instead of pip, "same probe contract, wire shape differs" replacing "identical behavior", rate-rise attribution moved to the pacing change (requirements.txt / aks_env / matcher comments), EXECUTOR_RULES §4.1 pre-NFKC symbol strip, CONTRIBUTING seam example `_http_open`, HANDOFF §1 (04 validate), §3 (audit bullet), §4 (no H24 CHANGELOG entry), §6 (cap = coverage), §7 (04 template/check, 06 learning → safe --i-authorize-batch, 08/09, 10 --dry-run + WRITE/GO markers + Eneba:19, 11 --run-id --urls-file, "mono-process"), matcher `_NFKC_LETTER_SYMBOL_RE` comment (two families; № dropped by design); (11) untouched by design: R25 stays removed, software-region catch-all stays (AGENTS.md reviewed decisions); list the 7 refuted findings briefly.
- [the] End-to-end wiring (local 127.0.0.1 server, REAL match_feed→resolve_aks→http_get, keep-alive Session backend, requests present): after 4 healthy 404s the FIRST 429 on a guessed slug raises AksThrottled('AKS answered 429 (rate limited): gamma-game -> 429') after exactly 5 requests, all with User-Agent 'AKS/Staff' — no 'one 429 per offer' loop.
- [the] 503 forever → AksThrottled after exactly THROTTLE_MAX_CONSECUTIVE_UNRELIABLE=5 requests (the 6th+ offers are never probed).
- [the] Transport failures (status None: server closes the socket without a response → RemoteDisconnected → URLError → http_get status=None) ARE counted; 5 in a row abort in 0.18 s. This is the right fail-closed direction: each failure discards the pooled connection so a healthy AKS recovers on the next probe and resets the counter; a genuine 5-in-a-row (≤ 5×8 s timeouts) is an outage/throttle, not a blip.
- [the] Below the limit an unreliable probe stays a per-offer 'AKS probe unreliable (throttled?)' skip (unchanged behaviour); any clean resolution (Candidate or None) resets the counter; precheck-skipped offers (e.g. 'forbidden region: ROW') neither increment nor reset because the resolver is not called.
- [the] scripts/03_match.py main() in-process against the local server: pre-flight 200 → match → rc=2, stdout {'aborted': true, 'reason': 'aks_throttled', 'detail': …}, run dir contains ONLY offers.json (no candidates/skipped/match_meta/report written), JSONL carries match_progress ×2 then match_aborted.
- [the] http_get on 429/503 through the keep-alive backend returns ok=False with the real status and issues exactly ONE request (HTTPAdapter default max_retries=0, no requests-level retry) — so the guard's request accounting holds.
- [the] `account_resolver = guard if resolver is resolve_aks else resolve_aks`: 03_match passes bare resolve_aks (identity holds → Difmark account resolutions share the same guard/counter); grep shows no functools.partial/wrapped resolver anywhere in production (match_feed's only caller is scripts/03_match.py; scripts/11 calls match_offer directly with a pinned resolver and never match_feed); nothing inside match_offer/resolve_aks tests `resolver is resolve_aks` (only `http_get_fn is http_get`, unaffected by the wrapper).
- [the] AksThrottled propagates: match_offer catches only AksProbeUnreliable / AksNameUnreadable / AksPageUnparseable; no blanket except between the resolver call and 03_match's handler; on_progress logging cannot swallow it.
- [the] scripts/10 copes with a run dir lacking candidates.json after rc=2: _load_json returns None (no FileNotFoundError/KeyError) → candidates=0, MatchOutcome.ok=False → halted=match_failed_pN, break, exit 2 (pre-existing test_match_failure_halts covers the halt path); manual_launch/run_executor.sh runs with `set -euo pipefail` so a 03 rc=2 stops the chain before 04's template step.
- [the] The admin's manual match (submit_manager.start_match → _spawn/_supervise) records state='failed', exit_code=2 and stdout_tail containing the aborted JSON, which app.js renders as a <pre> on the failed state — the operator sees 'aks_throttled' there.
- [the] The three new tests (tests/test_matcher.py:2834-2881) go through match_feed→match_offer (not the guard class alone), assert meaningful call counts (1 for the 429 fast path, N for the consecutive path, 2*N-2 skips with the reset), and CANNOT pass on the old code: `AksProbeUnreliable(msg, status=429)` raises `TypeError: takes no keyword arguments` on the old class and AksThrottled does not exist. MatchFeedTests: 6/6 OK.
- [the] scripts/11 _pace_between_urls: sleeps AKS_PROBE_DELAY_S only when index>0 and http_get_fn is the real http_get (no sleep under stubs, none before the first URL) — test_paces_between_urls_only_for_the_real_http_get pins it; AksProbeUnreliable raised in scripts/11 without a status kwarg still constructs (status defaults to None) and `getattr(exc, 'status', None)` in the guard is robust to foreign raisers.
- [the] 03_match's pre-flight probe (follow_redirects=False, staff UA) already aborts 'AKS not reachable' on a 429 at the blog root before any offer is probed.
- [the] HANDOFF §1 claim '`aborted: aks_throttled`' and the 03_match stdout/JSONL wording match the code exactly; `match_meta.probe_unreliable` counts skips by the 'AKS probe unreliable' prefix, which is the exact reason string match_offer emits.
- [test] Syntax/hygiene: `python3 -m py_compile` passes on all 11 changed .py files; `ast.parse` on scripts/03, 10, 11 OK; the new `AKS_PROBE_DELAY_S` line inside scripts/11's parenthesised `from src.matcher import (...)` is syntactically fine and `import time` is present (scripts/11:24).
- [test] No unused imports introduced in src/aks_env.py: `re` (3 uses), `string` (1), `quote` (1), `getproxies` (2), `proxy_bypass` (3) all used; no TODO/FIXME/pdb/debug prints in the src/scripts diff (the only new `print(` is 03_match's intended JSON abort output).
- [test] Stdlib-only box simulation: with `sys.modules['requests']=None`, `_SESSION` is None and tests.test_aks_env + the other changed modules run 158 tests, 2 skipped (both real-session tests skip with a clear reason), OK — under `-W error`. `python3 -X dev -W error -m unittest tests.test_aks_env` shows no ResourceWarning / unraisable exception (no socket/thread leak from the local server).
- [test] No test imports `requests` at module or test level any more (the three hard imports the audit flagged are gone; `ConnectionError` and a local `TooManyRedirects` stand-in are used), so the stdlib-only CI job cannot ERROR on import.
- [test] New tests pin the fixes: run against a `git archive HEAD` tree with the new test files dropped in, 9 test_aks_env tests fail/error on old code (follows_exactly_ten_redirects, eleventh_redirect_single_wrapped, requotes_latin1, refuses_backslash_authority, never_requests_a_refused_location, follows_same_domain_redirect [new proxies/shape assertions], real_session trust_env, keepalive netrc-blind, HostGuard refuses_ambiguous) and 8 tests in the other modules fail/error (3 throttle tests, max_pages_cap, feed_grew, uncapped_sweep, CLI coverage_cap_exit_0, PacingTests). Remaining new tests that pass on old code are legitimate regression guards the audit asked for (3xx-without-Location, relative Location against current hop, the three either-backend real-server tests, adapted transport/TooManyRedirects) — not vacuous, except the `_CI` one reported above.
- [test] RealBackendsLocalServerTests hygiene: ephemeral port (`('127.0.0.1', 0)`), daemon thread, `shutdown()` + `server_close()` in tearDownClass, `_LocalHandler.seen` reset in setUp and before each subtest, the refused `/off` hop is never requested (so the `localhost` name is never resolved), 6 consecutive isolated runs all OK; `_REAL_SESSION` is captured at import and every `_SESSION` patch in the suite is context-managed (no test assigns `_SESSION` directly), so ordering cannot change what `_backends()` yields.
- [test] No wall-clock dependence: PacingTests mocks `time.sleep`; the throttle tests use fake resolvers (no `AKS_PROBE_DELAY_S` sleep, which lives inside `resolve_aks`).
- [test] `_ThrottleGuard.__call__(name, **kwargs)` matches both resolver call shapes in `match_offer` (`resolver(offer.name)` and `account_resolver(offer.name, page_kind=...)`); `match_offer` has the `account_resolver` kwarg; the only other `match_offer` caller (scripts/11:290, `resolver=pinned_resolver`) is unaffected; scripts/03 passes `resolve_aks` so `resolver is resolve_aks` is True and account-page resolutions are guarded too.
- [test] `_pace_between_urls` fires in production: `run_plan` is called once (scripts/11:533) with the default `http_get_fn=http_get`, so the identity check is True; the test stub path correctly skips.
- [test] CLI test `test_coverage_cap_is_not_a_halt_batch_continues_exit_0` writes only under a TemporaryDirectory (`MOD.ROOT` patched in setUp, restored via addCleanup); scripts/10 reads `total_moved` with `.get(..., 0)` so the fake recap lacking it is fine; `coverage_incomplete` is initialised in the batch recap and appended per merchant; `halted` handling for real halts unchanged.
- [test] CI matrix YAML parses; an include-only matrix with two entries is valid GitHub Actions; `if: matrix.install != ''` is a valid expression that evaluates false for the empty string (step skipped in stdlib-only) and `run: ${{ matrix.install }}` only executes in with-requests; `name: test (${{ matrix.name }})` and `fail-fast: false` valid; python-version 3.11 matches the Debian 12 target.
- [test] requirements.txt: comments plus one PEP 508 spec `requests>=2.28`; pip is not installed on this box so no dry-run, but the format is trivially valid.
- [test] HANDOFF §7 command forms verified against the scripts' argparse `--help`: `04_validate.py template <candidates>` and `check <candidates> <validation>`; `06_move.py run_dir --store-id --execute --mode {safe,learning,advanced} --i-authorize-batch`; `08_sort_plan.py --run-id`; `09_sort_move.py run_dir --list --execute --mode --i-authorize-batch`; `10_data_entry_auto.py --dry-run`; `11_data_entry_by_urls.py --run-id --urls/--urls-file --dry-run`; `manual_launch/run_executor.sh check RUN_DIR` exists and writes approved.json.
- [test] CONTRIBUTING's seam name is right: `src.aks_env._http_open(request, timeout, follow_redirects=True, host_locked=False)` exists; no test patches `src.aks_env.urlopen` any more; BROWSER_RUNBOOK's `e._SESSION` keep-alive check works (Session object truthy, None otherwise).
- [test] Stale-comment grep: no remaining `range(10)`/'9 redirects' claims (the one `range(10)` at src/aks_env.py:426 is the historical explanation inside the fix comment); all `trust_env` mentions describe the new state; `coverage_incomplete_*` appears only in HANDOFF (correctly describing the new `coverage` values), the CHANGELOG line reported above, and the audit folder; `Eneba:70` is gone from HANDOFF and scripts/10's docstring.
- [test] `_urllib_proxies` behaviour (probe): returns `{}` for hosts covered by `no_proxy`/`proxy_bypass` (127.0.0.1, .allkeyshop.com) and the `getproxies()` mapping otherwise — the extra `no` key from `getproxies_environment` is ignored by requests' scheme/host lookup.

Critique de complétude — vérifié correct :
- Commit claim 'Not threaded into 05_submit … prove-gone coverage is unaffected' (and HANDOFF §6:108-109) holds: scripts/10:146-158 builds the 05 argv with --page-hint only (no --max-pages), 05's derive_max_pages (scripts/05:44-65) reads offers.json feed_last_page, and extract_pages records feed_last_page = max(nav_max, …) across the fetched page (src/extractor.py:608-622), so the whole-feed prove-gone ceiling is unchanged by the 200→30 sweep cap.
- _KeepAliveResponse exposes exactly what the callers consume: _response_to_probe/http_get/http_head_status read only .read(), .status, .headers.items() (src/aks_env.py:280-288, 456-518); no geturl()/.info()/get_content_charset()/getheader() usage anywhere in src/ or scripts/, and the HTTPError path's plain-dict headers satisfy exc.headers.items()/exc.read().
- No HEAD/GET redirect parity issue: Python 3.13 HTTPRedirectHandler.redirect_request preserves HEAD (method="HEAD" if m == "HEAD" else "GET"), and http_head_status has no production caller (grep: only defined in src/aks_env.py, used by tests).
- The shared module-level Session is never used concurrently: no threading/multiprocessing/os.fork in src/ or scripts/ except the admin's ThreadingHTTPServer, and src/admin never calls http_get/CdpClient in-process (stages are subprocesses); matcher probes are strictly serial with a fixed time.sleep before each (matcher.py:1415, 1439).
- First-hop probe URLs cannot be altered by requests' URL re-encoding: slugs are [a-z0-9-] (matcher.py:1385 regex, build_slug_candidates) and the ?s= search query goes through urllib.parse.quote (matcher.py:1370), so the divergence found only reaches Location-driven hops.
- requirements.txt `requests>=2.28` is satisfiable on the Debian 12 target via apt python3-requests 2.28.1; every Session attribute the code touches (max_redirects, cookies.set_policy with stdlib DefaultCookiePolicy, Response.status_code/reason/headers/content/close) exists there.
- AksProbeUnreliable on any non-404/410 status of a guessed slug (matcher.py:1417-1419) still prevents a throttled tier from resolving to a lower tier — the MA1 immediate-raise property is intact under the new pacing (it fails closed per offer; the gap is only the missing aggregate detection reported above).
- HANDOFF §7 argv shapes for 02/03/05 parse as documented: 02 --merchant/--store-id required (scripts/02:43-44), 03 positional offers.json (scripts/03:54), 05 positional approved.json + --merchant/--store-id/--submit/--mode (scripts/05:158-192); store ids Kinguin 58, Driffle 127, G2A 38 match src/admin/auto_merchants.py; the admin console's own argv for 05/10 (submit_manager.py:496-512, 733-738) matches the documented flags.
- HANDOFF §7 `python3 -m unittest discover -s tests` (without `-t .`) works: tests/__init__.py exists and a targeted run (`-p test_step_guard.py`) passed 27 tests both with and without -t.
- HANDOFF §7 `git add -A` cannot sweep the CLAUDE.md never-commit set: .gitignore already lists runs/, logs/, state/, .env, .env.*, cookies/2FA/secret patterns and .claude/settings.local.json.
- HANDOFF §2 marker instruction matches src/aks_env.py:195-217 (root-installed /etc/aks-executor.target, st_uid==0, no group/world write bits, content == socket.gethostname()); the documented runbook §3 order (Chromium → hold → UA policy → aks-chromium → socat → firewall → nginx/admin → marker → invariants → cookies → tests) matches ops/BROWSER_RUNBOOK.md §3 steps 1-11, and §2.1 (wedged browser) exists.
- HANDOFF §7 `cat state/browser.lock` comment is accurate: browser_lock writes '<label> pid=<pid> since <stamp>' into the file and empties it on release (src/browser_lock.py:43-80); recap.json's per-offer post_save text is 'gone from feed (available=<mode>)' (src/submitter.py:1513).
- No stale pacing/cap figures elsewhere: grep for 0.3 s / 300 ms / AKS_PROBE_DELAY / max-pages 200 across docs/, README.md, ops/, src/admin/static and src/scripts finds nothing outside the updated CHANGELOG/HANDOFF/matcher comment.
- Test-count claims are internally consistent: 184b2b8 (+8 tests → 1318), 54f1f88 (+2 → 1320), 9ddf185 (+2 → 1322 = HANDOFF §3 and the lead's full-suite run).
- scripts/10 --max-pages help text matches the implementation for the default --start-page 1 (top = min(feed_last, start+max-1), walked top→start, src/data_entry_auto.py:196; coverage_incomplete_max_pages set at :288-289).

## 5. Méthode

5 chasseurs (parity, security, matcher, ops, tests) sur le diff combiné + spot-check des commandes HANDOFF §7 ; dédoublonnage ; 3 vérificateurs adversariaux indépendants par finding (reproduction, relecture, matérialité), chacun instruit de RÉFUTER par défaut ; gardé = ≥2 lentilles non réfutantes ; réfuté = ≥2 réfutantes ; sinon indécis. Sévérité finale = médiane des sévérités corrigées des lentilles non réfutantes. Aucune requête réseau vers allkeyshop.com : expériences sur serveurs 127.0.0.1 locaux uniquement. Règles AGENTS.md « Reviewed decisions » exclues de la revue.
