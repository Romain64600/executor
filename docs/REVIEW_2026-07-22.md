# Revue 2026-07-22 — Learning / Move-to-List / README

Revue critique relayée par Romain après le 1er canary. Registre vivant :
OPEN → FIXED + commit en journal.

## Bloquant — comportement (batch)

### RV1. Un canary unitaire ne doit PAS débloquer le batch `--mode safe` — FIXED
Le garde MV6 débloquait `--execute --mode safe` dès qu'un move avait vérifié
(`moved>0`). C'est trop large : un canary sur **une** offre × **une** liste
n'autorise pas un lot général. **Fix** : `--execute --mode safe` est désormais
**refusé inconditionnellement** ; seuls les canaries unitaires supervisés
(`--mode learning`) écrivent. Un dry-run `--mode safe` reste permis (preview).
Le batch nécessite d'abord RV2 + RV3 (dette ci-dessous).

## Dette OUVERTE — deux conditions avant tout batch

### RV2. Vérifier la présence exacte dans la liste CIBLE — OPEN (dette)
Aujourd'hui le succès = « l'offre a quitté la liste source » (MV12). Pour un
**batch**, ce n'est pas suffisant : le move/delete d'un opérateur parallèle
compterait comme succès. Prérequis batch : après le départ de la source,
**confirmer la présence de l'offre dans la liste cible** (scan borné
`feed_page=<liste cible>`). Non implémenté → batch bloqué.

### RV3. Encoder la portée/version de l'autorisation issue du canary — OPEN (dette)
Un canary réussi doit produire une **autorisation explicite et versionnée** :
quel canary autorise quel périmètre (marchand × liste × identité d'extraction),
et jusqu'à quand. Sans cet encodage, aucun déblocage de batch n'est traçable ni
borné. Non implémenté → batch bloqué. `regle_globale` (côté Learning) est de la
même famille : une **intention**, pas une preuve (voir RV5).

## Learning — sorties et portée

### RV4. Ajouter une 4ᵉ sortie « observation retenue / pas d'action » — FIXED
`docs/LEARNING_PROCESS.md` ne listait que 3 sorties (move / saisie manuelle /
règle). Ajout d'une 4ᵉ : une **observation conservée** qui ne déclenche rien
(preuve insuffisante / décision non prise) — état légitime et explicite.

### RV5. `scope` = portée maximale *proposée*, pas « le contrat » — FIXED
Remplacé « le scope est le contrat » par **portée maximale proposée** :
`regle_globale` exprime une **intention**, pas une preuve de validité globale.
Le builder doit toujours **valider** avant de coder, et la portée effective peut
être plus étroite. (`docs/LEARNING_PROCESS.md`, EXECUTOR_RULES §13.)

## README — clarté / sécurité (délégués à un agent)

- **RV6. Adresse publique du VPS exposée** — `51.38.37.254.sslip.io` en clair
  dans README + docs + `ops/…conf`. → remplacer par un placeholder host
  (config templatée) ; note : déjà dans l'historique git, on réduit l'exposition
  future.
- **RV7. Succès HTTP vs succès métier mélangés** — distinguer HTTP 200 du succès
  métier (offre disparue du feed / arrivée sur la cible).
- **RV8. `uncertain → stop` non distingué d'un skip déterministe** — clarifier :
  fail-closed STOP (doute) ≠ skip déterministe (bundle/console/…).
- **RV9. Hermes présenté optionnel alors que son proxy CDP est requis** — le
  superviseur Hermes est optionnel ; son **proxy CDP** (172.17.0.1:9223) est
  **requis** sur le VPS. Distinguer.
- **RV10. `743` codé en dur** — retirer le nombre figé (ou le rendre générique).
- **RV11. Roadmap entièrement cochée malgré des dettes** — rouvrir des items
  (RV2, RV3, RV7…).
- **RV12. Cap de 100 candidats non justifié** — expliquer d'où vient le plafond.
- **RV13. Reprise après état UNKNOWN sans runbook explicite** — runbook clair
  (feed/CDP unreadable mid-run → vérif manuelle avant tout retry).
- **RV14. Persistance StepGuard vs BlockLedger non distinguée** — StepGuard =
  in-memory, per-process ; BlockLedger = persisté, cross-process, dans le run dir.

## Verdict (revue)

Le problème « une suggestion non confirmée pourrait-elle entrer dans le plan ? »
est fermé (technique : non ; ergonomie : confirmation explicite SF2-B ; serveur :
défense en profondeur SF1). **Canaries unitaires supervisés = raisonnables ;
déblocage général de `--mode safe` = NON** tant que RV2 + RV3 ne sont pas faits.

## Journal
- 2026-07-22 : RV1 FIXED (batch re-bloqué), RV4/RV5 FIXED (Learning doc). RV2/RV3
  ouverts (dette batch). RV6-RV14 délégués (README).
